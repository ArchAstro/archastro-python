# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: f97b2d7bdc4a

import pytest

from archastro.platform import PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


async def test_config_list_success():
    client = _client()
    result = await client.v1.config.list()
    assert result is not None
    assert "data" in result
    assert isinstance(result["data"], list)


async def test_config_list_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.list()
    assert exc_info.value.status == 400


async def test_config_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.list()
    assert exc_info.value.status == 401


async def test_config_create_success():
    client = _client()
    result = await client.v1.config.create(
        {"kind": "test", "mime_type": "application/json", "raw_content": "test content"}
    )
    assert result is not None


async def test_config_create_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.create(
            {"kind": "test", "mime_type": "application/json", "raw_content": "test content"}
        )
    assert exc_info.value.status == 400


async def test_config_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.create(
            {"kind": "test", "mime_type": "application/json", "raw_content": "test content"}
        )
    assert exc_info.value.status == 401


async def test_config_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.create(
            {"kind": "test", "mime_type": "application/json", "raw_content": "test content"}
        )
    assert exc_info.value.status == 422


async def test_config_encrypt_secret_success():
    client = _client()
    result = await client.v1.config.encrypt_secret({"plaintext": "test-value"})
    assert result is not None


async def test_config_encrypt_secret_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.encrypt_secret({"plaintext": "test-value"})
    assert exc_info.value.status == 401


async def test_config_encrypt_secret_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.encrypt_secret({"plaintext": "test-value"})
    assert exc_info.value.status == 422


async def test_config_validate_success():
    client = _client()
    result = await client.v1.config.validate(
        {"kind": "test", "mime_type": "application/json", "raw_content": "test content"}
    )
    assert result is not None


async def test_config_validate_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.validate(
            {"kind": "test", "mime_type": "application/json", "raw_content": "test content"}
        )
    assert exc_info.value.status == 400


async def test_config_validate_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.validate(
            {"kind": "test", "mime_type": "application/json", "raw_content": "test content"}
        )
    assert exc_info.value.status == 401


async def test_config_delete_success():
    client = _client()
    result = await client.v1.config.delete("test-value")
    assert result is None


async def test_config_delete_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.delete("test-value")
    assert exc_info.value.status == 400


async def test_config_delete_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.delete("test-value")
    assert exc_info.value.status == 401


async def test_config_delete_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.delete("test-value")
    assert exc_info.value.status == 404


async def test_config_get_success():
    client = _client()
    result = await client.v1.config.get("test-value")
    assert result is not None


async def test_config_get_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.get("test-value")
    assert exc_info.value.status == 400


async def test_config_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.get("test-value")
    assert exc_info.value.status == 401


async def test_config_get_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.get("test-value")
    assert exc_info.value.status == 404


async def test_config_replace_success():
    client = _client()
    result = await client.v1.config.replace(
        "test-value",
        {"config": "test-value", "mime_type": "application/json", "raw_content": "test content"},
    )
    assert result is not None


async def test_config_replace_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.replace(
            "test-value",
            {
                "config": "test-value",
                "mime_type": "application/json",
                "raw_content": "test content",
            },
        )
    assert exc_info.value.status == 400


async def test_config_replace_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.replace(
            "test-value",
            {
                "config": "test-value",
                "mime_type": "application/json",
                "raw_content": "test content",
            },
        )
    assert exc_info.value.status == 401


async def test_config_replace_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.replace(
            "test-value",
            {
                "config": "test-value",
                "mime_type": "application/json",
                "raw_content": "test content",
            },
        )
    assert exc_info.value.status == 404


async def test_config_replace_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.replace(
            "test-value",
            {
                "config": "test-value",
                "mime_type": "application/json",
                "raw_content": "test content",
            },
        )
    assert exc_info.value.status == 422


async def test_config_archive_success():
    client = _client()
    result = await client.v1.config.archive("test-value", {"config": "test-value"})
    assert result is not None


async def test_config_archive_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.archive("test-value", {"config": "test-value"})
    assert exc_info.value.status == 400


async def test_config_archive_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.archive("test-value", {"config": "test-value"})
    assert exc_info.value.status == 401


async def test_config_archive_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.archive("test-value", {"config": "test-value"})
    assert exc_info.value.status == 404


async def test_config_content_success():
    client = _client()
    result = await client.v1.config.content("test-value")
    assert result["content"] is not None
    assert result["mime_type"]


async def test_config_content_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.content("test-value")
    assert exc_info.value.status == 400


async def test_config_content_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.content("test-value")
    assert exc_info.value.status == 401


async def test_config_content_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.content("test-value")
    assert exc_info.value.status == 404


async def test_config_unarchive_success():
    client = _client()
    result = await client.v1.config.unarchive("test-value", {"config": "test-value"})
    assert result is not None


async def test_config_unarchive_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.unarchive("test-value", {"config": "test-value"})
    assert exc_info.value.status == 400


async def test_config_unarchive_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.unarchive("test-value", {"config": "test-value"})
    assert exc_info.value.status == 401


async def test_config_unarchive_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.unarchive("test-value", {"config": "test-value"})
    assert exc_info.value.status == 404


async def test_config_versions_success():
    client = _client()
    result = await client.v1.config.versions("test-value")
    assert result is not None


async def test_config_versions_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.versions("test-value")
    assert exc_info.value.status == 400


async def test_config_versions_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.versions("test-value")
    assert exc_info.value.status == 401


async def test_config_versions_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.versions("test-value")
    assert exc_info.value.status == 404


async def test_config_kinds_list_success():
    client = _client()
    result = await client.v1.config.kinds.list()
    assert result is not None
    assert "data" in result
    assert isinstance(result["data"], list)


async def test_config_kinds_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.kinds.list()
    assert exc_info.value.status == 401


async def test_config_kinds_schema_success():
    client = _client()
    result = await client.v1.config.kinds.schema("test")
    assert result is not None


async def test_config_kinds_schema_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.kinds.schema("test")
    assert exc_info.value.status == 401


async def test_config_kinds_schema_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.kinds.schema("test")
    assert exc_info.value.status == 404


async def test_config_system_list_success():
    client = _client()
    result = await client.v1.config.system.list()
    assert result is not None
    assert "data" in result
    assert isinstance(result["data"], list)


async def test_config_system_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.system.list()
    assert exc_info.value.status == 401


async def test_config_system_get_success():
    client = _client()
    result = await client.v1.config.system.get("test-value")
    assert result is not None


async def test_config_system_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.system.get("test-value")
    assert exc_info.value.status == 401


async def test_config_system_get_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.system.get("test-value")
    assert exc_info.value.status == 404


async def test_config_system_clone_success():
    client = _client()
    result = await client.v1.config.system.clone("test-value", {"system": "test-value"})
    assert result is not None


async def test_config_system_clone_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.system.clone("test-value", {"system": "test-value"})
    assert exc_info.value.status == 400


async def test_config_system_clone_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.system.clone("test-value", {"system": "test-value"})
    assert exc_info.value.status == 401


async def test_config_system_clone_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.system.clone("test-value", {"system": "test-value"})
    assert exc_info.value.status == 404


async def test_config_system_clone_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.config.system.clone("test-value", {"system": "test-value"})
    assert exc_info.value.status == 422
