# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: b1d6f6ca9577

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_solutions_list_success():
    client = _client()
    try:
        result = client.v1.solutions.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionListResponse"
    finally:
        client.close()


def test_solutions_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.list()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_solutions_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.list()
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_list_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.list()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.list()
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_solutions_create_success():
    client = _client()
    try:
        result = client.v1.solutions.create({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionImportResponse"
    finally:
        client.close()


def test_solutions_create_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.create({})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_solutions_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.create({})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_solutions_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.create({})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_solutions_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.create({})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_solutions_create_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.create({})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_solutions_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.create({})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_create_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.create({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionImportResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_create_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.create({})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.create({})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.create({})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.create({})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_create_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.create({})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.create({})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_solutions_delete_success():
    client = _client()
    try:
        result = client.v1.solutions.delete("test-value")
        assert result is None
    finally:
        client.close()


def test_solutions_delete_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_solutions_delete_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_solutions_delete_error_500():
    ec = _error_client(500)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.delete("test-value")
        assert exc_info.value.status == 500
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_delete_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.delete("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_delete_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_delete_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_delete_error_500():
    ec = _async_error_client(500)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.delete("test-value")
        assert exc_info.value.status == 500
    finally:
        await ec.close()


def test_solutions_get_success():
    client = _client()
    try:
        result = client.v1.solutions.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionSummary"
    finally:
        client.close()


def test_solutions_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_solutions_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.get("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_solutions_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.get("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_get_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionSummary"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.get("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.get("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_solutions_dependents_success():
    client = _client()
    try:
        result = client.v1.solutions.dependents("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionDependentsResponse"
    finally:
        client.close()


def test_solutions_dependents_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.dependents("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_solutions_dependents_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.dependents("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_dependents_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.dependents("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionDependentsResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_dependents_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.dependents("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_dependents_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.dependents("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_solutions_image_success():
    client = _client()
    try:
        result = client.v1.solutions.image("test-value", token="test-token")
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        client.close()


def test_solutions_image_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.image("test-value", token="test-token")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_image_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.image("test-value", token="test-token")
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_image_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.image("test-value", token="test-token")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_solutions_install_success():
    client = _client()
    try:
        result = client.v1.solutions.install("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionInstallResponse"
    finally:
        client.close()


def test_solutions_install_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_solutions_install_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_solutions_install_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_solutions_install_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_solutions_install_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_solutions_install_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_install_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.install("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionInstallResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_install_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_install_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_install_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_install_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_install_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_install_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.install("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_solutions_readme_success():
    client = _client()
    try:
        result = client.v1.solutions.readme("test-value", token="test-token")
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        client.close()


def test_solutions_readme_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.readme("test-value", token="test-token")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_solutions_readme_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.readme("test-value", token="test-token")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_readme_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.readme("test-value", token="test-token")
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_readme_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.readme("test-value", token="test-token")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_readme_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.readme("test-value", token="test-token")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_solutions_upgrade_success():
    client = _client()
    try:
        result = client.v1.solutions.upgrade("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionUpgradeResponse"
    finally:
        client.close()


def test_solutions_upgrade_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_solutions_upgrade_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_solutions_upgrade_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_solutions_upgrade_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_solutions_upgrade_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_upgrade_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.upgrade("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "SolutionUpgradeResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_upgrade_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_upgrade_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_upgrade_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_upgrade_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_upgrade_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.upgrade("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_solutions_view_success():
    client = _client()
    try:
        result = client.v1.solutions.view("test-value", {})
        assert result is None
    finally:
        client.close()


def test_solutions_view_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.view("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_solutions_view_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.view("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_solutions_view_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.solutions.view("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_solutions_view_success():
    client = _async_client()
    try:
        result = await client.v1.solutions.view("test-value", {})
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_solutions_view_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.view("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_view_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.view("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_solutions_view_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.solutions.view("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()
