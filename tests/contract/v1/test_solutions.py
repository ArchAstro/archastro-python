# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 6dce040e5f75

import pytest

from archastro.platform import PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


async def test_solutions_list_success():
    client = _client()
    result = await client.v1.solutions.list()
    assert result is not None


async def test_solutions_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.list()
    assert exc_info.value.status == 401


async def test_solutions_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.list()
    assert exc_info.value.status == 403


async def test_solutions_create_success():
    client = _client()
    result = await client.v1.solutions.create({})
    assert result is not None


async def test_solutions_create_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.create({})
    assert exc_info.value.status == 400


async def test_solutions_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.create({})
    assert exc_info.value.status == 401


async def test_solutions_create_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.create({})
    assert exc_info.value.status == 403


async def test_solutions_create_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.create({})
    assert exc_info.value.status == 404


async def test_solutions_create_error_409():
    ec = _error_client(409)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.create({})
    assert exc_info.value.status == 409


async def test_solutions_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.create({})
    assert exc_info.value.status == 422


async def test_solutions_delete_success():
    client = _client()
    result = await client.v1.solutions.delete("test-value")
    assert result is None


async def test_solutions_delete_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.delete("test-value")
    assert exc_info.value.status == 403


async def test_solutions_delete_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.delete("test-value")
    assert exc_info.value.status == 404


async def test_solutions_delete_error_500():
    ec = _error_client(500)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.delete("test-value")
    assert exc_info.value.status == 500


async def test_solutions_get_success():
    client = _client()
    result = await client.v1.solutions.get("test-value")
    assert result is not None


async def test_solutions_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.get("test-value")
    assert exc_info.value.status == 401


async def test_solutions_get_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.get("test-value")
    assert exc_info.value.status == 403


async def test_solutions_get_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.get("test-value")
    assert exc_info.value.status == 404


async def test_solutions_dependents_success():
    client = _client()
    result = await client.v1.solutions.dependents("test-value")
    assert result is not None


async def test_solutions_dependents_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.dependents("test-value")
    assert exc_info.value.status == 403


async def test_solutions_dependents_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.dependents("test-value")
    assert exc_info.value.status == 404


async def test_solutions_install_success():
    client = _client()
    result = await client.v1.solutions.install("test-value", {})
    assert result is not None


async def test_solutions_install_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.install("test-value", {})
    assert exc_info.value.status == 400


async def test_solutions_install_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.install("test-value", {})
    assert exc_info.value.status == 401


async def test_solutions_install_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.install("test-value", {})
    assert exc_info.value.status == 403


async def test_solutions_install_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.install("test-value", {})
    assert exc_info.value.status == 404


async def test_solutions_install_error_409():
    ec = _error_client(409)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.install("test-value", {})
    assert exc_info.value.status == 409


async def test_solutions_install_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.install("test-value", {})
    assert exc_info.value.status == 422


async def test_solutions_readme_success():
    client = _client()
    result = await client.v1.solutions.readme("test-value", token="test-token")
    assert result["content"] is not None
    assert result["mime_type"]


async def test_solutions_readme_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.readme("test-value", token="test-token")
    assert exc_info.value.status == 401


async def test_solutions_readme_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.readme("test-value", token="test-token")
    assert exc_info.value.status == 404


async def test_solutions_upgrade_success():
    client = _client()
    result = await client.v1.solutions.upgrade("test-value", {})
    assert result is not None


async def test_solutions_upgrade_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.upgrade("test-value", {})
    assert exc_info.value.status == 400


async def test_solutions_upgrade_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.upgrade("test-value", {})
    assert exc_info.value.status == 403


async def test_solutions_upgrade_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.upgrade("test-value", {})
    assert exc_info.value.status == 404


async def test_solutions_upgrade_error_409():
    ec = _error_client(409)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.upgrade("test-value", {})
    assert exc_info.value.status == 409


async def test_solutions_upgrade_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.solutions.upgrade("test-value", {})
    assert exc_info.value.status == 422
