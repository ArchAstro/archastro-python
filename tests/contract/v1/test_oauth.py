# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 302401dfe8b9

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_oauth_scopes_success():
    client = _client()
    try:
        result = client.v1.oauth.scopes()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OauthScopesResponse"
    finally:
        client.close()


@pytest.mark.asyncio
async def test_async_oauth_scopes_success():
    client = _async_client()
    try:
        result = await client.v1.oauth.scopes()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OauthScopesResponse"
    finally:
        await client.close()


def test_oauth_token_success():
    client = _client()
    try:
        result = client.v1.oauth.token({"grant_type": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OAuthTokenResponse"
    finally:
        client.close()


def test_oauth_token_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.token({"grant_type": "test-value"})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_oauth_token_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.token({"grant_type": "test-value"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_oauth_token_error_429():
    ec = _error_client(429)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.token({"grant_type": "test-value"})
        assert exc_info.value.status == 429
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_oauth_token_success():
    client = _async_client()
    try:
        result = await client.v1.oauth.token({"grant_type": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OAuthTokenResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_oauth_token_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.token({"grant_type": "test-value"})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_oauth_token_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.token({"grant_type": "test-value"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_oauth_token_error_429():
    ec = _async_error_client(429)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.token({"grant_type": "test-value"})
        assert exc_info.value.status == 429
    finally:
        await ec.close()


def test_oauth_device_approve_success():
    client = _client()
    try:
        result = client.v1.oauth.device.approve({"user_code": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationStatusResponse"
    finally:
        client.close()


def test_oauth_device_approve_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.approve({"user_code": "test-value"})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_oauth_device_approve_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.approve({"user_code": "test-value"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_approve_success():
    client = _async_client()
    try:
        result = await client.v1.oauth.device.approve({"user_code": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationStatusResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_oauth_device_approve_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.approve({"user_code": "test-value"})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_approve_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.approve({"user_code": "test-value"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


def test_oauth_device_authorization_success():
    client = _client()
    try:
        result = client.v1.oauth.device.authorization(code="test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationDetailsResponse"
    finally:
        client.close()


def test_oauth_device_authorization_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.authorization(code="test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_oauth_device_authorization_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.authorization(code="test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_authorization_success():
    client = _async_client()
    try:
        result = await client.v1.oauth.device.authorization(code="test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationDetailsResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_oauth_device_authorization_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.authorization(code="test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_authorization_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.authorization(code="test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


def test_oauth_device_authorize_success():
    client = _client()
    try:
        result = client.v1.oauth.device.authorize({"client": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationResponse"
    finally:
        client.close()


def test_oauth_device_authorize_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.authorize({"client": "test-value"})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_oauth_device_authorize_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.authorize({"client": "test-value"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_oauth_device_authorize_error_429():
    ec = _error_client(429)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.authorize({"client": "test-value"})
        assert exc_info.value.status == 429
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_authorize_success():
    client = _async_client()
    try:
        result = await client.v1.oauth.device.authorize({"client": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_oauth_device_authorize_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.authorize({"client": "test-value"})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_authorize_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.authorize({"client": "test-value"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_authorize_error_429():
    ec = _async_error_client(429)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.authorize({"client": "test-value"})
        assert exc_info.value.status == 429
    finally:
        await ec.close()


def test_oauth_device_deny_success():
    client = _client()
    try:
        result = client.v1.oauth.device.deny({"user_code": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationStatusResponse"
    finally:
        client.close()


def test_oauth_device_deny_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.deny({"user_code": "test-value"})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_oauth_device_deny_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.oauth.device.deny({"user_code": "test-value"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_deny_success():
    client = _async_client()
    try:
        result = await client.v1.oauth.device.deny({"user_code": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "DeviceAuthorizationStatusResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_oauth_device_deny_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.deny({"user_code": "test-value"})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_oauth_device_deny_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.oauth.device.deny({"user_code": "test-value"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()
