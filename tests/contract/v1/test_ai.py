# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 074b610d41b5

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_ai_chat_models_success():
    client = _client()
    try:
        result = client.v1.ai.chat.models()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ChatModelsResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_ai_chat_models_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.chat.models()
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_ai_chat_models_success():
    client = _async_client()
    try:
        result = await client.v1.ai.chat.models()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ChatModelsResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_ai_chat_models_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.chat.models()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


def test_ai_chat_completions_create_success():
    client = _client()
    try:
        result = client.v1.ai.chat.completions.create(
            {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AICompletionResult"
    finally:
        client.close()


def test_ai_chat_completions_create_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_ai_chat_completions_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_ai_chat_completions_create_error_402():
    ec = _error_client(402)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 402
    finally:
        ec.close()


def test_ai_chat_completions_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_ai_chat_completions_create_success():
    client = _async_client()
    try:
        result = await client.v1.ai.chat.completions.create(
            {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AICompletionResult"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_ai_chat_completions_create_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_ai_chat_completions_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_ai_chat_completions_create_error_402():
    ec = _async_error_client(402)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 402
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_ai_chat_completions_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.chat.completions.create(
                {"messages": [{"role": "user"}], "opts": {"model": "test-model"}}
            )
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_ai_image_edits_success():
    client = _client()
    try:
        result = client.v1.ai.image.edits(
            {
                "images": [{"image_data": "test-value", "image_type": "test-value"}],
                "prompt": "test-value",
            }
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AIImageResult"
    finally:
        client.close()


def test_ai_image_edits_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.image.edits(
                {
                    "images": [{"image_data": "test-value", "image_type": "test-value"}],
                    "prompt": "test-value",
                }
            )
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_ai_image_edits_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.image.edits(
                {
                    "images": [{"image_data": "test-value", "image_type": "test-value"}],
                    "prompt": "test-value",
                }
            )
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_ai_image_edits_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.image.edits(
                {
                    "images": [{"image_data": "test-value", "image_type": "test-value"}],
                    "prompt": "test-value",
                }
            )
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_ai_image_edits_success():
    client = _async_client()
    try:
        result = await client.v1.ai.image.edits(
            {
                "images": [{"image_data": "test-value", "image_type": "test-value"}],
                "prompt": "test-value",
            }
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AIImageResult"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_ai_image_edits_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.image.edits(
                {
                    "images": [{"image_data": "test-value", "image_type": "test-value"}],
                    "prompt": "test-value",
                }
            )
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_ai_image_edits_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.image.edits(
                {
                    "images": [{"image_data": "test-value", "image_type": "test-value"}],
                    "prompt": "test-value",
                }
            )
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_ai_image_edits_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.image.edits(
                {
                    "images": [{"image_data": "test-value", "image_type": "test-value"}],
                    "prompt": "test-value",
                }
            )
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_ai_image_generations_success():
    client = _client()
    try:
        result = client.v1.ai.image.generations({"prompt": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AIImageResult"
    finally:
        client.close()


def test_ai_image_generations_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.image.generations({"prompt": "test-value"})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_ai_image_generations_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.image.generations({"prompt": "test-value"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_ai_image_generations_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.image.generations({"prompt": "test-value"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_ai_image_generations_success():
    client = _async_client()
    try:
        result = await client.v1.ai.image.generations({"prompt": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AIImageResult"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_ai_image_generations_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.image.generations({"prompt": "test-value"})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_ai_image_generations_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.image.generations({"prompt": "test-value"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_ai_image_generations_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.image.generations({"prompt": "test-value"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_ai_image_models_success():
    client = _client()
    try:
        result = client.v1.ai.image.models()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ImageModelsResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_ai_image_models_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.ai.image.models()
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_ai_image_models_success():
    client = _async_client()
    try:
        result = await client.v1.ai.image.models()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ImageModelsResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_ai_image_models_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.ai.image.models()
        assert exc_info.value.status == 401
    finally:
        await ec.close()
