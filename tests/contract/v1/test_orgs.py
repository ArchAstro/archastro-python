# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 4eed66ef003a

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_orgs_list_success():
    client = _client()
    try:
        result = client.v1.orgs.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgListResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_orgs_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.list()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_orgs_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.list()
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_orgs_list_success():
    client = _async_client()
    try:
        result = await client.v1.orgs.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgListResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_orgs_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.list()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.list()
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_orgs_artifacts_success():
    client = _client()
    try:
        result = client.v1.orgs.artifacts("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgArtifactsResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_orgs_artifacts_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_orgs_artifacts_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_orgs_artifacts_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_orgs_artifacts_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_orgs_artifacts_success():
    client = _async_client()
    try:
        result = await client.v1.orgs.artifacts("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgArtifactsResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_orgs_artifacts_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_artifacts_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_artifacts_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_artifacts_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.artifacts("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_orgs_linked_accounts_success():
    client = _client()
    try:
        result = client.v1.orgs.linked_accounts("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgLinkedAccountsResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_orgs_linked_accounts_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.linked_accounts("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_orgs_linked_accounts_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.linked_accounts("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_orgs_linked_accounts_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.linked_accounts("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_orgs_linked_accounts_success():
    client = _async_client()
    try:
        result = await client.v1.orgs.linked_accounts("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgLinkedAccountsResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_orgs_linked_accounts_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.linked_accounts("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_linked_accounts_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.linked_accounts("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_linked_accounts_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.linked_accounts("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_orgs_threads_list_success():
    client = _client()
    try:
        result = client.v1.orgs.threads.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgThreadListResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_orgs_threads_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.threads.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_orgs_threads_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.threads.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_list_success():
    client = _async_client()
    try:
        result = await client.v1.orgs.threads.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "OrgThreadListResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.threads.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.threads.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_orgs_threads_create_success():
    client = _client()
    try:
        result = client.v1.orgs.threads.create("test-value", {"thread": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Thread"
    finally:
        client.close()


def test_orgs_threads_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_orgs_threads_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_orgs_threads_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_orgs_threads_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_create_success():
    client = _async_client()
    try:
        result = await client.v1.orgs.threads.create("test-value", {"thread": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Thread"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_orgs_threads_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.orgs.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        await ec.close()
