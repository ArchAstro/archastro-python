# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: fe747ea01a12

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_users_me_success():
    client = _client()
    try:
        result = client.v1.users.me()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "User"
    finally:
        client.close()


def test_users_me_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.me()
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_me_success():
    client = _async_client()
    try:
        result = await client.v1.users.me()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "User"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_me_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.me()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


def test_users_get_success():
    client = _client()
    try:
        result = client.v1.users.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "User"
    finally:
        client.close()


def test_users_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_users_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.get("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_get_success():
    client = _async_client()
    try:
        result = await client.v1.users.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "User"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.get("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_users_invites_success():
    client = _client()
    try:
        result = client.v1.users.invites("test-value", {"invite": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserInvite"
    finally:
        client.close()


def test_users_invites_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_users_invites_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_users_invites_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_users_invites_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_users_invites_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_invites_success():
    client = _async_client()
    try:
        result = await client.v1.users.invites("test-value", {"invite": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserInvite"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_invites_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_invites_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_invites_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_invites_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_invites_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.invites("test-value", {"invite": {}})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_users_orgs_success():
    client = _client()
    try:
        result = client.v1.users.orgs("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserOrgsResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_users_orgs_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.orgs("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_orgs_success():
    client = _async_client()
    try:
        result = await client.v1.users.orgs("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserOrgsResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_orgs_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.orgs("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


def test_users_profile_success():
    client = _client()
    try:
        result = client.v1.users.profile("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "User"
    finally:
        client.close()


def test_users_profile_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.profile("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_users_profile_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.profile("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_profile_success():
    client = _async_client()
    try:
        result = await client.v1.users.profile("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "User"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_profile_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.profile("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_profile_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.profile("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_users_artifacts_list_success():
    client = _client()
    try:
        result = client.v1.users.artifacts.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserArtifactListResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_users_artifacts_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.artifacts.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_users_artifacts_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.artifacts.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_artifacts_list_success():
    client = _async_client()
    try:
        result = await client.v1.users.artifacts.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserArtifactListResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_artifacts_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.artifacts.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_artifacts_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.artifacts.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_users_artifacts_create_success():
    client = _client()
    try:
        result = client.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Artifact"
    finally:
        client.close()


def test_users_artifacts_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_users_artifacts_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_users_artifacts_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_artifacts_create_success():
    client = _async_client()
    try:
        result = await client.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Artifact"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_artifacts_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_artifacts_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_artifacts_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_users_threads_list_success():
    client = _client()
    try:
        result = client.v1.users.threads.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserThreadListResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_users_threads_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.threads.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_users_threads_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.threads.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_threads_list_success():
    client = _async_client()
    try:
        result = await client.v1.users.threads.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "UserThreadListResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_threads_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.threads.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_threads_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.threads.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_users_threads_create_success():
    client = _client()
    try:
        result = client.v1.users.threads.create("test-value", {"thread": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Thread"
    finally:
        client.close()


def test_users_threads_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_users_threads_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_users_threads_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.users.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_users_threads_create_success():
    client = _async_client()
    try:
        result = await client.v1.users.threads.create("test-value", {"thread": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Thread"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_users_threads_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_threads_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_users_threads_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.users.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        await ec.close()
