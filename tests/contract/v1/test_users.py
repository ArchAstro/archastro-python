# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: d908db9b9d4b

import pytest

from archastro.platform import PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


async def test_users_me_success():
    client = _client()
    result = await client.v1.users.me()
    assert result is not None


async def test_users_me_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.me()
    assert exc_info.value.status == 401


async def test_users_get_success():
    client = _client()
    result = await client.v1.users.get("test-value")
    assert result is not None


async def test_users_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.get("test-value")
    assert exc_info.value.status == 401


async def test_users_get_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.get("test-value")
    assert exc_info.value.status == 403


async def test_users_invites_success():
    client = _client()
    result = await client.v1.users.invites("test-value", {"invite": {}})
    assert result is not None


async def test_users_invites_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.invites("test-value", {"invite": {}})
    assert exc_info.value.status == 401


async def test_users_invites_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.invites("test-value", {"invite": {}})
    assert exc_info.value.status == 403


async def test_users_invites_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.invites("test-value", {"invite": {}})
    assert exc_info.value.status == 404


async def test_users_invites_error_409():
    ec = _error_client(409)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.invites("test-value", {"invite": {}})
    assert exc_info.value.status == 409


async def test_users_invites_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.invites("test-value", {"invite": {}})
    assert exc_info.value.status == 422


async def test_users_orgs_success():
    client = _client()
    result = await client.v1.users.orgs("test-value")
    assert result is not None
    assert "data" in result
    assert isinstance(result["data"], list)


async def test_users_orgs_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.orgs("test-value")
    assert exc_info.value.status == 401


async def test_users_profile_success():
    client = _client()
    result = await client.v1.users.profile("test-value", {})
    assert result is not None


async def test_users_profile_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.profile("test-value", {})
    assert exc_info.value.status == 401


async def test_users_profile_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.profile("test-value", {})
    assert exc_info.value.status == 422


async def test_users_artifacts_list_success():
    client = _client()
    result = await client.v1.users.artifacts.list("test-value")
    assert result is not None
    assert "data" in result
    assert isinstance(result["data"], list)


async def test_users_artifacts_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.artifacts.list("test-value")
    assert exc_info.value.status == 401


async def test_users_artifacts_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.artifacts.list("test-value")
    assert exc_info.value.status == 403


async def test_users_artifacts_create_success():
    client = _client()
    result = await client.v1.users.artifacts.create("test-value", {"artifact": {}})
    assert result is not None


async def test_users_artifacts_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.artifacts.create("test-value", {"artifact": {}})
    assert exc_info.value.status == 401


async def test_users_artifacts_create_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.artifacts.create("test-value", {"artifact": {}})
    assert exc_info.value.status == 403


async def test_users_artifacts_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.artifacts.create("test-value", {"artifact": {}})
    assert exc_info.value.status == 422


async def test_users_threads_list_success():
    client = _client()
    result = await client.v1.users.threads.list("test-value")
    assert result is not None
    assert "data" in result
    assert isinstance(result["data"], list)


async def test_users_threads_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.threads.list("test-value")
    assert exc_info.value.status == 401


async def test_users_threads_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.threads.list("test-value")
    assert exc_info.value.status == 403


async def test_users_threads_create_success():
    client = _client()
    result = await client.v1.users.threads.create("test-value", {"thread": {}})
    assert result is not None


async def test_users_threads_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.threads.create("test-value", {"thread": {}})
    assert exc_info.value.status == 401


async def test_users_threads_create_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.threads.create("test-value", {"thread": {}})
    assert exc_info.value.status == 403


async def test_users_threads_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.users.threads.create("test-value", {"thread": {}})
    assert exc_info.value.status == 422
