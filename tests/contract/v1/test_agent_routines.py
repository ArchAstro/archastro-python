# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 732092c410d9

import pytest

from archastro.platform import PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


async def test_agent_routines_list_success():
    client = _client()
    result = await client.v1.agent_routines.list()
    assert result is not None


async def test_agent_routines_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.list()
    assert exc_info.value.status == 401


async def test_agent_routines_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.list()
    assert exc_info.value.status == 403


async def test_agent_routines_presets_success():
    client = _client()
    result = await client.v1.agent_routines.presets()
    assert result is not None


async def test_agent_routines_presets_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.presets()
    assert exc_info.value.status == 401


async def test_agent_routines_presets_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.presets()
    assert exc_info.value.status == 403


async def test_agent_routines_delete_success():
    client = _client()
    result = await client.v1.agent_routines.delete("test-value")
    assert result is None


async def test_agent_routines_delete_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.delete("test-value")
    assert exc_info.value.status == 401


async def test_agent_routines_delete_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.delete("test-value")
    assert exc_info.value.status == 403


async def test_agent_routines_delete_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.delete("test-value")
    assert exc_info.value.status == 404


async def test_agent_routines_get_success():
    client = _client()
    result = await client.v1.agent_routines.get("test-value")
    assert result is not None


async def test_agent_routines_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.get("test-value")
    assert exc_info.value.status == 401


async def test_agent_routines_get_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.get("test-value")
    assert exc_info.value.status == 403


async def test_agent_routines_get_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.get("test-value")
    assert exc_info.value.status == 404


async def test_agent_routines_update_success():
    client = _client()
    result = await client.v1.agent_routines.update("test-value", {})
    assert result is not None


async def test_agent_routines_update_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.update("test-value", {})
    assert exc_info.value.status == 401


async def test_agent_routines_update_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.update("test-value", {})
    assert exc_info.value.status == 403


async def test_agent_routines_update_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.update("test-value", {})
    assert exc_info.value.status == 404


async def test_agent_routines_update_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.update("test-value", {})
    assert exc_info.value.status == 422


async def test_agent_routines_activate_success():
    client = _client()
    result = await client.v1.agent_routines.activate("test-value")
    assert result is not None


async def test_agent_routines_activate_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.activate("test-value")
    assert exc_info.value.status == 401


async def test_agent_routines_activate_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.activate("test-value")
    assert exc_info.value.status == 403


async def test_agent_routines_activate_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.activate("test-value")
    assert exc_info.value.status == 404


async def test_agent_routines_activate_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.activate("test-value")
    assert exc_info.value.status == 422


async def test_agent_routines_invoke_success():
    client = _client()
    result = await client.v1.agent_routines.invoke("test-value", {"message": "test-value"})
    assert result is not None


async def test_agent_routines_invoke_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
    assert exc_info.value.status == 401


async def test_agent_routines_invoke_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
    assert exc_info.value.status == 403


async def test_agent_routines_invoke_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
    assert exc_info.value.status == 404


async def test_agent_routines_invoke_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
    assert exc_info.value.status == 422


async def test_agent_routines_pause_success():
    client = _client()
    result = await client.v1.agent_routines.pause("test-value")
    assert result is not None


async def test_agent_routines_pause_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.pause("test-value")
    assert exc_info.value.status == 401


async def test_agent_routines_pause_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.pause("test-value")
    assert exc_info.value.status == 403


async def test_agent_routines_pause_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.pause("test-value")
    assert exc_info.value.status == 404


async def test_agent_routines_runs_success():
    client = _client()
    result = await client.v1.agent_routines.runs("test-value")
    assert result is not None


async def test_agent_routines_runs_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.runs("test-value")
    assert exc_info.value.status == 400


async def test_agent_routines_runs_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.runs("test-value")
    assert exc_info.value.status == 401


async def test_agent_routines_runs_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.runs("test-value")
    assert exc_info.value.status == 403


async def test_agent_routines_runs_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.runs("test-value")
    assert exc_info.value.status == 404


async def test_agent_routines_agent_routine_runs_get_success():
    client = _client()
    result = await client.v1.agent_routines.agent_routine_runs.get("test-value")
    assert result is not None


async def test_agent_routines_agent_routine_runs_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.agent_routine_runs.get("test-value")
    assert exc_info.value.status == 401


async def test_agent_routines_agent_routine_runs_get_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.agent_routine_runs.get("test-value")
    assert exc_info.value.status == 403


async def test_agent_routines_agent_routine_runs_get_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agent_routines.agent_routine_runs.get("test-value")
    assert exc_info.value.status == 404
