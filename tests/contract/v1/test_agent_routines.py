# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: a93b5562a95b

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_agent_routines_list_success():
    client = _client()
    try:
        result = client.v1.agent_routines.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineListResponse"
    finally:
        client.close()


def test_agent_routines_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.list()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.list()
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_list_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.list()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.list()
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_agent_routines_presets_success():
    client = _client()
    try:
        result = client.v1.agent_routines.presets()
        assert isinstance(result, list)
        assert all(type(item).__name__ == "RoutinePreset" for item in result)
    finally:
        client.close()


def test_agent_routines_presets_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.presets()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_presets_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.presets()
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_presets_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.presets()
        assert isinstance(result, list)
        assert all(type(item).__name__ == "RoutinePreset" for item in result)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_presets_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.presets()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_presets_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.presets()
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_agent_routines_delete_success():
    client = _client()
    try:
        result = client.v1.agent_routines.delete("test-value")
        assert result is None
    finally:
        client.close()


def test_agent_routines_delete_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_delete_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_delete_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_delete_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.delete("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_delete_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_delete_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_delete_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agent_routines_get_success():
    client = _client()
    try:
        result = client.v1.agent_routines.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        client.close()


def test_agent_routines_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.get("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.get("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_get_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.get("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.get("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agent_routines_update_success():
    client = _client()
    try:
        result = client.v1.agent_routines.update("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        client.close()


def test_agent_routines_update_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_update_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_update_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agent_routines_update_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_update_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.update("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_update_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_update_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_update_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_update_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agent_routines_activate_success():
    client = _client()
    try:
        result = client.v1.agent_routines.activate("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        client.close()


def test_agent_routines_activate_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_activate_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_activate_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agent_routines_activate_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_activate_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.activate("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_activate_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_activate_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_activate_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_activate_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.activate("test-value")
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agent_routines_invoke_success():
    client = _client()
    try:
        result = client.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineRun"
    finally:
        client.close()


def test_agent_routines_invoke_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_agent_routines_invoke_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_invoke_error_402():
    ec = _error_client(402)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 402
    finally:
        ec.close()


def test_agent_routines_invoke_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_invoke_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agent_routines_invoke_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_agent_routines_invoke_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineRun"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_error_402():
    ec = _async_error_client(402)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 402
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_invoke_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.invoke("test-value", {"message": "test-value"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agent_routines_pause_success():
    client = _client()
    try:
        result = client.v1.agent_routines.pause("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        client.close()


def test_agent_routines_pause_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.pause("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_pause_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.pause("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_pause_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.pause("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_pause_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.pause("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_pause_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.pause("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_pause_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.pause("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_pause_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.pause("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agent_routines_runs_success():
    client = _client()
    try:
        result = client.v1.agent_routines.runs("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineRunListResponse"
    finally:
        client.close()


def test_agent_routines_runs_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_agent_routines_runs_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_runs_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_runs_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_runs_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.runs("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineRunListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_runs_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_runs_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_runs_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_runs_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.runs("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agent_routines_agent_routine_runs_get_success():
    client = _client()
    try:
        result = client.v1.agent_routines.agent_routine_runs.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineRun"
    finally:
        client.close()


def test_agent_routines_agent_routine_runs_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.agent_routine_runs.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_agent_routine_runs_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.agent_routine_runs.get("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_agent_routine_runs_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.agent_routine_runs.get("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_get_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.agent_routine_runs.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutineRun"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.agent_routine_runs.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.agent_routine_runs.get("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.agent_routine_runs.get("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agent_routines_agent_routine_runs_journal_success():
    client = _client()
    try:
        result = client.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "RunJournalPage"
    finally:
        client.close()


def test_agent_routines_agent_routine_runs_journal_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_agent_routines_agent_routine_runs_journal_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agent_routines_agent_routine_runs_journal_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agent_routines_agent_routine_runs_journal_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_journal_success():
    client = _async_client()
    try:
        result = await client.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "RunJournalPage"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_journal_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_journal_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_journal_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agent_routines_agent_routine_runs_journal_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agent_routines.agent_routine_runs.journal("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()
