# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: dddc177190e5

import pytest

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_teams_list_success():
    client = _client()
    try:
        result = client.v1.teams.list()
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        client.close()


def test_teams_list_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.list()
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_teams_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.list()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.list()
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_list_success():
    client = _async_client()
    try:
        result = await client.v1.teams.list()
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_list_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.list()
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.list()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.list()
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_teams_create_success():
    client = _client()
    try:
        result = client.v1.teams.create({"name": "test-name"})
        assert result is not None
    finally:
        client.close()


def test_teams_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.create({"name": "test-name"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.create({"name": "test-name"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.create({"name": "test-name"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_create_success():
    client = _async_client()
    try:
        result = await client.v1.teams.create({"name": "test-name"})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.create({"name": "test-name"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.create({"name": "test-name"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.create({"name": "test-name"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_teams_join_by_code_success():
    client = _client()
    try:
        result = client.v1.teams.join_by_code({})
        assert result is not None
    finally:
        client.close()


def test_teams_join_by_code_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_teams_join_by_code_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_join_by_code_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_teams_join_by_code_error_429():
    ec = _error_client(429)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 429
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_by_code_success():
    client = _async_client()
    try:
        result = await client.v1.teams.join_by_code({})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_join_by_code_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_by_code_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_by_code_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_by_code_error_429():
    ec = _async_error_client(429)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join_by_code({})
        assert exc_info.value.status == 429
    finally:
        await ec.close()


def test_teams_delete_success():
    client = _client()
    try:
        result = client.v1.teams.delete("test-value")
        assert result is None
    finally:
        client.close()


def test_teams_delete_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_delete_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_delete_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_delete_success():
    client = _async_client()
    try:
        result = await client.v1.teams.delete("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_delete_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_delete_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_delete_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_get_success():
    client = _client()
    try:
        result = client.v1.teams.get("test-value")
        assert result is not None
    finally:
        client.close()


def test_teams_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.get("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.get("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_get_success():
    client = _async_client()
    try:
        result = await client.v1.teams.get("test-value")
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.get("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.get("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_update_success():
    client = _client()
    try:
        result = client.v1.teams.update("test-value", {})
        assert result is not None
    finally:
        client.close()


def test_teams_update_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_update_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_update_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_teams_update_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_update_success():
    client = _async_client()
    try:
        result = await client.v1.teams.update("test-value", {})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_update_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_update_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_update_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_update_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_teams_invite_success():
    client = _client()
    try:
        result = client.v1.teams.invite("test-value")
        assert result is not None
    finally:
        client.close()


def test_teams_invite_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.invite("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_invite_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.invite("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_invite_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.invite("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_invite_success():
    client = _async_client()
    try:
        result = await client.v1.teams.invite("test-value")
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_invite_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.invite("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_invite_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.invite("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_invite_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.invite("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_invites_success():
    client = _client()
    try:
        result = client.v1.teams.invites("test-value")
        assert result is not None
    finally:
        client.close()


def test_teams_invites_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.invites("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_invites_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.invites("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_invites_success():
    client = _async_client()
    try:
        result = await client.v1.teams.invites("test-value")
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_invites_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.invites("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_invites_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.invites("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_join_success():
    client = _client()
    try:
        result = client.v1.teams.join("test-value", {})
        assert result is None
    finally:
        client.close()


def test_teams_join_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_teams_join_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_join_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_join_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_success():
    client = _async_client()
    try:
        result = await client.v1.teams.join("test-value", {})
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_join_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_join_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.join("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_leave_success():
    client = _client()
    try:
        result = client.v1.teams.leave("test-value")
        assert result is None
    finally:
        client.close()


def test_teams_leave_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_leave_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_leave_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_teams_leave_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_leave_success():
    client = _async_client()
    try:
        result = await client.v1.teams.leave("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_leave_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_leave_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_leave_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_leave_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.leave("test-value")
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_teams_artifacts_list_success():
    client = _client()
    try:
        result = client.v1.teams.artifacts.list("test-value")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        client.close()


def test_teams_artifacts_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.artifacts.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_artifacts_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.artifacts.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_artifacts_list_success():
    client = _async_client()
    try:
        result = await client.v1.teams.artifacts.list("test-value")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_artifacts_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.artifacts.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_artifacts_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.artifacts.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_artifacts_create_success():
    client = _client()
    try:
        result = client.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert result is not None
    finally:
        client.close()


def test_teams_artifacts_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_artifacts_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_artifacts_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_artifacts_create_success():
    client = _async_client()
    try:
        result = await client.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_artifacts_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_artifacts_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_artifacts_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.artifacts.create("test-value", {"artifact": {}})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_teams_custom_objects_list_success():
    client = _client()
    try:
        result = client.v1.teams.custom_objects.list("test-value", type="test")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        client.close()


def test_teams_custom_objects_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.custom_objects.list("test-value", type="test")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_custom_objects_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.custom_objects.list("test-value", type="test")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_custom_objects_list_success():
    client = _async_client()
    try:
        result = await client.v1.teams.custom_objects.list("test-value", type="test")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_custom_objects_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.custom_objects.list("test-value", type="test")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_custom_objects_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.custom_objects.list("test-value", type="test")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_custom_objects_create_success():
    client = _client()
    try:
        result = client.v1.teams.custom_objects.create("test-value", {"fields": {}, "type": "test"})
        assert result is not None
    finally:
        client.close()


def test_teams_custom_objects_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.custom_objects.create("test-value", {"fields": {}, "type": "test"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_custom_objects_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.custom_objects.create("test-value", {"fields": {}, "type": "test"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_teams_custom_objects_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.custom_objects.create("test-value", {"fields": {}, "type": "test"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_custom_objects_create_success():
    client = _async_client()
    try:
        result = await client.v1.teams.custom_objects.create(
            "test-value", {"fields": {}, "type": "test"}
        )
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_custom_objects_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.custom_objects.create("test-value", {"fields": {}, "type": "test"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_custom_objects_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.custom_objects.create("test-value", {"fields": {}, "type": "test"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_custom_objects_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.custom_objects.create("test-value", {"fields": {}, "type": "test"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_teams_members_remove_success():
    client = _client()
    try:
        result = client.v1.teams.members.remove("test-value")
        assert result is None
    finally:
        client.close()


def test_teams_members_remove_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_teams_members_remove_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_members_remove_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_members_remove_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_remove_success():
    client = _async_client()
    try:
        result = await client.v1.teams.members.remove("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_members_remove_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_remove_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_remove_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_remove_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.remove("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_members_list_success():
    client = _client()
    try:
        result = client.v1.teams.members.list("test-value")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        client.close()


def test_teams_members_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_members_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_list_success():
    client = _async_client()
    try:
        result = await client.v1.teams.members.list("test-value")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_members_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_members_create_success():
    client = _client()
    try:
        result = client.v1.teams.members.create("test-value", {})
        assert result is not None
    finally:
        client.close()


def test_teams_members_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.create("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_members_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.create("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_teams_members_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.create("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_create_success():
    client = _async_client()
    try:
        result = await client.v1.teams.members.create("test-value", {})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_members_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.create("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.create("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.create("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_teams_members_update_success():
    client = _client()
    try:
        result = client.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert result is not None
    finally:
        client.close()


def test_teams_members_update_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_members_update_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_teams_members_update_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_teams_members_update_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_teams_members_update_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_update_success():
    client = _async_client()
    try:
        result = await client.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_members_update_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_update_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_update_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_update_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_members_update_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.members.update("test-value", "test-value", {"role": "user"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_teams_threads_list_success():
    client = _client()
    try:
        result = client.v1.teams.threads.list("test-value")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        client.close()


def test_teams_threads_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.threads.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_threads_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.threads.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_threads_list_success():
    client = _async_client()
    try:
        result = await client.v1.teams.threads.list("test-value")
        assert result is not None
        assert "data" in result
        assert isinstance(result["data"], list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_threads_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.threads.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_threads_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.threads.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_teams_threads_create_success():
    client = _client()
    try:
        result = client.v1.teams.threads.create("test-value", {"thread": {}})
        assert result is not None
    finally:
        client.close()


def test_teams_threads_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_teams_threads_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_teams_threads_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.teams.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_teams_threads_create_success():
    client = _async_client()
    try:
        result = await client.v1.teams.threads.create("test-value", {"thread": {}})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_teams_threads_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_threads_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_teams_threads_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.teams.threads.create("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        await ec.close()
