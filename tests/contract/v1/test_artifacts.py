# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 98451127b0cf

import pytest

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_artifacts_delete_success():
    client = _client()
    try:
        result = client.v1.artifacts.delete("test-value")
        assert result is None
    finally:
        client.close()


def test_artifacts_delete_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_artifacts_delete_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_artifacts_delete_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_delete_success():
    client = _async_client()
    try:
        result = await client.v1.artifacts.delete("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_artifacts_delete_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_delete_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_delete_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_artifacts_get_success():
    client = _client()
    try:
        result = client.v1.artifacts.get("test-value")
        assert result is not None
    finally:
        client.close()


def test_artifacts_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_artifacts_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.get("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_artifacts_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.get("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_get_success():
    client = _async_client()
    try:
        result = await client.v1.artifacts.get("test-value")
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_artifacts_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.get("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.get("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_artifacts_replace_success():
    client = _client()
    try:
        result = client.v1.artifacts.replace("test-value", {"from_version": 1})
        assert result is not None
    finally:
        client.close()


def test_artifacts_replace_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_artifacts_replace_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_artifacts_replace_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_artifacts_replace_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_artifacts_replace_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_replace_success():
    client = _async_client()
    try:
        result = await client.v1.artifacts.replace("test-value", {"from_version": 1})
        assert result is not None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_artifacts_replace_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_replace_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_replace_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_replace_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_replace_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.replace("test-value", {"from_version": 1})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_artifacts_archive_success():
    client = _client()
    try:
        result = client.v1.artifacts.archive("test-value")
        assert result is None
    finally:
        client.close()


def test_artifacts_archive_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.archive("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_artifacts_archive_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.archive("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_artifacts_archive_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.archive("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_archive_success():
    client = _async_client()
    try:
        result = await client.v1.artifacts.archive("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_artifacts_archive_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.archive("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_archive_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.archive("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_archive_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.archive("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_artifacts_content_success():
    client = _client()
    try:
        result = client.v1.artifacts.content("test-value")
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        client.close()


def test_artifacts_content_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_artifacts_content_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_artifacts_content_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_artifacts_content_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_content_success():
    client = _async_client()
    try:
        result = await client.v1.artifacts.content("test-value")
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_artifacts_content_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_content_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_content_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_artifacts_content_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.artifacts.content("test-value")
        assert exc_info.value.status == 422
    finally:
        await ec.close()
