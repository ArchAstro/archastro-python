# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 7f15a27dc1ad

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_event_subscriptions_list_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionPage"
    finally:
        client.close()


def test_event_subscriptions_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.list()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_list_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.list()
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_list_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionPage"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.list()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_list_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.list()
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_event_subscriptions_create_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.create(
            {"event_names": ["test-value"], "name": "test-name"}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscription"
    finally:
        client.close()


def test_event_subscriptions_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.create({"event_names": ["test-value"], "name": "test-name"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.create({"event_names": ["test-value"], "name": "test-name"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_create_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.create(
            {"event_names": ["test-value"], "name": "test-name"}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscription"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.create(
                {"event_names": ["test-value"], "name": "test-name"}
            )
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.create(
                {"event_names": ["test-value"], "name": "test-name"}
            )
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_event_subscriptions_delete_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.delete("test-value")
        assert result is None
    finally:
        client.close()


def test_event_subscriptions_delete_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_delete_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_delete_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.delete("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_delete_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_delete_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_event_subscriptions_get_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscription"
    finally:
        client.close()


def test_event_subscriptions_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.get("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_get_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscription"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.get("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_event_subscriptions_update_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.update("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscription"
    finally:
        client.close()


def test_event_subscriptions_update_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_update_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_event_subscriptions_update_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_update_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.update("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscription"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_update_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_update_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_update_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_event_subscriptions_claim_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.claim("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionClaim"
    finally:
        client.close()


def test_event_subscriptions_claim_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.claim("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_claim_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.claim("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_event_subscriptions_claim_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.claim("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_claim_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.claim("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionClaim"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_claim_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.claim("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_claim_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.claim("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_claim_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.claim("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_event_subscriptions_head_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.head("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionHead"
    finally:
        client.close()


def test_event_subscriptions_head_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.head("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_head_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.head("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_head_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.head("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionHead"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_head_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.head("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_head_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.head("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_event_subscriptions_queue_success():
    client = _client()
    try:
        result = client.v1.event_subscriptions.queue("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionQueue"
    finally:
        client.close()


def test_event_subscriptions_queue_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.queue("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_event_subscriptions_queue_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.queue("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_event_subscriptions_queue_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.event_subscriptions.queue("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_queue_success():
    client = _async_client()
    try:
        result = await client.v1.event_subscriptions.queue("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "EventSubscriptionQueue"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_queue_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.queue("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_queue_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.queue("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_event_subscriptions_queue_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.event_subscriptions.queue("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()
