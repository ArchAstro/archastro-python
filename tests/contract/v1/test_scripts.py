# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 72a06dbae6eb

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_scripts_language_success():
    client = _client()
    try:
        result = client.v1.scripts.language()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScriptLanguageSpec"
    finally:
        client.close()


def test_scripts_language_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.language()
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_scripts_language_success():
    client = _async_client()
    try:
        result = await client.v1.scripts.language()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScriptLanguageSpec"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_scripts_language_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.language()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


def test_scripts_llm_txt_success():
    client = _client()
    try:
        result = client.v1.scripts.llm_txt()
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        client.close()


def test_scripts_llm_txt_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.llm_txt()
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_scripts_llm_txt_success():
    client = _async_client()
    try:
        result = await client.v1.scripts.llm_txt()
        assert result["content"] is not None
        assert result["mime_type"]
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_scripts_llm_txt_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.llm_txt()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


def test_scripts_run_success():
    client = _client()
    try:
        result = client.v1.scripts.run({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScriptRunResult"
    finally:
        client.close()


def test_scripts_run_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.run({})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_scripts_run_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.run({})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_scripts_run_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.run({})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_scripts_run_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.run({})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_scripts_run_success():
    client = _async_client()
    try:
        result = await client.v1.scripts.run({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScriptRunResult"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_scripts_run_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.run({})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_scripts_run_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.run({})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_scripts_run_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.run({})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_scripts_run_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.run({})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_scripts_runtime_env_vars_success():
    client = _client()
    try:
        result = client.v1.scripts.runtime_env_vars()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "RuntimeEnvVarList"
    finally:
        client.close()


def test_scripts_runtime_env_vars_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.runtime_env_vars()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_scripts_runtime_env_vars_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.runtime_env_vars()
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_scripts_runtime_env_vars_success():
    client = _async_client()
    try:
        result = await client.v1.scripts.runtime_env_vars()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "RuntimeEnvVarList"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_scripts_runtime_env_vars_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.runtime_env_vars()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_scripts_runtime_env_vars_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.runtime_env_vars()
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_scripts_test_success():
    client = _client()
    try:
        result = client.v1.scripts.test({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScriptTestRunResult"
    finally:
        client.close()


def test_scripts_test_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.test({})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_scripts_test_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.test({})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_scripts_test_success():
    client = _async_client()
    try:
        result = await client.v1.scripts.test({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScriptTestRunResult"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_scripts_test_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.test({})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_scripts_test_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.test({})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_scripts_validate_success():
    client = _client()
    try:
        result = client.v1.scripts.validate({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ExpressionValidation"
    finally:
        client.close()


def test_scripts_validate_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.scripts.validate({})
        assert exc_info.value.status == 401
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_scripts_validate_success():
    client = _async_client()
    try:
        result = await client.v1.scripts.validate({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ExpressionValidation"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_scripts_validate_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.scripts.validate({})
        assert exc_info.value.status == 401
    finally:
        await ec.close()
