# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 14edaf6d6edd

import pytest
from pydantic import BaseModel

from archastro.platform import AsyncPlatformClient, PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def _async_client() -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _async_error_client(code: int) -> AsyncPlatformClient:
    return AsyncPlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


def test_agents_list_success():
    client = _client()
    try:
        result = client.v1.agents.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentListResponse"
    finally:
        client.close()


def test_agents_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.list()
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.list()
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.list()
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.list()
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.list()
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_agents_create_success():
    client = _client()
    try:
        result = client.v1.agents.create({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentCreateResponse"
    finally:
        client.close()


def test_agents_create_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.create({})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_agents_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.create({})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.create({})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.create({})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_create_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.create({})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_agents_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.create({})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_create_success():
    client = _async_client()
    try:
        result = await client.v1.agents.create({})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentCreateResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_create_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.create({})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.create({})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.create({})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.create({})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_create_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.create({})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.create({})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_delete_success():
    client = _client()
    try:
        result = client.v1.agents.delete("test-value")
        assert result is None
    finally:
        client.close()


def test_agents_delete_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_delete_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_delete_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_delete_success():
    client = _async_client()
    try:
        result = await client.v1.agents.delete("test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_delete_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.delete("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_delete_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.delete("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_delete_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.delete("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_get_success():
    client = _client()
    try:
        result = client.v1.agents.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Agent"
    finally:
        client.close()


def test_agents_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.get("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.get("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.get("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_get_success():
    client = _async_client()
    try:
        result = await client.v1.agents.get("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Agent"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.get("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.get("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.get("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_update_success():
    client = _client()
    try:
        result = client.v1.agents.update("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Agent"
    finally:
        client.close()


def test_agents_update_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_update_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_update_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_update_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_update_success():
    client = _async_client()
    try:
        result = await client.v1.agents.update("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Agent"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_update_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_update_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_update_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_update_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.update("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_agent_health_actions_success():
    client = _client()
    try:
        result = client.v1.agents.agent_health_actions("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "HealthActionListResponse"
    finally:
        client.close()


def test_agents_agent_health_actions_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_health_actions("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_health_actions_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_health_actions("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_health_actions_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_health_actions("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_health_actions_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_health_actions("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "HealthActionListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_health_actions_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_health_actions("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_health_actions_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_health_actions("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_health_actions_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_health_actions("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_agent_routines_success():
    client = _client()
    try:
        result = client.v1.agents.agent_routines(
            "test-value", {"handler_type": "test-value", "name": "test-name"}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        client.close()


def test_agents_agent_routines_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_routines_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_routines_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_agent_routines_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_routines_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_routines(
            "test-value", {"handler_type": "test-value", "name": "test-name"}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentRoutine"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_routines_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_routines_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_routines_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_routines_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_routines(
                "test-value", {"handler_type": "test-value", "name": "test-name"}
            )
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_export_success():
    client = _client()
    try:
        result = client.v1.agents.export("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentExport"
    finally:
        client.close()


def test_agents_export_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.export("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_export_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.export("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_export_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.export("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_export_success():
    client = _async_client()
    try:
        result = await client.v1.agents.export("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentExport"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_export_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.export("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_export_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.export("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_export_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.export("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_health_success():
    client = _client()
    try:
        result = client.v1.agents.health("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentHealth"
    finally:
        client.close()


def test_agents_health_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.health("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_health_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.health("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_health_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.health("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_health_success():
    client = _async_client()
    try:
        result = await client.v1.agents.health("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentHealth"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_health_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.health("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_health_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.health("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_health_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.health("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_search_success():
    client = _client()
    try:
        result = client.v1.agents.search("test-value", {"query": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentSearchResponse"
        assert isinstance(result.data, list)
    finally:
        client.close()


def test_agents_search_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.search("test-value", {"query": "test-value"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_search_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.search("test-value", {"query": "test-value"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_search_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.search("test-value", {"query": "test-value"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_search_success():
    client = _async_client()
    try:
        result = await client.v1.agents.search("test-value", {"query": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentSearchResponse"
        assert isinstance(result.data, list)
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_search_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.search("test-value", {"query": "test-value"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_search_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.search("test-value", {"query": "test-value"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_search_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.search("test-value", {"query": "test-value"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_threads_success():
    client = _client()
    try:
        result = client.v1.agents.threads("test-value", {"thread": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Thread"
    finally:
        client.close()


def test_agents_threads_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_threads_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_threads_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_threads_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_threads_success():
    client = _async_client()
    try:
        result = await client.v1.agents.threads("test-value", {"thread": {}})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Thread"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_threads_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_threads_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_threads_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_threads_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.threads("test-value", {"thread": {}})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_upgrade_success():
    client = _client()
    try:
        result = client.v1.agents.upgrade("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentUpgradeResponse"
    finally:
        client.close()


def test_agents_upgrade_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_agents_upgrade_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_upgrade_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_upgrade_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_upgrade_error_409():
    ec = _error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 409
    finally:
        ec.close()


def test_agents_upgrade_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_upgrade_success():
    client = _async_client()
    try:
        result = await client.v1.agents.upgrade("test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentUpgradeResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_upgrade_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_upgrade_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_upgrade_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_upgrade_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_upgrade_error_409():
    ec = _async_error_client(409)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 409
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_upgrade_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.upgrade("test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_agent_computers_list_success():
    client = _client()
    try:
        result = client.v1.agents.agent_computers.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentComputerListResponse"
    finally:
        client.close()


def test_agents_agent_computers_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_computers.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_computers_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_computers.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_computers_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_computers.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_computers.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentComputerListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_computers.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_computers.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_computers.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_agent_computers_create_success():
    client = _client()
    try:
        result = client.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentComputer"
    finally:
        client.close()


def test_agents_agent_computers_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_computers_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_computers_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_agent_computers_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_create_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentComputer"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_computers_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_computers.create("test-value", {"name": "test-name"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_agent_env_vars_list_success():
    client = _client()
    try:
        result = client.v1.agents.agent_env_vars.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentEnvVarMaskedList"
    finally:
        client.close()


def test_agents_agent_env_vars_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_env_vars.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_env_vars_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_env_vars.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_env_vars_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_env_vars.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_env_vars.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentEnvVarMaskedList"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_env_vars.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_env_vars.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_env_vars.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_agent_env_vars_create_success():
    client = _client()
    try:
        result = client.v1.agents.agent_env_vars.create(
            "test-value", {"key": "test-key", "value": "test-value"}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentEnvVarMasked"
    finally:
        client.close()


def test_agents_agent_env_vars_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_env_vars_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_env_vars_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_agent_env_vars_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_create_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_env_vars.create(
            "test-value", {"key": "test-key", "value": "test-value"}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentEnvVarMasked"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_env_vars_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_env_vars.create(
                "test-value", {"key": "test-key", "value": "test-value"}
            )
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_agent_installations_list_success():
    client = _client()
    try:
        result = client.v1.agents.agent_installations.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "InstallationListResponse"
    finally:
        client.close()


def test_agents_agent_installations_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_installations_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_installations_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_installations.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "InstallationListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_agent_installations_create_success():
    client = _client()
    try:
        result = client.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Installation"
    finally:
        client.close()


def test_agents_agent_installations_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_installations_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_installations_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_agent_installations_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_create_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "Installation"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_agent_installations_kinds_success():
    client = _client()
    try:
        result = client.v1.agents.agent_installations.kinds("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "InstallationKindListResponse"
    finally:
        client.close()


def test_agents_agent_installations_kinds_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.kinds("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_installations_kinds_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_installations.kinds("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_kinds_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_installations.kinds("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "InstallationKindListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_kinds_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.kinds("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_installations_kinds_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_installations.kinds("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_agents_agent_tools_list_success():
    client = _client()
    try:
        result = client.v1.agents.agent_tools.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentToolListResponse"
    finally:
        client.close()


def test_agents_agent_tools_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_tools.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_tools_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_tools.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_tools_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_tools.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_tools.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentToolListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_tools.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_tools.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_tools.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_agent_tools_create_success():
    client = _client()
    try:
        result = client.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentTool"
    finally:
        client.close()


def test_agents_agent_tools_create_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_tools_create_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_tools_create_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_agent_tools_create_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_create_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentTool"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_create_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_create_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_create_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_tools_create_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_tools.create("test-value", {"kind": "test"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_agent_working_memory_list_success():
    client = _client()
    try:
        result = client.v1.agents.agent_working_memory.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkingMemoryEntryListResponse"
    finally:
        client.close()


def test_agents_agent_working_memory_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_working_memory_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_working_memory_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_working_memory.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkingMemoryEntryListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_agent_working_memory_delete_success():
    client = _client()
    try:
        result = client.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert result is None
    finally:
        client.close()


def test_agents_agent_working_memory_delete_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_working_memory_delete_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_working_memory_delete_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_delete_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert result is None
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_delete_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_delete_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_delete_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.delete("test-value", "test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_agent_working_memory_update_success():
    client = _client()
    try:
        result = client.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkingMemoryEntry"
    finally:
        client.close()


def test_agents_agent_working_memory_update_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_agent_working_memory_update_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_agent_working_memory_update_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_agent_working_memory_update_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_update_success():
    client = _async_client()
    try:
        result = await client.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkingMemoryEntry"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_update_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_update_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_update_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_agent_working_memory_update_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.agent_working_memory.update("test-value", "test-value", {})
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_schedules_list_success():
    client = _client()
    try:
        result = client.v1.agents.schedules.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScheduleListResponse"
    finally:
        client.close()


def test_agents_schedules_list_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.schedules.list("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_agents_schedules_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.schedules.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_schedules_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.schedules.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.schedules.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "ScheduleListResponse"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_list_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.schedules.list("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.schedules.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.schedules.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


def test_agents_schedules_get_success():
    client = _client()
    try:
        result = client.v1.agents.schedules.get("test-value", "test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentSchedule"
    finally:
        client.close()


def test_agents_schedules_get_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.schedules.get("test-value", "test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_schedules_get_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.schedules.get("test-value", "test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_schedules_get_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.schedules.get("test-value", "test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_get_success():
    client = _async_client()
    try:
        result = await client.v1.agents.schedules.get("test-value", "test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "AgentSchedule"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_get_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.schedules.get("test-value", "test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_get_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.schedules.get("test-value", "test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_schedules_get_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.schedules.get("test-value", "test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


def test_agents_work_items_list_success():
    client = _client()
    try:
        result = client.v1.agents.work_items.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkflowWorkItemList"
    finally:
        client.close()


def test_agents_work_items_list_error_400():
    ec = _error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 400
    finally:
        ec.close()


def test_agents_work_items_list_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_work_items_list_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_work_items_list_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_work_items_list_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_list_success():
    client = _async_client()
    try:
        result = await client.v1.agents.work_items.list("test-value")
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkflowWorkItemList"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_list_error_400():
    ec = _async_error_client(400)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 400
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_list_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_list_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_list_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_list_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.list("test-value")
        assert exc_info.value.status == 422
    finally:
        await ec.close()


def test_agents_work_items_claim_success():
    client = _client()
    try:
        result = client.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkflowWorkItemClaim"
    finally:
        client.close()


def test_agents_work_items_claim_error_401():
    ec = _error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 401
    finally:
        ec.close()


def test_agents_work_items_claim_error_403():
    ec = _error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 403
    finally:
        ec.close()


def test_agents_work_items_claim_error_404():
    ec = _error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 404
    finally:
        ec.close()


def test_agents_work_items_claim_error_422():
    ec = _error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 422
    finally:
        ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_claim_success():
    client = _async_client()
    try:
        result = await client.v1.agents.work_items.claim(
            "test-value", {"lease_owner": "test-value"}
        )
        assert isinstance(result, BaseModel)
        assert type(result).__name__ == "WorkflowWorkItemClaim"
    finally:
        await client.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_claim_error_401():
    ec = _async_error_client(401)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 401
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_claim_error_403():
    ec = _async_error_client(403)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 403
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_claim_error_404():
    ec = _async_error_client(404)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 404
    finally:
        await ec.close()


@pytest.mark.asyncio
async def test_async_agents_work_items_claim_error_422():
    ec = _async_error_client(422)
    try:
        with pytest.raises(ApiError) as exc_info:
            await ec.v1.agents.work_items.claim("test-value", {"lease_owner": "test-value"})
        assert exc_info.value.status == 422
    finally:
        await ec.close()
