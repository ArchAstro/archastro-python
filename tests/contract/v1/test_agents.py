# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 4326b01f839d

import pytest

from archastro.platform import PlatformClient
from archastro.platform.runtime.http_client import ApiError

PRISM_URL = "http://127.0.0.1:4040"


def _client() -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key"},
        access_token="test-token",
    )


def _error_client(code: int) -> PlatformClient:
    return PlatformClient(
        base_url=PRISM_URL,
        default_headers={"x-archastro-api-key": "pk_test-key", "Prefer": f"code={code}"},
        access_token="test-token",
    )


async def test_agents_list_success():
    client = _client()
    result = await client.v1.agents.list()
    assert result is not None


async def test_agents_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.list()
    assert exc_info.value.status == 401


async def test_agents_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.list()
    assert exc_info.value.status == 403


async def test_agents_create_success():
    client = _client()
    result = await client.v1.agents.create({})
    assert result is not None


async def test_agents_create_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.create({})
    assert exc_info.value.status == 400


async def test_agents_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.create({})
    assert exc_info.value.status == 401


async def test_agents_create_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.create({})
    assert exc_info.value.status == 403


async def test_agents_create_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.create({})
    assert exc_info.value.status == 404


async def test_agents_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.create({})
    assert exc_info.value.status == 422


async def test_agents_delete_success():
    client = _client()
    result = await client.v1.agents.delete("test-value")
    assert result is None


async def test_agents_delete_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.delete("test-value")
    assert exc_info.value.status == 401


async def test_agents_delete_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.delete("test-value")
    assert exc_info.value.status == 403


async def test_agents_delete_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.delete("test-value")
    assert exc_info.value.status == 404


async def test_agents_get_success():
    client = _client()
    result = await client.v1.agents.get("test-value")
    assert result is not None


async def test_agents_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.get("test-value")
    assert exc_info.value.status == 401


async def test_agents_get_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.get("test-value")
    assert exc_info.value.status == 403


async def test_agents_get_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.get("test-value")
    assert exc_info.value.status == 404


async def test_agents_update_success():
    client = _client()
    result = await client.v1.agents.update("test-value", {"agent": "test-value"})
    assert result is not None


async def test_agents_update_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.update("test-value", {"agent": "test-value"})
    assert exc_info.value.status == 401


async def test_agents_update_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.update("test-value", {"agent": "test-value"})
    assert exc_info.value.status == 403


async def test_agents_update_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.update("test-value", {"agent": "test-value"})
    assert exc_info.value.status == 404


async def test_agents_update_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.update("test-value", {"agent": "test-value"})
    assert exc_info.value.status == 422


async def test_agents_agent_routines_success():
    client = _client()
    result = await client.v1.agents.agent_routines(
        "test-value", {"agent": "test-value", "handler_type": "test-value", "name": "test-name"}
    )
    assert result is not None


async def test_agents_agent_routines_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_routines(
            "test-value", {"agent": "test-value", "handler_type": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 401


async def test_agents_agent_routines_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_routines(
            "test-value", {"agent": "test-value", "handler_type": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 403


async def test_agents_agent_routines_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_routines(
            "test-value", {"agent": "test-value", "handler_type": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 404


async def test_agents_agent_routines_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_routines(
            "test-value", {"agent": "test-value", "handler_type": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 422


async def test_agents_agent_working_memory_success():
    client = _client()
    result = await client.v1.agents.agent_working_memory("test-value")
    assert result is not None


async def test_agents_agent_working_memory_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_working_memory("test-value")
    assert exc_info.value.status == 401


async def test_agents_agent_working_memory_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_working_memory("test-value")
    assert exc_info.value.status == 403


async def test_agents_agent_working_memory_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_working_memory("test-value")
    assert exc_info.value.status == 404


async def test_agents_export_success():
    client = _client()
    result = await client.v1.agents.export("test-value")
    assert result is not None


async def test_agents_export_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.export("test-value")
    assert exc_info.value.status == 401


async def test_agents_export_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.export("test-value")
    assert exc_info.value.status == 403


async def test_agents_export_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.export("test-value")
    assert exc_info.value.status == 404


async def test_agents_search_success():
    client = _client()
    result = await client.v1.agents.search(
        "test-value", {"agent": "test-value", "query": "test-value"}
    )
    assert result is not None
    assert "data" in result
    assert isinstance(result["data"], list)


async def test_agents_search_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.search("test-value", {"agent": "test-value", "query": "test-value"})
    assert exc_info.value.status == 401


async def test_agents_search_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.search("test-value", {"agent": "test-value", "query": "test-value"})
    assert exc_info.value.status == 404


async def test_agents_search_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.search("test-value", {"agent": "test-value", "query": "test-value"})
    assert exc_info.value.status == 422


async def test_agents_threads_success():
    client = _client()
    result = await client.v1.agents.threads("test-value", {"agent": "test-value", "thread": {}})
    assert result is not None


async def test_agents_threads_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.threads("test-value", {"agent": "test-value", "thread": {}})
    assert exc_info.value.status == 401


async def test_agents_threads_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.threads("test-value", {"agent": "test-value", "thread": {}})
    assert exc_info.value.status == 403


async def test_agents_threads_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.threads("test-value", {"agent": "test-value", "thread": {}})
    assert exc_info.value.status == 404


async def test_agents_threads_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.threads("test-value", {"agent": "test-value", "thread": {}})
    assert exc_info.value.status == 422


async def test_agents_agent_computers_list_success():
    client = _client()
    result = await client.v1.agents.agent_computers.list("test-value")
    assert result is not None


async def test_agents_agent_computers_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_computers.list("test-value")
    assert exc_info.value.status == 401


async def test_agents_agent_computers_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_computers.list("test-value")
    assert exc_info.value.status == 403


async def test_agents_agent_computers_list_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_computers.list("test-value")
    assert exc_info.value.status == 404


async def test_agents_agent_computers_create_success():
    client = _client()
    result = await client.v1.agents.agent_computers.create(
        "test-value", {"agent": "test-value", "name": "test-name"}
    )
    assert result is not None


async def test_agents_agent_computers_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_computers.create(
            "test-value", {"agent": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 401


async def test_agents_agent_computers_create_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_computers.create(
            "test-value", {"agent": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 403


async def test_agents_agent_computers_create_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_computers.create(
            "test-value", {"agent": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 404


async def test_agents_agent_computers_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_computers.create(
            "test-value", {"agent": "test-value", "name": "test-name"}
        )
    assert exc_info.value.status == 422


async def test_agents_agent_installations_list_success():
    client = _client()
    result = await client.v1.agents.agent_installations.list("test-value")
    assert result is not None


async def test_agents_agent_installations_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.list("test-value")
    assert exc_info.value.status == 401


async def test_agents_agent_installations_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.list("test-value")
    assert exc_info.value.status == 403


async def test_agents_agent_installations_list_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.list("test-value")
    assert exc_info.value.status == 404


async def test_agents_agent_installations_create_success():
    client = _client()
    result = await client.v1.agents.agent_installations.create(
        "test-value", {"agent": "test-value", "kind": "test"}
    )
    assert result is not None


async def test_agents_agent_installations_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.create(
            "test-value", {"agent": "test-value", "kind": "test"}
        )
    assert exc_info.value.status == 401


async def test_agents_agent_installations_create_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.create(
            "test-value", {"agent": "test-value", "kind": "test"}
        )
    assert exc_info.value.status == 403


async def test_agents_agent_installations_create_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.create(
            "test-value", {"agent": "test-value", "kind": "test"}
        )
    assert exc_info.value.status == 404


async def test_agents_agent_installations_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.create(
            "test-value", {"agent": "test-value", "kind": "test"}
        )
    assert exc_info.value.status == 422


async def test_agents_agent_installations_kinds_success():
    client = _client()
    result = await client.v1.agents.agent_installations.kinds("test-value")
    assert result is not None


async def test_agents_agent_installations_kinds_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.kinds("test-value")
    assert exc_info.value.status == 401


async def test_agents_agent_installations_kinds_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_installations.kinds("test-value")
    assert exc_info.value.status == 403


async def test_agents_agent_tools_list_success():
    client = _client()
    result = await client.v1.agents.agent_tools.list("test-value")
    assert result is not None


async def test_agents_agent_tools_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_tools.list("test-value")
    assert exc_info.value.status == 401


async def test_agents_agent_tools_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_tools.list("test-value")
    assert exc_info.value.status == 403


async def test_agents_agent_tools_create_success():
    client = _client()
    result = await client.v1.agents.agent_tools.create(
        "test-value", {"agent": "test-value", "kind": "test"}
    )
    assert result is not None


async def test_agents_agent_tools_create_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_tools.create("test-value", {"agent": "test-value", "kind": "test"})
    assert exc_info.value.status == 401


async def test_agents_agent_tools_create_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_tools.create("test-value", {"agent": "test-value", "kind": "test"})
    assert exc_info.value.status == 403


async def test_agents_agent_tools_create_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_tools.create("test-value", {"agent": "test-value", "kind": "test"})
    assert exc_info.value.status == 404


async def test_agents_agent_tools_create_error_422():
    ec = _error_client(422)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.agent_tools.create("test-value", {"agent": "test-value", "kind": "test"})
    assert exc_info.value.status == 422


async def test_agents_schedules_list_success():
    client = _client()
    result = await client.v1.agents.schedules.list("test-value")
    assert result is not None


async def test_agents_schedules_list_error_400():
    ec = _error_client(400)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.schedules.list("test-value")
    assert exc_info.value.status == 400


async def test_agents_schedules_list_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.schedules.list("test-value")
    assert exc_info.value.status == 401


async def test_agents_schedules_list_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.schedules.list("test-value")
    assert exc_info.value.status == 403


async def test_agents_schedules_get_success():
    client = _client()
    result = await client.v1.agents.schedules.get("test-value", "test-value")
    assert result is not None


async def test_agents_schedules_get_error_401():
    ec = _error_client(401)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.schedules.get("test-value", "test-value")
    assert exc_info.value.status == 401


async def test_agents_schedules_get_error_403():
    ec = _error_client(403)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.schedules.get("test-value", "test-value")
    assert exc_info.value.status == 403


async def test_agents_schedules_get_error_404():
    ec = _error_client(404)
    with pytest.raises(ApiError) as exc_info:
        await ec.v1.agents.schedules.get("test-value", "test-value")
    assert exc_info.value.status == 404
