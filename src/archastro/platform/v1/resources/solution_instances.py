# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: ce96189e7d0a

from __future__ import annotations

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.common import SolutionInstanceListResponse


class AsyncSolutionInstanceResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self,
        solution_template_config_id: str,
        *,
        limit: int | None = None,
        after_cursor: str | None = None,
    ) -> SolutionInstanceListResponse:
        """
        List customer solution instances
        Returns a forward cursor-paginated page of customer instances stamped from one
        solution template. Results are restricted to the caller's organization; an
        app-wide viewer without an organization scope is forbidden.
        Pass `after_cursor` from a response with `has_more: true` to retrieve the next
        page. Each row includes the materialized agent, pinned template version, and
        local-edit count computed by the solution-instance read path.

        Args:
            solution_template_config_id: ID of the installed solution template whose customer instances should be listed.
            limit: Maximum number of instances to return. Defaults to 50; maximum is 100.
            after_cursor: Opaque cursor returned by the preceding page. Omit to retrieve the first page.

        Returns:
            Cursor-paginated customer solution instances visible to the caller.
        """
        query: dict[str, object] = {}
        query["solution_template_config_id"] = solution_template_config_id
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return await self._http.request(
            "/api/v1/solution_instances",
            query=query,
            response_type=SolutionInstanceListResponse,
        )


class SolutionInstanceResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(
        self,
        solution_template_config_id: str,
        *,
        limit: int | None = None,
        after_cursor: str | None = None,
    ) -> SolutionInstanceListResponse:
        """
        List customer solution instances
        Returns a forward cursor-paginated page of customer instances stamped from one
        solution template. Results are restricted to the caller's organization; an
        app-wide viewer without an organization scope is forbidden.
        Pass `after_cursor` from a response with `has_more: true` to retrieve the next
        page. Each row includes the materialized agent, pinned template version, and
        local-edit count computed by the solution-instance read path.

        Args:
            solution_template_config_id: ID of the installed solution template whose customer instances should be listed.
            limit: Maximum number of instances to return. Defaults to 50; maximum is 100.
            after_cursor: Opaque cursor returned by the preceding page. Omit to retrieve the first page.

        Returns:
            Cursor-paginated customer solution instances visible to the caller.
        """
        query: dict[str, object] = {}
        query["solution_template_config_id"] = solution_template_config_id
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return self._http.request(
            "/api/v1/solution_instances",
            query=query,
            response_type=SolutionInstanceListResponse,
        )
