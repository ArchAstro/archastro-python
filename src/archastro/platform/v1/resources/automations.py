# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: f8a6c28afc3a

from __future__ import annotations

from typing import Any, TypedDict

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.automations import AutomationRun


class AutomationInvokeInput(TypedDict, total=False):
    "Invoke an automation"

    idempotency_key: str | None
    "Unique key to deduplicate concurrent or retried invocations. A second request with the same key returns the existing run instead of creating a new one."
    participants: dict[str, Any] | None
    "Map of symbolic participant refs to agent ids (`agi_...` or UUID) for distributed embed_agent handoffs. Stored in the run's top-level `participants` field."
    payload: dict[str, Any] | None
    "Free-form invoke input the workflow parameters. Validated against the automation's `input_schema` when configured. Stored as `event_payload` and becomes the workflow `$` after trigger unwrap (e.g. `{{$.bug}}`)."


class AsyncAutomationResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def invoke(self, automation: str, input: AutomationInvokeInput) -> AutomationRun:
        """
        Invoke an automation
        Triggers a single run of an automation that has `type: "invoked"`. Returns
        the resulting automation run object, which you can use to poll or display
        run status.
        Both server-to-server (secret key) and user (publishable key + JWT) auth
        are supported. The automation's `invoke_auth` setting controls which auth
        modes are accepted; requests using an unsupported mode are rejected with
        403. For server-to-server callers the run executes under the identity
        configured in the automation's `run_as_user` or `run_as_agent` fields. For
        authenticated user callers the invoking user's identity is used
        automatically.
        If you supply an `idempotency_key`, a second request with the same key
        returns the existing run rather than creating a new one.
        ## Body fields
        * `payload` free-form invoke input. **This is the workflow parameters**:
        validated against `input_schema` when configured, stored as
        `event_payload`, and becomes the workflow `$` after trigger unwrap
        (e.g. `{{$.bug}}` for `{"bug":"upload fails"}`).
        * `participants` optional map of symbolic participant refs to agent ids
        for distributed `embed_agent` nodes, e.g.
        `{"investigator":"agi_...","evaluator":"agi_..."}`. Stored in the
        run's top-level `participants` field (not inside free-form payload) and
        exposed to workflows through their system context.
        Same top-level `payload` + `participants` shape as routine invoke.

        Args:
            automation: Automation ID (`auto_...`) or `lookup_key` of the automation to invoke.
            input: Request body.
            input.idempotency_key: Unique key to deduplicate concurrent or retried invocations. A second request with the same key returns the existing run instead of creating a new one.
            input.participants: Map of symbolic participant refs to agent ids (`agi_...` or UUID) for distributed embed_agent handoffs. Stored in the run's top-level `participants` field.
            input.payload: Free-form invoke input the workflow parameters. Validated against the automation's `input_schema` when configured. Stored as `event_payload` and becomes the workflow `$` after trigger unwrap (e.g. `{{$.bug}}`).

        Returns:
            The automation run created by this invocation.
        """
        return await self._http.request(
            f"/api/v1/automations/{automation}/invoke",
            method="POST",
            body=input,
            response_type=AutomationRun,
        )


class AutomationResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def invoke(self, automation: str, input: AutomationInvokeInput) -> AutomationRun:
        """
        Invoke an automation
        Triggers a single run of an automation that has `type: "invoked"`. Returns
        the resulting automation run object, which you can use to poll or display
        run status.
        Both server-to-server (secret key) and user (publishable key + JWT) auth
        are supported. The automation's `invoke_auth` setting controls which auth
        modes are accepted; requests using an unsupported mode are rejected with
        403. For server-to-server callers the run executes under the identity
        configured in the automation's `run_as_user` or `run_as_agent` fields. For
        authenticated user callers the invoking user's identity is used
        automatically.
        If you supply an `idempotency_key`, a second request with the same key
        returns the existing run rather than creating a new one.
        ## Body fields
        * `payload` free-form invoke input. **This is the workflow parameters**:
        validated against `input_schema` when configured, stored as
        `event_payload`, and becomes the workflow `$` after trigger unwrap
        (e.g. `{{$.bug}}` for `{"bug":"upload fails"}`).
        * `participants` optional map of symbolic participant refs to agent ids
        for distributed `embed_agent` nodes, e.g.
        `{"investigator":"agi_...","evaluator":"agi_..."}`. Stored in the
        run's top-level `participants` field (not inside free-form payload) and
        exposed to workflows through their system context.
        Same top-level `payload` + `participants` shape as routine invoke.

        Args:
            automation: Automation ID (`auto_...`) or `lookup_key` of the automation to invoke.
            input: Request body.
            input.idempotency_key: Unique key to deduplicate concurrent or retried invocations. A second request with the same key returns the existing run instead of creating a new one.
            input.participants: Map of symbolic participant refs to agent ids (`agi_...` or UUID) for distributed embed_agent handoffs. Stored in the run's top-level `participants` field.
            input.payload: Free-form invoke input the workflow parameters. Validated against the automation's `input_schema` when configured. Stored as `event_payload` and becomes the workflow `$` after trigger unwrap (e.g. `{{$.bug}}`).

        Returns:
            The automation run created by this invocation.
        """
        return self._http.request(
            f"/api/v1/automations/{automation}/invoke",
            method="POST",
            body=input,
            response_type=AutomationRun,
        )
