# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: c745802568f5

from __future__ import annotations

import builtins

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.common import SolutionTagListResponse


class AsyncSolutionTagResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self,
        *,
        page: int | None = None,
        page_size: int | None = None,
        keys: builtins.list[str] | None = None,
        lookup_key: str | None = None,
        path_prefix: str | None = None,
        owners: builtins.list[str] | None = None,
    ) -> SolutionTagListResponse:
        """
        List solution tags
        Returns a paginated list of solution tag definitions visible to the authenticated
        user, scoped to their app. Results include tags from both the system scope and the
        caller's organization scope by default; use the `owners` parameter to restrict to
        one or both.
        Use `keys` to fetch a specific set of tags by their stable key identifiers, or
        `lookup_key` to find a single tag by its lookup key. Use `path_prefix` to filter
        tags by virtual path hierarchy. Combine filters as needed; all supplied filters are
        applied together.
        Pagination is page-based. Supply `page` and `page_size` to control which page is
        returned. The response includes `total_entries`, `total_pages`, `has_next`, and
        `has_prev` fields for navigating the full result set.

        Args:
            page: Page number to return. Defaults to 1.
            page_size: Number of solution tags to return per page. Defaults to 25.
            keys: Return only solution tags whose `key` exactly matches one of the provided values.
            lookup_key: Return only the solution tag whose `lookup_key` exactly matches this value.
            path_prefix: Return only solution tags whose `virtual_path` starts with this prefix.
            owners: Restrict results to one or more owner scopes. Accepted values are `"system"` (app-level system tags) and `"org"` (tags belonging to the caller's organization). Omit to include all scopes visible to the caller.

        Returns:
            Paginated list of solution tag definitions visible to the authenticated user.
        """
        query: dict[str, object] = {}
        if page is not None:
            query["page"] = page
        if page_size is not None:
            query["page_size"] = page_size
        if keys is not None:
            query["keys"] = keys
        if lookup_key is not None:
            query["lookup_key"] = lookup_key
        if path_prefix is not None:
            query["path_prefix"] = path_prefix
        if owners is not None:
            query["owners"] = owners
        return await self._http.request(
            "/api/v1/solution_tags",
            query=query,
            response_type=SolutionTagListResponse,
        )


class SolutionTagResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(
        self,
        *,
        page: int | None = None,
        page_size: int | None = None,
        keys: builtins.list[str] | None = None,
        lookup_key: str | None = None,
        path_prefix: str | None = None,
        owners: builtins.list[str] | None = None,
    ) -> SolutionTagListResponse:
        """
        List solution tags
        Returns a paginated list of solution tag definitions visible to the authenticated
        user, scoped to their app. Results include tags from both the system scope and the
        caller's organization scope by default; use the `owners` parameter to restrict to
        one or both.
        Use `keys` to fetch a specific set of tags by their stable key identifiers, or
        `lookup_key` to find a single tag by its lookup key. Use `path_prefix` to filter
        tags by virtual path hierarchy. Combine filters as needed; all supplied filters are
        applied together.
        Pagination is page-based. Supply `page` and `page_size` to control which page is
        returned. The response includes `total_entries`, `total_pages`, `has_next`, and
        `has_prev` fields for navigating the full result set.

        Args:
            page: Page number to return. Defaults to 1.
            page_size: Number of solution tags to return per page. Defaults to 25.
            keys: Return only solution tags whose `key` exactly matches one of the provided values.
            lookup_key: Return only the solution tag whose `lookup_key` exactly matches this value.
            path_prefix: Return only solution tags whose `virtual_path` starts with this prefix.
            owners: Restrict results to one or more owner scopes. Accepted values are `"system"` (app-level system tags) and `"org"` (tags belonging to the caller's organization). Omit to include all scopes visible to the caller.

        Returns:
            Paginated list of solution tag definitions visible to the authenticated user.
        """
        query: dict[str, object] = {}
        if page is not None:
            query["page"] = page
        if page_size is not None:
            query["page_size"] = page_size
        if keys is not None:
            query["keys"] = keys
        if lookup_key is not None:
            query["lookup_key"] = lookup_key
        if path_prefix is not None:
            query["path_prefix"] = path_prefix
        if owners is not None:
            query["owners"] = owners
        return self._http.request(
            "/api/v1/solution_tags",
            query=query,
            response_type=SolutionTagListResponse,
        )
