# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: db5e3465b54b

from __future__ import annotations

from datetime import datetime
from typing import Literal

from pydantic import BaseModel, Field

from ...runtime.http_client import HttpClient, SyncHttpClient


class OrgListResponseDataItem(BaseModel):
    domain: str | None = Field(
        default=None,
        description="Company domain. For personal orgs, the owner email is returned only to a viewer in that org.",
    )
    id: str = Field(..., description="Organization ID (`org_...`).")
    kind: Literal["company", "personal"] = Field(
        ..., description="Whether this is a company-domain org or a person-owned personal org."
    )
    name: str = Field(..., description="Display name of the organization.")


class OrgListResponse(BaseModel):
    """
    Successful response
    """

    data: list[OrgListResponseDataItem] = Field(
        ..., description="Array of organization objects for the current page."
    )
    has_next: bool | None = Field(
        default=None, description="`true` when a subsequent page exists; `false` on the last page."
    )
    has_prev: bool | None = Field(
        default=None, description="`true` when a previous page exists; `false` on the first page."
    )
    page: int | None = Field(default=None, description="The current page number returned.")
    page_size: int | None = Field(
        default=None, description="The number of results per page used for this response."
    )
    total_entries: int | None = Field(
        default=None,
        description="Total number of organizations matching the query across all pages.",
    )
    total_pages: int | None = Field(
        default=None, description="Total number of pages for the current query and page size."
    )


class OrgArtifactsResponseDataItemImageSource(BaseModel):
    file: str | None = Field(
        default=None,
        description="ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file.",
    )
    height: int | None = Field(
        default=None, description="Height of the image in pixels. `null` if not known."
    )
    media: str | None = Field(
        default=None,
        description="ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity.",
    )
    mime_type: str | None = Field(
        default=None,
        description='MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.',
    )
    refresh_url: str | None = Field(
        default=None,
        description="Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing.",
    )
    url: str | None = Field(
        default=None,
        description="Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires.",
    )
    width: int | None = Field(
        default=None, description="Width of the image in pixels. `null` if not known."
    )


class OrgArtifactsResponseDataItem(BaseModel):
    agent: str | None = Field(
        default=None,
        description="ID of the agent that produced this artifact (`agt_...`). `null` if not agent-produced.",
    )
    content_type: str | None = Field(
        default=None,
        description='MIME type of the current version\'s file, e.g. `"text/csv"` or `"image/png"`. `null` if no file is attached.',
    )
    created_at: datetime | None = Field(
        default=None, description="When the artifact was first created (ISO 8601)."
    )
    current_version: str | None = Field(
        default=None,
        description="ID of the current (latest published) artifact version (`artv_...`). `null` if no version has been published.",
    )
    description: str | None = Field(
        default=None,
        description="Optional longer description of the artifact's contents or purpose. `null` if not set.",
    )
    file: str | None = Field(
        default=None,
        description="Storage file ID for the current version (`fil_...`). `null` if no file is attached.",
    )
    file_name: str | None = Field(
        default=None,
        description='Original filename of the current version\'s file, e.g. `"output.csv"`. `null` if no file is attached.',
    )
    file_url: str | None = Field(
        default=None,
        description="Short-lived signed URL for downloading the current version's file. `null` if no file is attached.",
    )
    group_key: str | None = Field(
        default=None,
        description="Optional nonunique, case-sensitive grouping key, limited to 1024 UTF-8 bytes. Null when unset; belongs to the artifact, not a content version.",
    )
    id: str = Field(..., description="Artifact ID (`art_...`).")
    image_source: OrgArtifactsResponseDataItemImageSource | None = Field(
        default=None,
        description='Image source metadata for rendering the current version\'s file inline. Present only when `content_type` starts with `"image/"`. `null` otherwise.',
    )
    name: str | None = Field(
        default=None,
        description='Human-readable name for the artifact, e.g. `"Q2 Report"`. `null` if not set.',
    )
    org: str | None = Field(
        default=None, description="ID of the organization this artifact belongs to (`org_...`)."
    )
    sandbox: str | None = Field(
        default=None,
        description="Identifier of the sandbox environment associated with this artifact. `null` if not sandbox-scoped.",
    )
    system: bool | None = Field(
        default=None,
        description="True when the artifact has no user, team, or agent owner. An organization may own a system artifact.",
    )
    team: str | None = Field(
        default=None,
        description="ID of the team that owns this artifact (`tea_...`). `null` if not team-scoped.",
    )
    thread: str | None = Field(
        default=None,
        description="ID of the thread in which this artifact was created (`thr_...`). `null` if not thread-scoped.",
    )
    updated_at: datetime | None = Field(
        default=None, description="When the artifact record was last modified (ISO 8601)."
    )
    user: str | None = Field(
        default=None,
        description="ID of the user who created this artifact (`usr_...`). `null` if not user-scoped.",
    )
    version: int | None = Field(
        default=None,
        description="Current version number of the artifact. Increments each time a new version is published.",
    )


class OrgArtifactsResponse(BaseModel):
    """
    Successful response
    """

    after_cursor: str | None = None
    before_cursor: str | None = Field(
        default=None, description="Always null; pagination is forward-only."
    )
    data: list[OrgArtifactsResponseDataItem]
    has_more: bool


class AsyncOrgResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self, *, search: str | None = None, page: int | None = None, page_size: int | None = None
    ) -> OrgListResponse:
        """
        Search organizations
        Returns a paginated list of organizations within the authenticated app scope,
        optionally filtered by a free-text search term matched against name, slug, and
        domain (case-insensitive). Results are ordered by relevance when a search term
        is provided, and by creation time descending otherwise.
        The response includes only public-facing organization fields: ID, name, domain,
        and logo. Use the developer-scoped org endpoints to access full organization
        records.
        Pagination is offset-based. Pass `page` and `page_size` to navigate through
        results. The `has_next` and `has_prev` fields indicate whether adjacent pages
        exist.

        Args:
            search: Free-text search term matched against organization name, slug, and domain (case-insensitive). Omit to return all organizations in the app.
            page: Page number to retrieve, starting at `1`. Defaults to `1` when omitted.
            page_size: Number of organizations to return per page. Defaults to `25`; maximum is `100`.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if search is not None:
            query["search"] = search
        if page is not None:
            query["page"] = page
        if page_size is not None:
            query["page_size"] = page_size
        return await self._http.request("/api/v1/orgs", query=query, response_type=OrgListResponse)

    async def artifacts(
        self,
        org: str,
        *,
        limit: int | None = None,
        after_cursor: str | None = None,
        group_key: str | None = None,
        group_key_prefix: str | None = None,
    ) -> OrgArtifactsResponse:
        """
        List completed organization artifacts
        Returns completed system-owned organization snapshots, newest first. Private user, team, agent and thread artifacts are excluded.

        Args:
            org: Organization ID.
            limit: Maximum snapshots returned, from 1 to 100.
            after_cursor: Opaque cursor for the next page of older snapshots.
            group_key: Exact, case-sensitive grouping key.
            group_key_prefix: Nonempty literal group prefix; mutually exclusive with group_key.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        if group_key is not None:
            query["group_key"] = group_key
        if group_key_prefix is not None:
            query["group_key_prefix"] = group_key_prefix
        return await self._http.request(
            f"/api/v1/orgs/{org}/artifacts",
            query=query,
            response_type=OrgArtifactsResponse,
        )


class OrgResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(
        self, *, search: str | None = None, page: int | None = None, page_size: int | None = None
    ) -> OrgListResponse:
        """
        Search organizations
        Returns a paginated list of organizations within the authenticated app scope,
        optionally filtered by a free-text search term matched against name, slug, and
        domain (case-insensitive). Results are ordered by relevance when a search term
        is provided, and by creation time descending otherwise.
        The response includes only public-facing organization fields: ID, name, domain,
        and logo. Use the developer-scoped org endpoints to access full organization
        records.
        Pagination is offset-based. Pass `page` and `page_size` to navigate through
        results. The `has_next` and `has_prev` fields indicate whether adjacent pages
        exist.

        Args:
            search: Free-text search term matched against organization name, slug, and domain (case-insensitive). Omit to return all organizations in the app.
            page: Page number to retrieve, starting at `1`. Defaults to `1` when omitted.
            page_size: Number of organizations to return per page. Defaults to `25`; maximum is `100`.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if search is not None:
            query["search"] = search
        if page is not None:
            query["page"] = page
        if page_size is not None:
            query["page_size"] = page_size
        return self._http.request("/api/v1/orgs", query=query, response_type=OrgListResponse)

    def artifacts(
        self,
        org: str,
        *,
        limit: int | None = None,
        after_cursor: str | None = None,
        group_key: str | None = None,
        group_key_prefix: str | None = None,
    ) -> OrgArtifactsResponse:
        """
        List completed organization artifacts
        Returns completed system-owned organization snapshots, newest first. Private user, team, agent and thread artifacts are excluded.

        Args:
            org: Organization ID.
            limit: Maximum snapshots returned, from 1 to 100.
            after_cursor: Opaque cursor for the next page of older snapshots.
            group_key: Exact, case-sensitive grouping key.
            group_key_prefix: Nonempty literal group prefix; mutually exclusive with group_key.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        if group_key is not None:
            query["group_key"] = group_key
        if group_key_prefix is not None:
            query["group_key_prefix"] = group_key_prefix
        return self._http.request(
            f"/api/v1/orgs/{org}/artifacts",
            query=query,
            response_type=OrgArtifactsResponse,
        )
