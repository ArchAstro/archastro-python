# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: ee08edaaa3bf

from __future__ import annotations

from collections.abc import AsyncIterator, Iterator
from typing import Literal, TypedDict

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.automations import AutomationRun
from ...types.common import RunJournalPage


class AutomationRunStreamEventRunUpdate(TypedDict):
    event: Literal["run_update"]
    data: AutomationRun


AutomationRunStreamEvent = AutomationRunStreamEventRunUpdate


class AsyncAutomationRunResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def get(self, automation_run: str) -> AutomationRun:
        """
        Retrieve an automation run
        Returns a single automation run by ID.
        Public v1 path: `GET /api/v1/automation_runs/:automation_run`.
        Developer app-scoped path (legacy shape, same module):
        `GET /protected/api/v1/developer/apps/:app/automations/runs/:automation_run`.
        Any automation type is accepted (`trigger`, `scheduled`, or `invoked`).
        Resolution is viewer-scoped (AppScope + OrgScope): a run the caller cannot
        see returns 404.

        Args:
            automation_run: Automation run ID (`arun_...`).

        Returns:
            The requested automation run.
        """
        return await self._http.request(
            f"/api/v1/automation_runs/{automation_run}",
            response_type=AutomationRun,
        )

    async def journal(
        self, automation_run: str, *, limit: int | None = None, after_cursor: str | None = None
    ) -> RunJournalPage:
        """
        List an automation run journal
        Returns durable workflow journal entries for an automation run.
        Public v1 path: `GET /api/v1/automation_runs/:automation_run/journal`.
        Developer app-scoped path (legacy shape, same module):
        `GET /protected/api/v1/developer/apps/:app/automations/runs/:automation_run/journal`.
        Any automation type is accepted. Resolution is viewer-scoped
        (AppScope + OrgScope). An authorized run without a journal returns
        `journal: null` and an empty `data` array with HTTP 200.

        Args:
            automation_run: Automation run ID (`atr_...`) whose journal to retrieve.
            limit: Maximum number of entries to return. Defaults to 50; maximum is 100.
            after_cursor: Opaque cursor from the previous response's `after_cursor` field.

        Returns:
            Forward-paginated automation run journal entries.
        """
        query: dict[str, object] = {}
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return await self._http.request(
            f"/api/v1/automation_runs/{automation_run}/journal",
            query=query,
            response_type=RunJournalPage,
        )

    async def stream(self, automation_run: str) -> AsyncIterator[AutomationRunStreamEvent]:
        """
        Stream automation run status
        Opens a Server-Sent Events connection that emits a `run_update` event whenever
        the invoked automation run's status changes, replaying the current status on
        connect and closing on a terminal status (`completed`, `failed`, `cancelled`)
        or after the max stream duration.

        Args:
            automation_run: ID of the invoked automation run.

        Returns:
            Server-Sent Events stream
        """
        async for event in self._http.stream_sse(
            f"/api/v1/automation_runs/{automation_run}/stream"
        ):
            yield event


class AutomationRunResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def get(self, automation_run: str) -> AutomationRun:
        """
        Retrieve an automation run
        Returns a single automation run by ID.
        Public v1 path: `GET /api/v1/automation_runs/:automation_run`.
        Developer app-scoped path (legacy shape, same module):
        `GET /protected/api/v1/developer/apps/:app/automations/runs/:automation_run`.
        Any automation type is accepted (`trigger`, `scheduled`, or `invoked`).
        Resolution is viewer-scoped (AppScope + OrgScope): a run the caller cannot
        see returns 404.

        Args:
            automation_run: Automation run ID (`arun_...`).

        Returns:
            The requested automation run.
        """
        return self._http.request(
            f"/api/v1/automation_runs/{automation_run}",
            response_type=AutomationRun,
        )

    def journal(
        self, automation_run: str, *, limit: int | None = None, after_cursor: str | None = None
    ) -> RunJournalPage:
        """
        List an automation run journal
        Returns durable workflow journal entries for an automation run.
        Public v1 path: `GET /api/v1/automation_runs/:automation_run/journal`.
        Developer app-scoped path (legacy shape, same module):
        `GET /protected/api/v1/developer/apps/:app/automations/runs/:automation_run/journal`.
        Any automation type is accepted. Resolution is viewer-scoped
        (AppScope + OrgScope). An authorized run without a journal returns
        `journal: null` and an empty `data` array with HTTP 200.

        Args:
            automation_run: Automation run ID (`atr_...`) whose journal to retrieve.
            limit: Maximum number of entries to return. Defaults to 50; maximum is 100.
            after_cursor: Opaque cursor from the previous response's `after_cursor` field.

        Returns:
            Forward-paginated automation run journal entries.
        """
        query: dict[str, object] = {}
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return self._http.request(
            f"/api/v1/automation_runs/{automation_run}/journal",
            query=query,
            response_type=RunJournalPage,
        )

    def stream(self, automation_run: str) -> Iterator[AutomationRunStreamEvent]:
        """
        Stream automation run status
        Opens a Server-Sent Events connection that emits a `run_update` event whenever
        the invoked automation run's status changes, replaying the current status on
        connect and closing on a terminal status (`completed`, `failed`, `cancelled`)
        or after the max stream duration.

        Args:
            automation_run: ID of the invoked automation run.

        Returns:
            Server-Sent Events stream
        """
        yield from self._http.stream_sse_sync(f"/api/v1/automation_runs/{automation_run}/stream")
