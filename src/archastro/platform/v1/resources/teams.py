# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: b37aff69b1bd

from __future__ import annotations

from ...runtime.http_client import HttpClient
from ...types.artifacts import Artifact
from ...types.common import CustomObject
from ...types.teams import Team, TeamInvite, TeamMembership
from ...types.threads import Thread


class TeamArtifactResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(self, team: str) -> dict[str, object]:
        return await self._http.request(f"/api/v1/teams/{team}/artifacts")

    async def create(self, team: str, input: dict) -> Artifact:
        return await self._http.request(
            f"/api/v1/teams/{team}/artifacts",
            method="POST",
            body=input,
        )


class TeamCustomObjectResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(self, team: str, **params) -> dict[str, object]:
        return await self._http.request(f"/api/v1/teams/{team}/custom_objects", query=params)

    async def create(self, team: str, input: dict) -> CustomObject:
        return await self._http.request(
            f"/api/v1/teams/{team}/custom_objects",
            method="POST",
            body=input,
        )


class MemberResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(self, team: str) -> dict[str, object]:
        return await self._http.request(f"/api/v1/teams/{team}/members")

    async def create(self, team: str, input: dict) -> TeamMembership:
        return await self._http.request(f"/api/v1/teams/{team}/members", method="POST", body=input)


class TeamThreadResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(self, team: str) -> dict[str, object]:
        return await self._http.request(f"/api/v1/teams/{team}/threads")

    async def create(self, team: str, input: dict) -> Thread:
        return await self._http.request(f"/api/v1/teams/{team}/threads", method="POST", body=input)


class TeamResource:
    def __init__(self, http: HttpClient):
        self._http = http
        self.artifacts = TeamArtifactResource(http)
        self.custom_objects = TeamCustomObjectResource(http)
        self.members = MemberResource(http)
        self.threads = TeamThreadResource(http)

    async def list(self, **params) -> dict[str, object]:
        return await self._http.request("/api/v1/teams", query=params)

    async def create(self, input: dict) -> Team:
        return await self._http.request("/api/v1/teams", method="POST", body=input)

    async def join_by_code(self, input: dict) -> Team:
        """
        Join a team using an invite code
        Accepts either `join_code` or `invite_code`.
        For user-authenticated requests, the current user joins the team identified by
        the invite code. For server-to-server requests, provide `agent` or `user` to
        add that principal to the team instead.
        """
        return await self._http.request("/api/v1/teams/join_by_code", method="POST", body=input)

    async def delete(self, team: str) -> None:
        await self._http.request(f"/api/v1/teams/{team}", method="DELETE")

    async def get(self, team: str) -> Team:
        return await self._http.request(f"/api/v1/teams/{team}")

    async def update(self, team: str, input: dict) -> Team:
        return await self._http.request(f"/api/v1/teams/{team}", method="PATCH", body=input)

    async def invite(self, team: str, input: dict) -> TeamInvite:
        return await self._http.request(f"/api/v1/teams/{team}/invite", method="POST", body=input)

    async def invites(self, team: str, input: dict) -> dict[str, object]:
        return await self._http.request(f"/api/v1/teams/{team}/invites", method="POST", body=input)

    async def join(self, team: str, input: dict) -> None:
        """
        Join a team the current user can see
        Joins a specific visible team by team ID.
        For standard user requests, the current user is added to the team. When
        `agent` is provided, the current user must already be a team member and the
        agent is added instead.
        """
        await self._http.request(f"/api/v1/teams/{team}/join", method="POST", body=input)

    async def leave(self, team: str) -> None:
        await self._http.request(f"/api/v1/teams/{team}/leave", method="DELETE")
