# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: fe35f9a93fac

from __future__ import annotations

import builtins
from typing import Any, Required, TypedDict

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.common import AgentSkill, AgentSkillList


class AgentSkillCreateInput(TypedDict, total=False):
    "Enable a skill on an agent"

    agent: Required[str]
    "Agent ID (`agt_...`) to attach the skill to."
    config: Required[str]
    "Skill config ID (`cfg_...`) that defines the skill's behavior. Must belong to the authenticated app."
    instruction: str | None
    "Optional plain-text instruction override. When supplied, replaces the default instruction from the skill config for this agent."
    metadata: dict[str, Any] | None
    "Arbitrary key-value metadata to store with the agent skill. Useful for tracking provisioning context or custom labels."


class AgentSkillUpdateInput(TypedDict, total=False):
    "Update an agent skill"

    instruction: str | None
    "Plain-text instruction override for this agent skill. Replaces the default instruction from the skill config. Omit to leave the current value unchanged."
    metadata: dict[str, Any] | None
    "Arbitrary key-value metadata to store with the agent skill. Omit to leave the current value unchanged."
    template: str | None
    "Agent skill template config ID (`cfg_...`), virtual path, or lookup key. When supplied, re-resolves the template and updates the skill's underlying config in place, refreshing template provenance while preserving the skill's current status. Any `instruction` or `metadata` values provided alongside `template` override the template defaults."


class AsyncAgentSkillResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(self, *, agent: builtins.list[str] | None = None) -> AgentSkillList:
        """
        List agent skills
        Returns all agent skills belonging to the authenticated app. Results include
        skills in any status (`"active"` or `"inactive"`). Use the `agent` filter to
        narrow results to one or more specific agents.
        Requires an app-scoped API key. Supplying one or more `agent` values that
        cannot be resolved within the app returns 404.

        Args:
            agent: Filter results to skills belonging to the specified agent(s). Accepts one or more agent IDs (`agt_...`) or lookup keys. Omit to return skills across all agents in the app.

        Returns:
            List of agent skills matching the query.
        """
        query: dict[str, object] = {}
        if agent is not None:
            query["agent"] = agent
        return await self._http.request(
            "/api/v1/agent_skills",
            query=query,
            response_type=AgentSkillList,
        )

    async def create(self, input: AgentSkillCreateInput) -> AgentSkill:
        """
        Enable a skill on an agent
        Attaches a skill config to an agent, creating an agent skill record and
        returning it with an initial status of `"inactive"`. Use the activate
        endpoint to make the skill available during agent runs.
        Requires an app-scoped API key. The `agent` and `config` must both belong
        to the authenticated app. Supplying a `config` that does not exist within
        the app returns 422. Returns 201 on success.

        Args:
            input: Request body.
            input.agent: Agent ID (`agt_...`) to attach the skill to.
            input.config: Skill config ID (`cfg_...`) that defines the skill's behavior. Must belong to the authenticated app.
            input.instruction: Optional plain-text instruction override. When supplied, replaces the default instruction from the skill config for this agent.
            input.metadata: Arbitrary key-value metadata to store with the agent skill. Useful for tracking provisioning context or custom labels.

        Returns:
            The newly created agent skill record.
        """
        return await self._http.request(
            "/api/v1/agent_skills",
            method="POST",
            body=input,
            response_type=AgentSkill,
        )

    async def delete(self, agent_skill: str) -> None:
        """
        Remove a skill from an agent
        Permanently removes the specified agent skill, detaching the skill config
        from the agent. This action cannot be undone. To temporarily stop a skill
        from being invoked without removing it, use the deactivate endpoint instead.
        Requires an app-scoped API key. Returns 204 No Content on success.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to remove.

        Returns:
            No content
        """
        await self._http.request(f"/api/v1/agent_skills/{agent_skill}", method="DELETE")

    async def get(self, agent_skill: str) -> AgentSkill:
        """
        Retrieve an agent skill
        Returns the agent skill identified by its ID. The skill must belong to
        the authenticated app's scope.
        Requires an app-scoped API key.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to retrieve.

        Returns:
            The requested agent skill.
        """
        return await self._http.request(
            f"/api/v1/agent_skills/{agent_skill}",
            response_type=AgentSkill,
        )

    async def update(self, agent_skill: str, input: AgentSkillUpdateInput) -> AgentSkill:
        """
        Update an agent skill
        Updates one or more mutable fields on the specified agent skill. All
        parameters are optional; supply only the fields you want to change.
        When `template` is provided, the platform re-resolves that skill template
        and re-points the skill's underlying config in place. The skill's current
        status is preserved. Any `instruction` or `metadata` supplied alongside
        `template` override the template's defaults.
        Requires an app-scoped API key. The skill must belong to the authenticated
        app's scope.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to update.
            input: Request body.
            input.instruction: Plain-text instruction override for this agent skill. Replaces the default instruction from the skill config. Omit to leave the current value unchanged.
            input.metadata: Arbitrary key-value metadata to store with the agent skill. Omit to leave the current value unchanged.
            input.template: Agent skill template config ID (`cfg_...`), virtual path, or lookup key. When supplied, re-resolves the template and updates the skill's underlying config in place, refreshing template provenance while preserving the skill's current status. Any `instruction` or `metadata` values provided alongside `template` override the template defaults.

        Returns:
            The agent skill after the update has been applied.
        """
        return await self._http.request(
            f"/api/v1/agent_skills/{agent_skill}",
            method="PATCH",
            body=input,
            response_type=AgentSkill,
        )

    async def activate(self, agent_skill: str) -> AgentSkill:
        """
        Activate an agent skill
        Sets the status of the specified agent skill to `"active"`, allowing the
        skill to be invoked during agent runs. The skill must already exist on the
        agent (created via the enable endpoint) and belong to the authenticated
        app's scope.
        Requires an app-scoped API key. Activating a skill that is already active
        is a no-op and returns the skill unchanged.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to activate.

        Returns:
            The agent skill after activation, with `status` set to `"active"`.
        """
        return await self._http.request(
            f"/api/v1/agent_skills/{agent_skill}/activate",
            method="POST",
            response_type=AgentSkill,
        )

    async def deactivate(self, agent_skill: str) -> AgentSkill:
        """
        Deactivate an agent skill
        Sets the status of the specified agent skill to `"inactive"`, preventing
        the skill from being invoked during future agent runs. The skill record
        is preserved and can be reactivated at any time.
        Requires an app-scoped API key. Deactivating a skill that is already
        inactive is a no-op and returns the skill unchanged.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to deactivate.

        Returns:
            The agent skill after deactivation, with `status` set to `"inactive"`.
        """
        return await self._http.request(
            f"/api/v1/agent_skills/{agent_skill}/deactivate",
            method="POST",
            response_type=AgentSkill,
        )


class AgentSkillResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(self, *, agent: builtins.list[str] | None = None) -> AgentSkillList:
        """
        List agent skills
        Returns all agent skills belonging to the authenticated app. Results include
        skills in any status (`"active"` or `"inactive"`). Use the `agent` filter to
        narrow results to one or more specific agents.
        Requires an app-scoped API key. Supplying one or more `agent` values that
        cannot be resolved within the app returns 404.

        Args:
            agent: Filter results to skills belonging to the specified agent(s). Accepts one or more agent IDs (`agt_...`) or lookup keys. Omit to return skills across all agents in the app.

        Returns:
            List of agent skills matching the query.
        """
        query: dict[str, object] = {}
        if agent is not None:
            query["agent"] = agent
        return self._http.request(
            "/api/v1/agent_skills",
            query=query,
            response_type=AgentSkillList,
        )

    def create(self, input: AgentSkillCreateInput) -> AgentSkill:
        """
        Enable a skill on an agent
        Attaches a skill config to an agent, creating an agent skill record and
        returning it with an initial status of `"inactive"`. Use the activate
        endpoint to make the skill available during agent runs.
        Requires an app-scoped API key. The `agent` and `config` must both belong
        to the authenticated app. Supplying a `config` that does not exist within
        the app returns 422. Returns 201 on success.

        Args:
            input: Request body.
            input.agent: Agent ID (`agt_...`) to attach the skill to.
            input.config: Skill config ID (`cfg_...`) that defines the skill's behavior. Must belong to the authenticated app.
            input.instruction: Optional plain-text instruction override. When supplied, replaces the default instruction from the skill config for this agent.
            input.metadata: Arbitrary key-value metadata to store with the agent skill. Useful for tracking provisioning context or custom labels.

        Returns:
            The newly created agent skill record.
        """
        return self._http.request(
            "/api/v1/agent_skills",
            method="POST",
            body=input,
            response_type=AgentSkill,
        )

    def delete(self, agent_skill: str) -> None:
        """
        Remove a skill from an agent
        Permanently removes the specified agent skill, detaching the skill config
        from the agent. This action cannot be undone. To temporarily stop a skill
        from being invoked without removing it, use the deactivate endpoint instead.
        Requires an app-scoped API key. Returns 204 No Content on success.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to remove.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/agent_skills/{agent_skill}", method="DELETE")

    def get(self, agent_skill: str) -> AgentSkill:
        """
        Retrieve an agent skill
        Returns the agent skill identified by its ID. The skill must belong to
        the authenticated app's scope.
        Requires an app-scoped API key.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to retrieve.

        Returns:
            The requested agent skill.
        """
        return self._http.request(f"/api/v1/agent_skills/{agent_skill}", response_type=AgentSkill)

    def update(self, agent_skill: str, input: AgentSkillUpdateInput) -> AgentSkill:
        """
        Update an agent skill
        Updates one or more mutable fields on the specified agent skill. All
        parameters are optional; supply only the fields you want to change.
        When `template` is provided, the platform re-resolves that skill template
        and re-points the skill's underlying config in place. The skill's current
        status is preserved. Any `instruction` or `metadata` supplied alongside
        `template` override the template's defaults.
        Requires an app-scoped API key. The skill must belong to the authenticated
        app's scope.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to update.
            input: Request body.
            input.instruction: Plain-text instruction override for this agent skill. Replaces the default instruction from the skill config. Omit to leave the current value unchanged.
            input.metadata: Arbitrary key-value metadata to store with the agent skill. Omit to leave the current value unchanged.
            input.template: Agent skill template config ID (`cfg_...`), virtual path, or lookup key. When supplied, re-resolves the template and updates the skill's underlying config in place, refreshing template provenance while preserving the skill's current status. Any `instruction` or `metadata` values provided alongside `template` override the template defaults.

        Returns:
            The agent skill after the update has been applied.
        """
        return self._http.request(
            f"/api/v1/agent_skills/{agent_skill}",
            method="PATCH",
            body=input,
            response_type=AgentSkill,
        )

    def activate(self, agent_skill: str) -> AgentSkill:
        """
        Activate an agent skill
        Sets the status of the specified agent skill to `"active"`, allowing the
        skill to be invoked during agent runs. The skill must already exist on the
        agent (created via the enable endpoint) and belong to the authenticated
        app's scope.
        Requires an app-scoped API key. Activating a skill that is already active
        is a no-op and returns the skill unchanged.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to activate.

        Returns:
            The agent skill after activation, with `status` set to `"active"`.
        """
        return self._http.request(
            f"/api/v1/agent_skills/{agent_skill}/activate",
            method="POST",
            response_type=AgentSkill,
        )

    def deactivate(self, agent_skill: str) -> AgentSkill:
        """
        Deactivate an agent skill
        Sets the status of the specified agent skill to `"inactive"`, preventing
        the skill from being invoked during future agent runs. The skill record
        is preserved and can be reactivated at any time.
        Requires an app-scoped API key. Deactivating a skill that is already
        inactive is a no-op and returns the skill unchanged.

        Args:
            agent_skill: Agent skill ID (`ask_...`) to deactivate.

        Returns:
            The agent skill after deactivation, with `status` set to `"inactive"`.
        """
        return self._http.request(
            f"/api/v1/agent_skills/{agent_skill}/deactivate",
            method="POST",
            response_type=AgentSkill,
        )
