# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: e30b5fbd5ce5

from __future__ import annotations

from datetime import datetime
from typing import Any, Required, TypedDict

from pydantic import BaseModel, Field

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.notifications import Notification


class NotificationSendInput(TypedDict, total=False):
    "Send a custom notification to a user"

    data: dict[str, Any] | None
    "Arbitrary key-value payload merged with platform-provided context (recipient, app, org, brand) when rendering the notification's display fields. Defaults to an empty object when omitted."
    idempotency_key: str | None
    "Optional deduplication key. A second call with the same `idempotency_key` for the same recipient returns the originally-created notification without inserting a new record. Scoped per calling app."
    type: Required[str]
    'Custom notification type identifier in the form `"custom:<lookup_key>"`, where `<lookup_key>` matches a `NotificationType` config declared in the calling app\'s bundle.'
    user: Required[str]
    "Recipient user ID (`usr_...`). Must be a member of the calling app's tenant."


class NotificationListResponseDataItem(BaseModel):
    archived_at: datetime | None = Field(
        default=None,
        description="When the recipient archived this notification. `null` if the notification has not been archived.",
    )
    created_at: datetime = Field(..., description="When the notification was sent (ISO 8601).")
    id: str = Field(..., description="Notification ID (`ntf_...`).")
    read_at: datetime | None = Field(
        default=None,
        description="When the recipient marked this notification read. `null` if the notification has not been read.",
    )
    rendered: dict[str, Any] = Field(
        ...,
        description='Type-specific render spec resolved at request time. All types include `title`, `kind`, and `actions`; custom types may add their own keys. Notifications whose type is no longer registered render with `kind: "unknown"`.',
    )
    status: str = Field(
        ...,
        description='Current read state of the notification. One of `"unread"`, `"read"`, or `"archived"`.',
    )
    type: str = Field(
        ...,
        description='Notification type slug, e.g. `"app_info"` for a built-in type or `"custom:deploy_complete"` for a custom type.',
    )


class NotificationListResponse(BaseModel):
    """
    Successful response
    """

    after_cursor: str | None = Field(
        default=None,
        description="Opaque cursor to pass as `after_cursor` on the next request to fetch older notifications. `null` when this is the last page.",
    )
    before_cursor: str | None = Field(
        default=None,
        description="Always `null` inbox pagination is forward-only and does not support fetching newer pages via cursor.",
    )
    data: list[NotificationListResponseDataItem] = Field(
        ..., description="Array of notification objects for the current page, ordered newest first."
    )
    has_more: bool = Field(
        ...,
        description="`true` if additional (older) notifications exist beyond this page; `false` if this is the last page.",
    )


class NotificationUnreadCountResponse(BaseModel):
    """
    Successful response
    """

    count: int = Field(
        ...,
        description='Total number of notifications with `"unread"` status belonging to the authenticated user.',
    )


class AsyncNotificationResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self,
        *,
        status: str | None = None,
        limit: int | None = None,
        after_cursor: str | None = None,
    ) -> NotificationListResponse:
        """
        List a user's notifications
        Returns a cursor-paginated list of inbox notifications for the authenticated
        user, ordered by creation time descending (newest first). All status groups
        are included by default; pass `status` to narrow results to a specific group.
        Each notification's `rendered` field contains type-specific display data
        resolved at request time. Notifications whose type is no longer registered
        in the platform are rendered with `kind: "unknown"` rather than being omitted.
        Pagination is forward-only: supply `after_cursor` from a previous response to
        fetch the next (older) page. The `before_cursor` field is always `null` for
        this endpoint. Requires an app-scoped token.

        Args:
            status: Filter by notification status. One of `"all"`, `"active"`, `"unread"`, `"read"`, or `"archived"`. Defaults to `"all"` when omitted.
            limit: Maximum number of notifications to return per page. Defaults to 20; maximum is 100.
            after_cursor: Opaque pagination cursor from a previous response's `after_cursor` field. Omit to fetch the most recent notifications.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if status is not None:
            query["status"] = status
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return await self._http.request(
            "/api/v1/notifications",
            query=query,
            response_type=NotificationListResponse,
        )

    async def read_all(self) -> None:
        """
        Mark all notifications as read
        Marks every `"unread"` notification belonging to the authenticated user as
        `"read"` in a single operation. Notifications that are already `"read"` or
        `"archived"` are not affected.
        This call is safe to retry if there are no unread notifications, it
        succeeds without error. Requires an app-scoped token. Returns 204 No Content
        on success.

        Returns:
            No content
        """
        await self._http.request("/api/v1/notifications/read_all", method="POST")

    async def send(self, input: NotificationSendInput) -> Notification:
        """
        Send a custom notification to a user
        Delivers a custom-typed notification to one of the calling app's users.
        Apps define notification types by declaring `NotificationType` config objects
        in their bundle (one per `lookup_key`). Supply the type as
        `"custom:<lookup_key>"` and provide a `data` map that is merged with
        platform-provided context to render the notification's display fields.
        Only app-scoped tokens may call this endpoint user tokens are rejected with
        403. The app scope is stamped onto the notification automatically; an app
        cannot target recipients outside its tenant. Built-in platform types such as
        `"app_info"` and `"billing_alert"` are not accepted here.
        Pass `idempotency_key` to deduplicate sends. If you call this endpoint twice
        with the same `idempotency_key` for the same recipient, the second call
        returns the original notification without creating a duplicate. The key is
        scoped to the calling app, so the same raw key used by different apps cannot
        collide.

        Args:
            input: Request body.
            input.data: Arbitrary key-value payload merged with platform-provided context (recipient, app, org, brand) when rendering the notification's display fields. Defaults to an empty object when omitted.
            input.idempotency_key: Optional deduplication key. A second call with the same `idempotency_key` for the same recipient returns the originally-created notification without inserting a new record. Scoped per calling app.
            input.type: Custom notification type identifier in the form `"custom:<lookup_key>"`, where `<lookup_key>` matches a `NotificationType` config declared in the calling app's bundle.
            input.user: Recipient user ID (`usr_...`). Must be a member of the calling app's tenant.

        Returns:
            The created notification, or the existing notification when deduplicated by `idempotency_key`.
        """
        return await self._http.request(
            "/api/v1/notifications/send",
            method="POST",
            body=input,
            response_type=Notification,
        )

    async def unread_count(self) -> NotificationUnreadCountResponse:
        """
        Get the unread notification count
        Returns the total number of `"unread"` notifications for the authenticated
        user. Useful for displaying a badge or indicator in your UI without
        fetching the full notification list.
        Notifications with `"read"` or `"archived"` status are not included in the
        count. Requires an app-scoped token.

        Returns:
            Successful response
        """
        return await self._http.request(
            "/api/v1/notifications/unread_count",
            response_type=NotificationUnreadCountResponse,
        )

    async def archive(self, notification: str) -> None:
        """
        Archive a notification
        Moves a notification to `"archived"` status regardless of whether it is
        currently `"unread"` or `"read"`. Archived notifications are excluded from
        the default inbox view but remain retrievable by passing `status: "archived"`
        to the list endpoint.
        The authenticated user must own the notification. Passing a notification ID
        that belongs to a different user returns a 404. If the notification is
        already archived this call succeeds without error (idempotent).
        Requires an app-scoped token. Returns 204 No Content on success.

        Args:
            notification: Notification ID (`ntf_...`) to archive. Must belong to the authenticated user.

        Returns:
            No content
        """
        await self._http.request(f"/api/v1/notifications/{notification}/archive", method="POST")

    async def read(self, notification: str) -> None:
        """
        Mark a notification as read
        Transitions a notification from `"unread"` to `"read"` status. If the
        notification is already `"read"` or `"archived"`, the call succeeds without
        changing its status (idempotent).
        The authenticated user must own the notification. Passing a notification ID
        that belongs to a different user returns a 404. Requires an app-scoped token.
        Returns 204 No Content on success.

        Args:
            notification: Notification ID (`ntf_...`) to mark as read. Must belong to the authenticated user.

        Returns:
            No content
        """
        await self._http.request(f"/api/v1/notifications/{notification}/read", method="POST")

    async def unarchive(self, notification: str) -> None:
        """
        Unarchive a notification
        Restores an `"archived"` notification to its previous active status:
        `"read"` if the notification had been read before archiving, or `"unread"`
        otherwise. The notification will appear again in the default inbox view.
        The authenticated user must own the notification. Passing a notification ID
        that belongs to a different user returns a 404. If the notification is not
        currently archived this call succeeds without changing its status (idempotent).
        Requires an app-scoped token. Returns 204 No Content on success.

        Args:
            notification: Notification ID (`ntf_...`) to unarchive. Must belong to the authenticated user.

        Returns:
            No content
        """
        await self._http.request(f"/api/v1/notifications/{notification}/unarchive", method="POST")


class NotificationResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(
        self,
        *,
        status: str | None = None,
        limit: int | None = None,
        after_cursor: str | None = None,
    ) -> NotificationListResponse:
        """
        List a user's notifications
        Returns a cursor-paginated list of inbox notifications for the authenticated
        user, ordered by creation time descending (newest first). All status groups
        are included by default; pass `status` to narrow results to a specific group.
        Each notification's `rendered` field contains type-specific display data
        resolved at request time. Notifications whose type is no longer registered
        in the platform are rendered with `kind: "unknown"` rather than being omitted.
        Pagination is forward-only: supply `after_cursor` from a previous response to
        fetch the next (older) page. The `before_cursor` field is always `null` for
        this endpoint. Requires an app-scoped token.

        Args:
            status: Filter by notification status. One of `"all"`, `"active"`, `"unread"`, `"read"`, or `"archived"`. Defaults to `"all"` when omitted.
            limit: Maximum number of notifications to return per page. Defaults to 20; maximum is 100.
            after_cursor: Opaque pagination cursor from a previous response's `after_cursor` field. Omit to fetch the most recent notifications.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if status is not None:
            query["status"] = status
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return self._http.request(
            "/api/v1/notifications",
            query=query,
            response_type=NotificationListResponse,
        )

    def read_all(self) -> None:
        """
        Mark all notifications as read
        Marks every `"unread"` notification belonging to the authenticated user as
        `"read"` in a single operation. Notifications that are already `"read"` or
        `"archived"` are not affected.
        This call is safe to retry if there are no unread notifications, it
        succeeds without error. Requires an app-scoped token. Returns 204 No Content
        on success.

        Returns:
            No content
        """
        self._http.request("/api/v1/notifications/read_all", method="POST")

    def send(self, input: NotificationSendInput) -> Notification:
        """
        Send a custom notification to a user
        Delivers a custom-typed notification to one of the calling app's users.
        Apps define notification types by declaring `NotificationType` config objects
        in their bundle (one per `lookup_key`). Supply the type as
        `"custom:<lookup_key>"` and provide a `data` map that is merged with
        platform-provided context to render the notification's display fields.
        Only app-scoped tokens may call this endpoint user tokens are rejected with
        403. The app scope is stamped onto the notification automatically; an app
        cannot target recipients outside its tenant. Built-in platform types such as
        `"app_info"` and `"billing_alert"` are not accepted here.
        Pass `idempotency_key` to deduplicate sends. If you call this endpoint twice
        with the same `idempotency_key` for the same recipient, the second call
        returns the original notification without creating a duplicate. The key is
        scoped to the calling app, so the same raw key used by different apps cannot
        collide.

        Args:
            input: Request body.
            input.data: Arbitrary key-value payload merged with platform-provided context (recipient, app, org, brand) when rendering the notification's display fields. Defaults to an empty object when omitted.
            input.idempotency_key: Optional deduplication key. A second call with the same `idempotency_key` for the same recipient returns the originally-created notification without inserting a new record. Scoped per calling app.
            input.type: Custom notification type identifier in the form `"custom:<lookup_key>"`, where `<lookup_key>` matches a `NotificationType` config declared in the calling app's bundle.
            input.user: Recipient user ID (`usr_...`). Must be a member of the calling app's tenant.

        Returns:
            The created notification, or the existing notification when deduplicated by `idempotency_key`.
        """
        return self._http.request(
            "/api/v1/notifications/send",
            method="POST",
            body=input,
            response_type=Notification,
        )

    def unread_count(self) -> NotificationUnreadCountResponse:
        """
        Get the unread notification count
        Returns the total number of `"unread"` notifications for the authenticated
        user. Useful for displaying a badge or indicator in your UI without
        fetching the full notification list.
        Notifications with `"read"` or `"archived"` status are not included in the
        count. Requires an app-scoped token.

        Returns:
            Successful response
        """
        return self._http.request(
            "/api/v1/notifications/unread_count",
            response_type=NotificationUnreadCountResponse,
        )

    def archive(self, notification: str) -> None:
        """
        Archive a notification
        Moves a notification to `"archived"` status regardless of whether it is
        currently `"unread"` or `"read"`. Archived notifications are excluded from
        the default inbox view but remain retrievable by passing `status: "archived"`
        to the list endpoint.
        The authenticated user must own the notification. Passing a notification ID
        that belongs to a different user returns a 404. If the notification is
        already archived this call succeeds without error (idempotent).
        Requires an app-scoped token. Returns 204 No Content on success.

        Args:
            notification: Notification ID (`ntf_...`) to archive. Must belong to the authenticated user.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/notifications/{notification}/archive", method="POST")

    def read(self, notification: str) -> None:
        """
        Mark a notification as read
        Transitions a notification from `"unread"` to `"read"` status. If the
        notification is already `"read"` or `"archived"`, the call succeeds without
        changing its status (idempotent).
        The authenticated user must own the notification. Passing a notification ID
        that belongs to a different user returns a 404. Requires an app-scoped token.
        Returns 204 No Content on success.

        Args:
            notification: Notification ID (`ntf_...`) to mark as read. Must belong to the authenticated user.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/notifications/{notification}/read", method="POST")

    def unarchive(self, notification: str) -> None:
        """
        Unarchive a notification
        Restores an `"archived"` notification to its previous active status:
        `"read"` if the notification had been read before archiving, or `"unread"`
        otherwise. The notification will appear again in the default inbox view.
        The authenticated user must own the notification. Passing a notification ID
        that belongs to a different user returns a 404. If the notification is not
        currently archived this call succeeds without changing its status (idempotent).
        Requires an app-scoped token. Returns 204 No Content on success.

        Args:
            notification: Notification ID (`ntf_...`) to unarchive. Must belong to the authenticated user.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/notifications/{notification}/unarchive", method="POST")
