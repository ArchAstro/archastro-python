# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: ba40910ad7e2

from __future__ import annotations

from typing import TypedDict

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.users import UserInvite


class InviteAcceptInput(TypedDict):
    "Accept an invite"

    key: str
    "Opaque invite key identifying the invite to accept. Obtained from an invite link or a previous invite creation response."


class AsyncInviteResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def accept(self, input: InviteAcceptInput) -> UserInvite:
        """
        Accept an invite
        Accepts an invite on behalf of the authenticated user and adds them to the
        associated team or thread. The invite `key` is passed in the request body
        rather than the URL so it never appears in access logs, `Referer` headers,
        or error-reporter URL captures.
        This endpoint requires an authenticated end-user session. S2S secret-key
        tokens and unauthenticated requests are rejected with `401`. If the
        authenticated user is already a member of the invite's target, the request
        returns `409`. Both per-IP and per-user rate limits apply; exceeding either
        returns `429`.

        Args:
            input: Request body.
            input.key: Opaque invite key identifying the invite to accept. Obtained from an invite link or a previous invite creation response.

        Returns:
            The accepted invite, including its ID, key, metadata, optional thread scope, creator, and creation timestamp.
        """
        return await self._http.request(
            "/api/v1/invites/accept",
            method="POST",
            body=input,
            response_type=UserInvite,
        )


class InviteResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def accept(self, input: InviteAcceptInput) -> UserInvite:
        """
        Accept an invite
        Accepts an invite on behalf of the authenticated user and adds them to the
        associated team or thread. The invite `key` is passed in the request body
        rather than the URL so it never appears in access logs, `Referer` headers,
        or error-reporter URL captures.
        This endpoint requires an authenticated end-user session. S2S secret-key
        tokens and unauthenticated requests are rejected with `401`. If the
        authenticated user is already a member of the invite's target, the request
        returns `409`. Both per-IP and per-user rate limits apply; exceeding either
        returns `429`.

        Args:
            input: Request body.
            input.key: Opaque invite key identifying the invite to accept. Obtained from an invite link or a previous invite creation response.

        Returns:
            The accepted invite, including its ID, key, metadata, optional thread scope, creator, and creation timestamp.
        """
        return self._http.request(
            "/api/v1/invites/accept",
            method="POST",
            body=input,
            response_type=UserInvite,
        )
