# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 87b5e0da34ba

from __future__ import annotations

import builtins
from collections.abc import AsyncIterator, Iterator
from typing import Any, Literal, Required, TypedDict

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.common import AgentSession, AgentSessionListResponse


class AgentSessionCreateInput(TypedDict, total=False):
    "Create an agent session"

    agent: Required[str]
    "Agent ID (`agi_...`) of the agent that will execute the session."
    instructions: Required[str]
    "Plain-text task description given to the agent as its primary objective for this session."
    max_runs_per_turn: int | None
    "Maximum number of tool invocations allowed within a single agent turn. Defaults to 25."
    max_tokens: int | None
    "Maximum number of tokens the agent may consume across all turns. Defaults to 20,000."
    max_turns: int | None
    "Maximum number of agent turns before the session is automatically terminated. Defaults to 100."
    metadata: dict[str, Any] | None
    "Arbitrary key-value metadata to attach to the session. Stored and returned as-is; not interpreted by the platform."
    name: str | None
    "Human-readable display name for the session. Useful for identifying sessions in the dashboard. `null` if omitted."
    start_idle: bool | None
    'When `true`, create the session without running an opening turn. The session starts in `"waiting"` status and runs its first turn only when you post a message. Defaults to `false`, which runs an initial turn from `instructions` immediately.'
    team: str | None
    "Team ID (`tea_...`) to associate with this session for access-control and attribution purposes. `null` if omitted."
    thread: str | None
    "Thread ID (`thr_...`) to link this session to an existing conversation thread. `null` if omitted."
    user: str | None
    "User ID to associate with this session for attribution purposes. `null` if omitted."


class AgentSessionUpdateInput(TypedDict, total=False):
    "Update an agent session"

    metadata: dict[str, Any] | None
    "Arbitrary key-value metadata to attach to the session. Replaces the existing metadata map entirely. Omit to leave the current metadata unchanged."


class AgentSessionMessageInput(TypedDict, total=False):
    "Send a message to an agent session"

    content: Required[str]
    "Plain-text body of the message to deliver to the agent."
    metadata: dict[str, Any] | None
    "Arbitrary key-value metadata to attach to the message. Stored and returned as-is; not interpreted by the platform."
    role: str | None
    'Role of the message sender. Typically `"user"` or `"tool"`. Defaults to `"user"`.'


class AgentSessionStreamEventSessionUpdate(TypedDict):
    event: Literal["session_update"]
    data: AgentSession


AgentSessionStreamEvent = AgentSessionStreamEventSessionUpdate


class AsyncAgentSessionResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self,
        *,
        agent: builtins.list[str] | None = None,
        status: builtins.list[str] | None = None,
        routine_run: builtins.list[str] | None = None,
        exclude_system: bool | None = None,
        limit: int | None = None,
    ) -> AgentSessionListResponse:
        """
        List agent sessions
        Returns a flat list of agent sessions visible to the authenticated app,
        ordered by creation time descending. Use the `agent`, `status`, and
        `routine_run` filters to narrow results.
        All filters are optional and can be combined. The `status` and `routine_run`
        parameters each accept multiple values; pass the parameter more than once or
        as a comma-separated array to match any of the supplied values.
        Requires an app-scoped API key. Results are limited to sessions that belong
        to agents owned by the authenticated app.

        Args:
            agent: Filter by agent IDs (`agi_...`). Omit to return sessions for all agents in the app. Multiple values are OR'd.
            status: Filter by one or more session statuses. Accepted values are `"pending"`, `"running"`, `"waiting"`, `"completed"`, `"failed"`, and `"cancelled"`. Omit to return sessions in any status.
            routine_run: Filter to sessions that were created by the specified routine run IDs. Accepts up to 100 IDs. Omit to return sessions regardless of their originating routine run.
            exclude_system: When `true`, omits sessions that were created automatically by the platform rather than by your app. Defaults to `false`.
            limit: Maximum number of sessions to return. Defaults to 25; maximum is 100.

        Returns:
            A list of agent sessions matching the supplied filters.
        """
        query: dict[str, object] = {}
        if agent is not None:
            query["agent"] = agent
        if status is not None:
            query["status"] = status
        if routine_run is not None:
            query["routine_run"] = routine_run
        if exclude_system is not None:
            query["exclude_system"] = exclude_system
        if limit is not None:
            query["limit"] = limit
        return await self._http.request(
            "/api/v1/agent_sessions",
            query=query,
            response_type=AgentSessionListResponse,
        )

    async def create(self, input: AgentSessionCreateInput) -> AgentSession:
        """
        Create an agent session
        Creates a new agent session and enqueues it for execution. The session begins
        in `"pending"` status and transitions to `"running"` once the platform picks
        it up. Subscribe to the session stream endpoint to receive real-time status
        updates.
        You must supply the ID of an agent that the authenticated app owns and a
        plain-text `instructions` string describing the task. All other parameters
        are optional and default to the agent's configured limits when omitted.
        Set `start_idle` to `true` to create the session without running an opening
        turn it begins in `"waiting"` status and runs its first turn only once you
        post a message (see the message endpoint). Use this when you want the first
        message to drive the session instead of the `instructions` alone.
        Requires an app-scoped API key. Returns HTTP 201 on success.

        Args:
            input: Request body.
            input.agent: Agent ID (`agi_...`) of the agent that will execute the session.
            input.instructions: Plain-text task description given to the agent as its primary objective for this session.
            input.max_runs_per_turn: Maximum number of tool invocations allowed within a single agent turn. Defaults to 25.
            input.max_tokens: Maximum number of tokens the agent may consume across all turns. Defaults to 20,000.
            input.max_turns: Maximum number of agent turns before the session is automatically terminated. Defaults to 100.
            input.metadata: Arbitrary key-value metadata to attach to the session. Stored and returned as-is; not interpreted by the platform.
            input.name: Human-readable display name for the session. Useful for identifying sessions in the dashboard. `null` if omitted.
            input.start_idle: When `true`, create the session without running an opening turn. The session starts in `"waiting"` status and runs its first turn only when you post a message. Defaults to `false`, which runs an initial turn from `instructions` immediately.
            input.team: Team ID (`tea_...`) to associate with this session for access-control and attribution purposes. `null` if omitted.
            input.thread: Thread ID (`thr_...`) to link this session to an existing conversation thread. `null` if omitted.
            input.user: User ID to associate with this session for attribution purposes. `null` if omitted.

        Returns:
            The newly created agent session.
        """
        return await self._http.request(
            "/api/v1/agent_sessions",
            method="POST",
            body=input,
            response_type=AgentSession,
        )

    async def delete(self, agent_session: str) -> None:
        """
        Delete an agent session
        Permanently deletes an agent session and its associated data. This action is
        irreversible the session record, its trajectory, and all inbox messages are
        removed.
        To stop a running session without deleting it, use the cancel endpoint
        instead. The session must be in a terminal state (`"completed"`, `"failed"`,
        or `"cancelled"`) before it can be deleted; attempting to delete an active
        session returns 422.
        Requires an app-scoped API key. Returns HTTP 204 with no body on success.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to delete.

        Returns:
            Empty body. HTTP 204 indicates the session was permanently deleted.
        """
        await self._http.request(f"/api/v1/agent_sessions/{agent_session}", method="DELETE")

    async def get(self, agent_session: str) -> AgentSession:
        """
        Retrieve an agent session
        Returns the agent session identified by `agent_session`. Use this endpoint
        to poll session status or to inspect the final result after execution
        completes.
        For real-time updates without polling, subscribe to the session stream
        endpoint instead, which delivers server-sent events whenever the session
        state changes.
        Requires an app-scoped API key. The session must belong to an agent owned
        by the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to retrieve.

        Returns:
            The requested agent session.
        """
        return await self._http.request(
            f"/api/v1/agent_sessions/{agent_session}",
            response_type=AgentSession,
        )

    async def update(self, agent_session: str, input: AgentSessionUpdateInput) -> AgentSession:
        """
        Update an agent session
        Updates the mutable fields of an agent session. Currently only `metadata`
        can be changed; supply any key-value pairs you want to store alongside the
        session. Omitting `metadata` leaves it unchanged.
        This endpoint may be called while the session is in any status, including
        while it is actively running.
        Requires an app-scoped API key. The session must belong to an agent owned
        by the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to update.
            input: Request body.
            input.metadata: Arbitrary key-value metadata to attach to the session. Replaces the existing metadata map entirely. Omit to leave the current metadata unchanged.

        Returns:
            The agent session with the updated fields applied.
        """
        return await self._http.request(
            f"/api/v1/agent_sessions/{agent_session}",
            method="PATCH",
            body=input,
            response_type=AgentSession,
        )

    async def cancel(self, agent_session: str) -> AgentSession:
        """
        Cancel an agent session
        Requests cancellation of an active agent session. The session status is set
        to `"cancelled"` and any in-progress agent turn is interrupted as soon as the
        platform can safely stop it.
        If the session is already in a terminal state (`"completed"`, `"failed"`, or
        `"cancelled"`), the call succeeds and returns the session unchanged it is
        safe to call this endpoint more than once.
        Requires an app-scoped API key. The session must belong to an agent owned by
        the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to cancel.

        Returns:
            The agent session after the cancellation request is applied.
        """
        return await self._http.request(
            f"/api/v1/agent_sessions/{agent_session}/cancel",
            method="POST",
            response_type=AgentSession,
        )

    async def message(self, agent_session: str, input: AgentSessionMessageInput) -> AgentSession:
        """
        Send a message to an agent session
        Appends a message to the inbox of the specified agent session. The agent
        reads inbox messages at the start of each turn; sending a message to a
        `"waiting"` session signals it to resume execution.
        Use `role` to identify the sender type. The default role is `"user"`.
        Arbitrary key-value metadata may be attached to the message for tracking
        or display purposes.
        Requires an app-scoped API key. The session must belong to an agent owned
        by the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session whose inbox should receive the message.
            input: Request body.
            input.content: Plain-text body of the message to deliver to the agent.
            input.metadata: Arbitrary key-value metadata to attach to the message. Stored and returned as-is; not interpreted by the platform.
            input.role: Role of the message sender. Typically `"user"` or `"tool"`. Defaults to `"user"`.

        Returns:
            The agent session with the new message appended to its `inbox`.
        """
        return await self._http.request(
            f"/api/v1/agent_sessions/{agent_session}/message",
            method="POST",
            body=input,
            response_type=AgentSession,
        )

    async def stream(self, agent_session: str) -> AsyncIterator[AgentSessionStreamEvent]:
        """
        Stream agent session status
        Opens a Server-Sent Events connection that emits a `session_update` event
        whenever the agent session's status changes, replaying the current status on
        connect and closing on a terminal status (`completed`, `failed`, `cancelled`).

        Args:
            agent_session: ID of the agent session to stream.

        Returns:
            Server-Sent Events stream
        """
        async for event in self._http.stream_sse(f"/api/v1/agent_sessions/{agent_session}/stream"):
            yield event


class AgentSessionResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(
        self,
        *,
        agent: builtins.list[str] | None = None,
        status: builtins.list[str] | None = None,
        routine_run: builtins.list[str] | None = None,
        exclude_system: bool | None = None,
        limit: int | None = None,
    ) -> AgentSessionListResponse:
        """
        List agent sessions
        Returns a flat list of agent sessions visible to the authenticated app,
        ordered by creation time descending. Use the `agent`, `status`, and
        `routine_run` filters to narrow results.
        All filters are optional and can be combined. The `status` and `routine_run`
        parameters each accept multiple values; pass the parameter more than once or
        as a comma-separated array to match any of the supplied values.
        Requires an app-scoped API key. Results are limited to sessions that belong
        to agents owned by the authenticated app.

        Args:
            agent: Filter by agent IDs (`agi_...`). Omit to return sessions for all agents in the app. Multiple values are OR'd.
            status: Filter by one or more session statuses. Accepted values are `"pending"`, `"running"`, `"waiting"`, `"completed"`, `"failed"`, and `"cancelled"`. Omit to return sessions in any status.
            routine_run: Filter to sessions that were created by the specified routine run IDs. Accepts up to 100 IDs. Omit to return sessions regardless of their originating routine run.
            exclude_system: When `true`, omits sessions that were created automatically by the platform rather than by your app. Defaults to `false`.
            limit: Maximum number of sessions to return. Defaults to 25; maximum is 100.

        Returns:
            A list of agent sessions matching the supplied filters.
        """
        query: dict[str, object] = {}
        if agent is not None:
            query["agent"] = agent
        if status is not None:
            query["status"] = status
        if routine_run is not None:
            query["routine_run"] = routine_run
        if exclude_system is not None:
            query["exclude_system"] = exclude_system
        if limit is not None:
            query["limit"] = limit
        return self._http.request(
            "/api/v1/agent_sessions",
            query=query,
            response_type=AgentSessionListResponse,
        )

    def create(self, input: AgentSessionCreateInput) -> AgentSession:
        """
        Create an agent session
        Creates a new agent session and enqueues it for execution. The session begins
        in `"pending"` status and transitions to `"running"` once the platform picks
        it up. Subscribe to the session stream endpoint to receive real-time status
        updates.
        You must supply the ID of an agent that the authenticated app owns and a
        plain-text `instructions` string describing the task. All other parameters
        are optional and default to the agent's configured limits when omitted.
        Set `start_idle` to `true` to create the session without running an opening
        turn it begins in `"waiting"` status and runs its first turn only once you
        post a message (see the message endpoint). Use this when you want the first
        message to drive the session instead of the `instructions` alone.
        Requires an app-scoped API key. Returns HTTP 201 on success.

        Args:
            input: Request body.
            input.agent: Agent ID (`agi_...`) of the agent that will execute the session.
            input.instructions: Plain-text task description given to the agent as its primary objective for this session.
            input.max_runs_per_turn: Maximum number of tool invocations allowed within a single agent turn. Defaults to 25.
            input.max_tokens: Maximum number of tokens the agent may consume across all turns. Defaults to 20,000.
            input.max_turns: Maximum number of agent turns before the session is automatically terminated. Defaults to 100.
            input.metadata: Arbitrary key-value metadata to attach to the session. Stored and returned as-is; not interpreted by the platform.
            input.name: Human-readable display name for the session. Useful for identifying sessions in the dashboard. `null` if omitted.
            input.start_idle: When `true`, create the session without running an opening turn. The session starts in `"waiting"` status and runs its first turn only when you post a message. Defaults to `false`, which runs an initial turn from `instructions` immediately.
            input.team: Team ID (`tea_...`) to associate with this session for access-control and attribution purposes. `null` if omitted.
            input.thread: Thread ID (`thr_...`) to link this session to an existing conversation thread. `null` if omitted.
            input.user: User ID to associate with this session for attribution purposes. `null` if omitted.

        Returns:
            The newly created agent session.
        """
        return self._http.request(
            "/api/v1/agent_sessions",
            method="POST",
            body=input,
            response_type=AgentSession,
        )

    def delete(self, agent_session: str) -> None:
        """
        Delete an agent session
        Permanently deletes an agent session and its associated data. This action is
        irreversible the session record, its trajectory, and all inbox messages are
        removed.
        To stop a running session without deleting it, use the cancel endpoint
        instead. The session must be in a terminal state (`"completed"`, `"failed"`,
        or `"cancelled"`) before it can be deleted; attempting to delete an active
        session returns 422.
        Requires an app-scoped API key. Returns HTTP 204 with no body on success.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to delete.

        Returns:
            Empty body. HTTP 204 indicates the session was permanently deleted.
        """
        self._http.request(f"/api/v1/agent_sessions/{agent_session}", method="DELETE")

    def get(self, agent_session: str) -> AgentSession:
        """
        Retrieve an agent session
        Returns the agent session identified by `agent_session`. Use this endpoint
        to poll session status or to inspect the final result after execution
        completes.
        For real-time updates without polling, subscribe to the session stream
        endpoint instead, which delivers server-sent events whenever the session
        state changes.
        Requires an app-scoped API key. The session must belong to an agent owned
        by the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to retrieve.

        Returns:
            The requested agent session.
        """
        return self._http.request(
            f"/api/v1/agent_sessions/{agent_session}",
            response_type=AgentSession,
        )

    def update(self, agent_session: str, input: AgentSessionUpdateInput) -> AgentSession:
        """
        Update an agent session
        Updates the mutable fields of an agent session. Currently only `metadata`
        can be changed; supply any key-value pairs you want to store alongside the
        session. Omitting `metadata` leaves it unchanged.
        This endpoint may be called while the session is in any status, including
        while it is actively running.
        Requires an app-scoped API key. The session must belong to an agent owned
        by the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to update.
            input: Request body.
            input.metadata: Arbitrary key-value metadata to attach to the session. Replaces the existing metadata map entirely. Omit to leave the current metadata unchanged.

        Returns:
            The agent session with the updated fields applied.
        """
        return self._http.request(
            f"/api/v1/agent_sessions/{agent_session}",
            method="PATCH",
            body=input,
            response_type=AgentSession,
        )

    def cancel(self, agent_session: str) -> AgentSession:
        """
        Cancel an agent session
        Requests cancellation of an active agent session. The session status is set
        to `"cancelled"` and any in-progress agent turn is interrupted as soon as the
        platform can safely stop it.
        If the session is already in a terminal state (`"completed"`, `"failed"`, or
        `"cancelled"`), the call succeeds and returns the session unchanged it is
        safe to call this endpoint more than once.
        Requires an app-scoped API key. The session must belong to an agent owned by
        the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session to cancel.

        Returns:
            The agent session after the cancellation request is applied.
        """
        return self._http.request(
            f"/api/v1/agent_sessions/{agent_session}/cancel",
            method="POST",
            response_type=AgentSession,
        )

    def message(self, agent_session: str, input: AgentSessionMessageInput) -> AgentSession:
        """
        Send a message to an agent session
        Appends a message to the inbox of the specified agent session. The agent
        reads inbox messages at the start of each turn; sending a message to a
        `"waiting"` session signals it to resume execution.
        Use `role` to identify the sender type. The default role is `"user"`.
        Arbitrary key-value metadata may be attached to the message for tracking
        or display purposes.
        Requires an app-scoped API key. The session must belong to an agent owned
        by the authenticated app.

        Args:
            agent_session: Agent session ID (`ase_...`) of the session whose inbox should receive the message.
            input: Request body.
            input.content: Plain-text body of the message to deliver to the agent.
            input.metadata: Arbitrary key-value metadata to attach to the message. Stored and returned as-is; not interpreted by the platform.
            input.role: Role of the message sender. Typically `"user"` or `"tool"`. Defaults to `"user"`.

        Returns:
            The agent session with the new message appended to its `inbox`.
        """
        return self._http.request(
            f"/api/v1/agent_sessions/{agent_session}/message",
            method="POST",
            body=input,
            response_type=AgentSession,
        )

    def stream(self, agent_session: str) -> Iterator[AgentSessionStreamEvent]:
        """
        Stream agent session status
        Opens a Server-Sent Events connection that emits a `session_update` event
        whenever the agent session's status changes, replaying the current status on
        connect and closing on a terminal status (`completed`, `failed`, `cancelled`).

        Args:
            agent_session: ID of the agent session to stream.

        Returns:
            Server-Sent Events stream
        """
        yield from self._http.stream_sse_sync(f"/api/v1/agent_sessions/{agent_session}/stream")
