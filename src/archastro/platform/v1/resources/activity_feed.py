# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 5256b1db0e32

from __future__ import annotations

import builtins
from datetime import datetime
from typing import Any

from pydantic import BaseModel, Field

from ...runtime.http_client import HttpClient, SyncHttpClient


class ActivityFeedListResponseDataItem(BaseModel):
    agent: str | dict[str, Any] | None = Field(
        default=None,
        description="The agent that produced this event. Returns an agent ID (`agi_...`) by default, or an expanded agent object when the association is loaded. `null` if no agent is associated.",
    )
    app: str | None = Field(
        default=None,
        description="ID of the application that produced this entry (`dap_...`). `null` if not scoped to an app.",
    )
    attachments: list[dict[str, Any]] | None = Field(
        default=None,
        description='Array of attachment objects associated with this entry. Each attachment has a `type` field (e.g. `"file"`, `"task"`, `"artifact"`) and type-specific additional fields. Empty array when there are no attachments.',
    )
    automation_run: str | None = Field(
        default=None,
        description="ID of the automation run that produced this entry (`atr_...`). `null` if not produced by an automation run.",
    )
    content: str | None = Field(
        default=None,
        description="A longer explanation of the event rendered as Markdown. `null` if no additional content is available.",
    )
    correlation_id: str | None = Field(
        default=None,
        description="An opaque string used to group related entries together. Entries sharing the same `correlation_id` belong to a single logical operation. `null` if not correlated.",
    )
    created_at: datetime | None = Field(
        default=None, description="When this activity feed entry was created (ISO 8601)."
    )
    id: str = Field(..., description="Activity feed entry ID (`afe_...`).")
    kind: str | None = Field(
        default=None,
        description='The type of event this entry represents, e.g. `"agent_step"` or `"tool_call"`. Determines how `title`, `content`, and `attachments` should be interpreted.',
    )
    level: str | None = Field(
        default=None,
        description='Severity level of the event. One of `"info"`, `"warning"`, or `"error"`. `null` if no severity is set.',
    )
    metadata: dict[str, Any] | None = Field(
        default=None,
        description="Arbitrary key-value metadata stored on this entry. Returns an empty object when no metadata is set.",
    )
    org: str | None = Field(
        default=None,
        description="ID of the organization this entry belongs to (`org_...`). `null` if not org-scoped.",
    )
    routine_run: str | None = Field(
        default=None,
        description="ID of the agent routine run that produced this entry (`arr_...`). `null` if not produced by a routine run.",
    )
    sandbox: str | None = Field(
        default=None,
        description="Identifier of the sandbox environment this entry was generated in. `null` in production contexts.",
    )
    session_record: str | None = Field(
        default=None,
        description="ID of the agent session record this entry belongs to (`ase_...`). `null` if not part of an agent session.",
    )
    team: str | None = Field(
        default=None,
        description="ID of the team this entry is associated with (`tem_...`). `null` if not team-scoped.",
    )
    thread: str | None = Field(
        default=None,
        description="ID of the thread this entry is associated with (`thr_...`). `null` if not linked to a thread.",
    )
    title: str | None = Field(
        default=None,
        description="A one-line human-readable summary of the event. `null` if the entry has no title.",
    )
    updated_at: datetime | None = Field(
        default=None, description="When this activity feed entry was last modified (ISO 8601)."
    )
    user: str | dict[str, Any] | None = Field(
        default=None,
        description="The user who triggered this event. Returns a user ID (`usr_...`) by default, or an expanded user object when the association is loaded. `null` if no user is associated.",
    )


class ActivityFeedListResponse(BaseModel):
    """
    Successful response
    """

    after_cursor: str | None = Field(
        default=None,
        description="Opaque cursor to pass as `after_cursor` to retrieve the next newer page. Absent when no newer entries exist.",
    )
    before_cursor: str | None = Field(
        default=None,
        description="Opaque cursor to pass as `before_cursor` to retrieve the next older page. Absent when no older entries exist.",
    )
    data: list[ActivityFeedListResponseDataItem] = Field(
        ...,
        description="Array of activity feed entry objects for the current page, ordered newest first.",
    )
    has_more: bool = Field(
        ...,
        description="`true` when additional entries exist beyond this page in the requested direction.",
    )


class AsyncActivityFeedResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self,
        *,
        kind: builtins.list[str] | None = None,
        level: builtins.list[str] | None = None,
        agent: builtins.list[str] | None = None,
        thread: builtins.list[str] | None = None,
        team: builtins.list[str] | None = None,
        org: builtins.list[str] | None = None,
        correlation_id: str | None = None,
        limit: int | None = None,
        before_cursor: str | None = None,
        after_cursor: str | None = None,
    ) -> ActivityFeedListResponse:
        """
        List activity feed entries
        Returns a cursor-paginated list of activity feed entries visible to the
        authenticated user. Entries are ordered from newest to oldest by default.
        Use `before_cursor` to page backward and `after_cursor` to page forward.
        Results are scoped to the caller's app. All filter params are optional and
        can be combined. Passing multiple values in an array filter returns entries
        matching any of the supplied values (OR semantics).
        Invalid `kind` or `level` values return a 400 error listing the accepted
        values rather than being silently ignored.

        Args:
            kind: One or more entry kinds to include. Accepted values: routine_run, automation_run, thread_story, agent_quality_verdict, generic. Omit to return all kinds.
            level: One or more severity levels to include. Accepted values: debug, info, warn, error, audit. Omit to return all levels.
            agent: Restrict results to entries associated with these agent IDs (`agt_...`). Accepts multiple values.
            thread: Restrict results to entries associated with these thread IDs (`thr_...`). Accepts multiple values.
            team: Restrict results to entries associated with these team IDs (`tem_...`). Accepts multiple values.
            org: Restrict results to entries associated with these organization IDs (`org_...`). Accepts multiple values.
            correlation_id: Restrict results to entries that share this correlation group identifier. Useful for tracing a chain of related events.
            limit: Maximum number of entries to return per page. Defaults to 50; maximum is 100.
            before_cursor: Opaque cursor from a previous response's `before_cursor` field. Returns entries older than the cursor's position.
            after_cursor: Opaque cursor from a previous response's `after_cursor` field. Returns entries newer than the cursor's position.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if kind is not None:
            query["kind"] = kind
        if level is not None:
            query["level"] = level
        if agent is not None:
            query["agent"] = agent
        if thread is not None:
            query["thread"] = thread
        if team is not None:
            query["team"] = team
        if org is not None:
            query["org"] = org
        if correlation_id is not None:
            query["correlation_id"] = correlation_id
        if limit is not None:
            query["limit"] = limit
        if before_cursor is not None:
            query["before_cursor"] = before_cursor
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return await self._http.request(
            "/api/v1/activity_feed",
            query=query,
            response_type=ActivityFeedListResponse,
        )


class ActivityFeedResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(
        self,
        *,
        kind: builtins.list[str] | None = None,
        level: builtins.list[str] | None = None,
        agent: builtins.list[str] | None = None,
        thread: builtins.list[str] | None = None,
        team: builtins.list[str] | None = None,
        org: builtins.list[str] | None = None,
        correlation_id: str | None = None,
        limit: int | None = None,
        before_cursor: str | None = None,
        after_cursor: str | None = None,
    ) -> ActivityFeedListResponse:
        """
        List activity feed entries
        Returns a cursor-paginated list of activity feed entries visible to the
        authenticated user. Entries are ordered from newest to oldest by default.
        Use `before_cursor` to page backward and `after_cursor` to page forward.
        Results are scoped to the caller's app. All filter params are optional and
        can be combined. Passing multiple values in an array filter returns entries
        matching any of the supplied values (OR semantics).
        Invalid `kind` or `level` values return a 400 error listing the accepted
        values rather than being silently ignored.

        Args:
            kind: One or more entry kinds to include. Accepted values: routine_run, automation_run, thread_story, agent_quality_verdict, generic. Omit to return all kinds.
            level: One or more severity levels to include. Accepted values: debug, info, warn, error, audit. Omit to return all levels.
            agent: Restrict results to entries associated with these agent IDs (`agt_...`). Accepts multiple values.
            thread: Restrict results to entries associated with these thread IDs (`thr_...`). Accepts multiple values.
            team: Restrict results to entries associated with these team IDs (`tem_...`). Accepts multiple values.
            org: Restrict results to entries associated with these organization IDs (`org_...`). Accepts multiple values.
            correlation_id: Restrict results to entries that share this correlation group identifier. Useful for tracing a chain of related events.
            limit: Maximum number of entries to return per page. Defaults to 50; maximum is 100.
            before_cursor: Opaque cursor from a previous response's `before_cursor` field. Returns entries older than the cursor's position.
            after_cursor: Opaque cursor from a previous response's `after_cursor` field. Returns entries newer than the cursor's position.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if kind is not None:
            query["kind"] = kind
        if level is not None:
            query["level"] = level
        if agent is not None:
            query["agent"] = agent
        if thread is not None:
            query["thread"] = thread
        if team is not None:
            query["team"] = team
        if org is not None:
            query["org"] = org
        if correlation_id is not None:
            query["correlation_id"] = correlation_id
        if limit is not None:
            query["limit"] = limit
        if before_cursor is not None:
            query["before_cursor"] = before_cursor
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return self._http.request(
            "/api/v1/activity_feed",
            query=query,
            response_type=ActivityFeedListResponse,
        )
