# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 92e0c4b9c2d0

from __future__ import annotations

from typing import Any, Required, TypedDict

from ...runtime.http_client import HttpClient, SyncHttpClient
from ...types.common import WorkflowWorkItemList


class WorkItemFailInput(TypedDict):
    "Fail workflow work and route its durable execution"

    error: dict[str, Any]
    "JSON-serializable failure returned by the worker."
    lease_owner: str
    "Saved lease token."


class WorkItemHeartbeatInput(TypedDict, total=False):
    "Extend a workflow work item lease"

    lease_owner: Required[str]
    "Saved lease token."
    lease_seconds: int | None
    "Replacement lease duration from 15 through 3600 seconds. Defaults to 300."


class WorkItemStartInput(TypedDict):
    "Mark claimed workflow work as running"

    lease_owner: str
    "Saved lease token."


class WorkItemSubmitInput(TypedDict):
    "Submit workflow work output and wake its durable execution"

    lease_owner: str
    "Saved lease token."
    result: dict[str, Any]
    "JSON-serializable output returned to the workflow."


class AsyncWorkItemResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self,
        *,
        execution: str | None = None,
        limit: int | None = None,
        after_cursor: str | None = None,
    ) -> WorkflowWorkItemList:
        """
        List active workflow work available to the viewer
        Lists queued, claimed, and running external work yielded by durable workflows.
        The top-level collection includes work for every agent the viewer can execute;
        the agent-nested collection limits results to that agent. This discovery
        response never includes lease tokens. Use the agent claim endpoint to acquire
        new work or resume a saved lease.

        Args:
            execution: Optional durable execution ID filter.
            limit: Maximum work items per page. Defaults to 50; maximum is 100.
            after_cursor: Opaque cursor for the next page of older queued work.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if execution is not None:
            query["execution"] = execution
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return await self._http.request(
            "/api/v1/work_items",
            query=query,
            response_type=WorkflowWorkItemList,
        )

    async def fail(self, work_item: str, input: WorkItemFailInput) -> None:
        """
        Fail workflow work and route its durable execution
        Atomically records command failure, marks the work item failed, and either
        wakes the workflow at the node's error edge or fails the owning run when no
        error edge exists. Retrying the same lease and error is idempotent; a
        different terminal payload conflicts.

        Args:
            work_item: Claimed or running work item ID.
            input: Request body.
            input.error: JSON-serializable failure returned by the worker.
            input.lease_owner: Saved lease token.

        Returns:
            No content
        """
        await self._http.request(f"/api/v1/work_items/{work_item}/fail", method="POST", body=input)

    async def heartbeat(self, work_item: str, input: WorkItemHeartbeatInput) -> None:
        """
        Extend a workflow work item lease

        Args:
            work_item: Claimed or running work item ID.
            input: Request body.
            input.lease_owner: Saved lease token.
            input.lease_seconds: Replacement lease duration from 15 through 3600 seconds. Defaults to 300.

        Returns:
            No content
        """
        await self._http.request(
            f"/api/v1/work_items/{work_item}/heartbeat",
            method="POST",
            body=input,
        )

    async def start(self, work_item: str, input: WorkItemStartInput) -> None:
        """
        Mark claimed workflow work as running

        Args:
            work_item: Claimed work item ID.
            input: Request body.
            input.lease_owner: Saved lease token.

        Returns:
            No content
        """
        await self._http.request(f"/api/v1/work_items/{work_item}/start", method="POST", body=input)

    async def submit(self, work_item: str, input: WorkItemSubmitInput) -> None:
        """
        Submit workflow work output and wake its durable execution
        Atomically records the command completion, marks the work item succeeded,
        advances the journal sequence, and enqueues the owning workflow continuation.
        Retrying the same lease and result is idempotent; a different result conflicts.

        Args:
            work_item: Claimed or running work item ID.
            input: Request body.
            input.lease_owner: Saved lease token.
            input.result: JSON-serializable output returned to the workflow.

        Returns:
            No content
        """
        await self._http.request(
            f"/api/v1/work_items/{work_item}/submit",
            method="POST",
            body=input,
        )


class WorkItemResource:
    def __init__(self, http: SyncHttpClient):
        self._http = http

    def list(
        self,
        *,
        execution: str | None = None,
        limit: int | None = None,
        after_cursor: str | None = None,
    ) -> WorkflowWorkItemList:
        """
        List active workflow work available to the viewer
        Lists queued, claimed, and running external work yielded by durable workflows.
        The top-level collection includes work for every agent the viewer can execute;
        the agent-nested collection limits results to that agent. This discovery
        response never includes lease tokens. Use the agent claim endpoint to acquire
        new work or resume a saved lease.

        Args:
            execution: Optional durable execution ID filter.
            limit: Maximum work items per page. Defaults to 50; maximum is 100.
            after_cursor: Opaque cursor for the next page of older queued work.

        Returns:
            Successful response
        """
        query: dict[str, object] = {}
        if execution is not None:
            query["execution"] = execution
        if limit is not None:
            query["limit"] = limit
        if after_cursor is not None:
            query["after_cursor"] = after_cursor
        return self._http.request(
            "/api/v1/work_items",
            query=query,
            response_type=WorkflowWorkItemList,
        )

    def fail(self, work_item: str, input: WorkItemFailInput) -> None:
        """
        Fail workflow work and route its durable execution
        Atomically records command failure, marks the work item failed, and either
        wakes the workflow at the node's error edge or fails the owning run when no
        error edge exists. Retrying the same lease and error is idempotent; a
        different terminal payload conflicts.

        Args:
            work_item: Claimed or running work item ID.
            input: Request body.
            input.error: JSON-serializable failure returned by the worker.
            input.lease_owner: Saved lease token.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/work_items/{work_item}/fail", method="POST", body=input)

    def heartbeat(self, work_item: str, input: WorkItemHeartbeatInput) -> None:
        """
        Extend a workflow work item lease

        Args:
            work_item: Claimed or running work item ID.
            input: Request body.
            input.lease_owner: Saved lease token.
            input.lease_seconds: Replacement lease duration from 15 through 3600 seconds. Defaults to 300.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/work_items/{work_item}/heartbeat", method="POST", body=input)

    def start(self, work_item: str, input: WorkItemStartInput) -> None:
        """
        Mark claimed workflow work as running

        Args:
            work_item: Claimed work item ID.
            input: Request body.
            input.lease_owner: Saved lease token.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/work_items/{work_item}/start", method="POST", body=input)

    def submit(self, work_item: str, input: WorkItemSubmitInput) -> None:
        """
        Submit workflow work output and wake its durable execution
        Atomically records the command completion, marks the work item succeeded,
        advances the journal sequence, and enqueues the owning workflow continuation.
        Retrying the same lease and result is idempotent; a different result conflicts.

        Args:
            work_item: Claimed or running work item ID.
            input: Request body.
            input.lease_owner: Saved lease token.
            input.result: JSON-serializable output returned to the workflow.

        Returns:
            No content
        """
        self._http.request(f"/api/v1/work_items/{work_item}/submit", method="POST", body=input)
