# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 33ac05411d94

from __future__ import annotations

from datetime import datetime
from typing import Required, TypedDict

from pydantic import BaseModel

from ...runtime.http_client import HttpClient
from ...types.common import ValidationResult
from ...types.config import Config, ConfigFacets, ConfigKindSchema


class SystemCloneInput(TypedDict, total=False):
    lookup_key: str | None  # Override lookup_key on the clone
    org: str | None  # Scope the clone to an org (must match viewer's org if set)
    team: str | None  # Clone to this team
    user: str | None  # Clone to this user
    virtual_path: str | None  # Override virtual_path on the clone


class ConfigCreateInput(TypedDict, total=False):
    agent: str | None  # Agent ID (for agent-owned configs)
    change_description: str | None  # Description of changes
    data: dict[str, object] | None  # Additional structured data stored on the version
    kind: Required[str]  # Config kind (e.g., Agent, APITool)
    lookup_key: str | None  # Optional lookup key
    mime_type: Required[str]  # Content mime type
    org: str | None  # Organization ID
    parent: str | None  # Parent config ID (for bundle children, e.g. files under a Skill)
    parent_solution: str | None  # Solution config ID this config was imported with
    raw_content: Required[str]  # Raw content (YAML/JSON/etc)
    relative_path: str | None  # Path within the parent bundle (required when parent is set)
    system: bool | None
    take_ownership: bool | None
    team: str | None  # Team ID (for team-owned configs)
    user: str | None  # User ID (for user-owned configs)
    virtual_path: str | None  # Unique path within the owner scope


class ConfigEncryptSecretInput(TypedDict, total=False):
    org: str | None  # Organization ID — overrides viewer's org for KEK scoping
    plaintext: Required[str]  # Secret value to encrypt


class ConfigValidateInput(TypedDict, total=False):
    agent: str | None  # Agent ID (for agent-owned configs)
    kind: Required[str]  # Config kind to validate against
    mime_type: Required[str]  # Content mime type
    raw_content: Required[str]  # Raw content to validate
    team: str | None  # Team ID (for team-owned configs)
    user: str | None  # User ID (for user-owned configs)


class ConfigUpdateInput(TypedDict, total=False):
    agent: str | None  # Agent ID (for agent-owned configs)
    change_description: str | None  # Description of changes
    data: dict[str, object] | None  # Additional structured data stored on the version
    expected_version: int | None  # Expected current version number for optimistic locking
    lookup_key: str | None  # Update lookup key
    mime_type: str | None  # Content mime type (defaults to existing if not provided)
    org: str | None  # Org ID — scope the lookup to a specific org's configs
    parent_solution: str | None  # Update parent Solution provenance
    raw_content: str | None
    relative_path: str | None  # Update relative path within parent bundle
    system: bool | None
    team: str | None  # Team ID (for team-owned configs)
    user: str | None  # User ID (for user-owned configs)
    virtual_path: str | None  # Update virtual path


class ConfigArchiveInput(TypedDict, total=False):
    agent: str | None  # Agent ID (for agent-owned configs)
    org: str | None  # Org ID — scope the lookup to a specific org's configs
    system: bool | None
    team: str | None  # Team ID (for team-owned configs)
    user: str | None  # User ID (for user-owned configs)


class ConfigChangeOwnerInput(TypedDict, total=False):
    agent: str | None  # New owner: Agent ID
    org: str | None
    system: bool | None  # New owner: system (app-level). Requires privileged viewer.
    team: str | None  # New owner: Team ID
    user: str | None  # New owner: User ID


class ConfigUnarchiveInput(TypedDict, total=False):
    agent: str | None  # Agent ID (for agent-owned configs)
    org: str | None  # Org ID — scope the lookup to a specific org's configs
    system: bool | None
    team: str | None  # Team ID (for team-owned configs)
    user: str | None  # User ID (for user-owned configs)


class KindListResponseDataItem(BaseModel):
    classification: str  # Kind classification: root or supplemental
    description: str | None = None
    kind: str  # The config kind name (e.g., Agent, APITool)
    sample_available: bool  # Whether a YAML sample is available
    schema_available: bool  # Whether a JSON schema is available


class KindListResponse(BaseModel):
    data: list[KindListResponseDataItem]  # The config kinds


class SystemListResponseDataItemCurrentVersion(BaseModel):
    change_description: str | None = None  # Description of changes
    content_hash: str | None = None
    created_at: datetime | None = None  # Creation timestamp
    data: dict[str, object] | None = None  # Additional structured data
    id: str  # Config version ID (cfv_...)
    org: str | None = None  # Organization
    sandbox: str | None = None  # Sandbox
    version_number: int  # Version number


class SystemListResponseDataItem(BaseModel):
    agent: str | None = None  # Agent
    created_at: datetime | None = None  # Creation timestamp
    current_version: SystemListResponseDataItemCurrentVersion | None = None  # Current version
    id: str  # Config ID (cfg_...)
    is_archived: bool | None = None  # Whether config is archived
    kind: str  # Config kind (e.g., Agent, APITool)
    lookup_key: str | None = None  # Optional lookup key
    mime_type: str | None = None  # Content mime type
    org: str | None = None  # Organization
    parent: str | None = None  # Parent config ID (bundle children only)
    parent_solution: str | None = None  # Solution config ID this config was imported with
    raw_content: str | None = None  # Raw file content (system configs only)
    relative_path: str | None = None  # Path within parent bundle (bundle children only)
    sandbox: str | None = None  # Sandbox identifier
    team: str | None = None  # Team
    updated_at: datetime | None = None  # Last update timestamp
    user: str | None = None  # User
    virtual_path: str | None = None  # Unique path within the team


class SystemListResponse(BaseModel):
    data: list[SystemListResponseDataItem]  # The system configs
    has_next: bool  # Has next page
    has_prev: bool  # Has previous page
    page: int  # Current page
    page_size: int  # Results per page
    total_entries: int  # Total entries
    total_pages: int  # Total pages


class ConfigListResponseDataItemCurrentVersion(BaseModel):
    change_description: str | None = None  # Description of changes
    content_hash: str | None = None
    created_at: datetime | None = None  # Creation timestamp
    data: dict[str, object] | None = None  # Additional structured data
    id: str  # Config version ID (cfv_...)
    org: str | None = None  # Organization
    sandbox: str | None = None  # Sandbox
    version_number: int  # Version number


class ConfigListResponseDataItem(BaseModel):
    agent: str | None = None  # Agent
    created_at: datetime | None = None  # Creation timestamp
    current_version: ConfigListResponseDataItemCurrentVersion | None = None  # Current version
    id: str  # Config ID (cfg_...)
    is_archived: bool | None = None  # Whether config is archived
    kind: str  # Config kind (e.g., Agent, APITool)
    lookup_key: str | None = None  # Optional lookup key
    mime_type: str | None = None  # Content mime type
    org: str | None = None  # Organization
    parent: str | None = None  # Parent config ID (bundle children only)
    parent_solution: str | None = None  # Solution config ID this config was imported with
    raw_content: str | None = None  # Raw file content (system configs only)
    relative_path: str | None = None  # Path within parent bundle (bundle children only)
    sandbox: str | None = None  # Sandbox identifier
    team: str | None = None  # Team
    updated_at: datetime | None = None  # Last update timestamp
    user: str | None = None  # User
    virtual_path: str | None = None  # Unique path within the team


class ConfigListResponse(BaseModel):
    data: list[ConfigListResponseDataItem]  # The configs


class ConfigEncryptSecretResponse(BaseModel):
    encrypted_value: str  # Encrypted ciphertext for use in secret_value!


class ConfigVersionsResponseVersionsItem(BaseModel):
    change_description: str | None = None  # Description of changes
    content_hash: str | None = None
    created_at: datetime | None = None  # Creation timestamp
    data: dict[str, object] | None = None  # Additional structured data
    id: str  # Config version ID (cfv_...)
    org: str | None = None  # Organization
    sandbox: str | None = None  # Sandbox
    version_number: int  # Version number


class ConfigVersionsResponse(BaseModel):
    versions: list[ConfigVersionsResponseVersionsItem]  # List of versions


class KindResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(self, *, kind: list[str] | None = None) -> KindListResponse:
        query: dict[str, object] = {}
        if kind is not None:
            query["kind"] = kind
        return await self._http.request("/api/v1/config/kinds", query=query)

    async def schema(self, kind: str) -> ConfigKindSchema:
        return await self._http.request(f"/api/v1/config/kinds/{kind}/schema")


class SystemResource:
    def __init__(self, http: HttpClient):
        self._http = http

    async def list(
        self,
        *,
        kind: str | None = None,
        kinds: list[str] | None = None,
        excluded_path_prefixes: list[str] | None = None,
        page: int | None = None,
        page_size: int | None = None,
    ) -> SystemListResponse:
        query: dict[str, object] = {}
        if kind is not None:
            query["kind"] = kind
        if kinds is not None:
            query["kinds"] = kinds
        if excluded_path_prefixes is not None:
            query["excludedPathPrefixes"] = excluded_path_prefixes
        if page is not None:
            query["page"] = page
        if page_size is not None:
            query["pageSize"] = page_size
        return await self._http.request("/api/v1/config/system", query=query)

    async def facets(self) -> ConfigFacets:
        return await self._http.request("/api/v1/config/system/facets")

    async def get(self, system: str) -> Config:
        return await self._http.request(f"/api/v1/config/system/{system}")

    async def clone(self, system: str, input: SystemCloneInput) -> Config:
        return await self._http.request(
            f"/api/v1/config/system/{system}/clone",
            method="POST",
            body=input,
        )


class ConfigResource:
    def __init__(self, http: HttpClient):
        self._http = http
        self.kinds = KindResource(http)
        self.system = SystemResource(http)

    async def list(
        self,
        *,
        team: str | None = None,
        user: str | None = None,
        agent: str | None = None,
        kind: str | None = None,
        lookup_key: str | None = None,
        path_prefix: str | None = None,
        parents: list[str] | None = None,
        parent_solutions: list[str] | None = None,
    ) -> ConfigListResponse:
        query: dict[str, object] = {}
        if team is not None:
            query["team"] = team
        if user is not None:
            query["user"] = user
        if agent is not None:
            query["agent"] = agent
        if kind is not None:
            query["kind"] = kind
        if lookup_key is not None:
            query["lookupKey"] = lookup_key
        if path_prefix is not None:
            query["pathPrefix"] = path_prefix
        if parents is not None:
            query["parents"] = parents
        if parent_solutions is not None:
            query["parentSolutions"] = parent_solutions
        return await self._http.request("/api/v1/config", query=query)

    async def create(self, input: ConfigCreateInput) -> Config:
        return await self._http.request("/api/v1/config", method="POST", body=input)

    async def encrypt_secret(self, input: ConfigEncryptSecretInput) -> ConfigEncryptSecretResponse:
        return await self._http.request("/api/v1/config/encrypt_secret", method="POST", body=input)

    async def facets(
        self,
        *,
        app: str | None = None,
        team: str | None = None,
        user: str | None = None,
        org: str | None = None,
    ) -> ConfigFacets:
        query: dict[str, object] = {}
        if app is not None:
            query["app"] = app
        if team is not None:
            query["team"] = team
        if user is not None:
            query["user"] = user
        if org is not None:
            query["org"] = org
        return await self._http.request("/api/v1/config/facets", query=query)

    async def validate(self, input: ConfigValidateInput) -> ValidationResult:
        return await self._http.request("/api/v1/config/validate", method="POST", body=input)

    async def delete(self, config: str) -> None:
        await self._http.request(f"/api/v1/config/{config}", method="DELETE")

    async def get(
        self,
        config: str,
        *,
        team: str | None = None,
        user: str | None = None,
        agent: str | None = None,
        system: bool | None = None,
        org: str | None = None,
    ) -> Config:
        query: dict[str, object] = {}
        if team is not None:
            query["team"] = team
        if user is not None:
            query["user"] = user
        if agent is not None:
            query["agent"] = agent
        if system is not None:
            query["system"] = system
        if org is not None:
            query["org"] = org
        return await self._http.request(f"/api/v1/config/{config}", query=query)

    async def update(self, config: str, input: ConfigUpdateInput) -> Config:
        return await self._http.request(f"/api/v1/config/{config}", method="PATCH", body=input)

    async def archive(self, config: str, input: ConfigArchiveInput) -> Config:
        return await self._http.request(
            f"/api/v1/config/{config}/archive",
            method="POST",
            body=input,
        )

    async def change_owner(self, config: str, input: ConfigChangeOwnerInput) -> Config:
        return await self._http.request(
            f"/api/v1/config/{config}/change_owner",
            method="POST",
            body=input,
        )

    async def content(
        self,
        config: str,
        *,
        team: str | None = None,
        user: str | None = None,
        agent: str | None = None,
        system: bool | None = None,
        org: str | None = None,
        format: str | None = None,
        inject_protected_fields: bool | None = None,
    ) -> dict[str, str]:
        query: dict[str, object] = {}
        if team is not None:
            query["team"] = team
        if user is not None:
            query["user"] = user
        if agent is not None:
            query["agent"] = agent
        if system is not None:
            query["system"] = system
        if org is not None:
            query["org"] = org
        if format is not None:
            query["format"] = format
        if inject_protected_fields is not None:
            query["injectProtectedFields"] = inject_protected_fields
        return await self._http.request_raw(f"/api/v1/config/{config}/content", query=query)

    async def unarchive(self, config: str, input: ConfigUnarchiveInput) -> Config:
        return await self._http.request(
            f"/api/v1/config/{config}/unarchive",
            method="POST",
            body=input,
        )

    async def versions(
        self,
        config: str,
        *,
        team: str | None = None,
        user: str | None = None,
        agent: str | None = None,
        system: bool | None = None,
        org: str | None = None,
    ) -> ConfigVersionsResponse:
        query: dict[str, object] = {}
        if team is not None:
            query["team"] = team
        if user is not None:
            query["user"] = user
        if agent is not None:
            query["agent"] = agent
        if system is not None:
            query["system"] = system
        if org is not None:
            query["org"] = org
        return await self._http.request(f"/api/v1/config/{config}/versions", query=query)
