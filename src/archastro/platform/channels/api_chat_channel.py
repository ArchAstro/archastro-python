# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 45505bffc4df

from collections.abc import Callable
from datetime import datetime
from typing import TYPE_CHECKING, Any, Literal, Required, TypedDict

if TYPE_CHECKING:
    from archastro.phx_channel.socket import Socket


class ApiChatForkThreadInput(TypedDict, total=False):
    "Fork a sub-thread from an existing message"

    message_id: Required[str]
    title: str


class ApiChatMarkThreadReadInput(TypedDict):
    "Mark a thread as read up to a given message"

    message_id: str


class ApiChatLoadMoreMessagesInput(TypedDict, total=False):
    "Load additional messages with cursor-based pagination"

    after_cursor: str
    before_cursor: str
    include_metadata: bool
    limit: int


class ApiChatPostMessageInput(TypedDict, total=False):
    "Post a new message with optional uploads and reply-to"

    content: Required[str]
    idempotency_key: str
    reply_to: str
    uploads: list[dict[str, Any]]


class ApiChatPostSimpleMessageInput(TypedDict, total=False):
    "Post a simple text message"

    content: str
    idempotency_key: str
    reply_to: str


class ApiChatEditMessageInput(TypedDict):
    "Edit an existing message's content"

    content: str
    message_id: str


class ApiChatDeleteMessageInput(TypedDict):
    "Delete a message"

    message_id: str


class ApiChatAddReactionInput(TypedDict):
    "Add an emoji reaction to a message"

    emoji: str
    message_id: str


class ApiChatRemoveReactionInput(TypedDict):
    "Remove an emoji reaction from a message"

    emoji: str
    message_id: str


class ApiChatTypingInput(TypedDict):
    "Signal that the current user has started or stopped typing in the thread"

    is_typing: bool


class MessageAddedPayloadMessageAclAddItem(TypedDict, total=False):
    actions: Required[list[str]]
    'Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.'
    principal: str | None
    'The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.'
    principal_type: Required[str]
    'The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.'


class MessageAddedPayloadMessageAclGrantsItem(TypedDict, total=False):
    actions: Required[list[str]]
    'Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.'
    principal: str | None
    'The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.'
    principal_type: Required[str]
    'The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.'


class MessageAddedPayloadMessageAclRemoveItem(TypedDict, total=False):
    principal: str | None
    'The identifier of the principal to remove. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`. Omit when `principal_type` is `"everyone"`.'
    principal_type: Required[str]
    'The kind of principal to remove. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.'


class MessageAddedPayloadMessageAcl(TypedDict, total=False):
    add: list[MessageAddedPayloadMessageAclAddItem] | None
    "Patch mode: grants to add or merge into the existing list. Cannot be combined with `grants`."
    grants: list[MessageAddedPayloadMessageAclGrantsItem] | None
    "Replace mode: the complete new list of grants that replaces all existing entries. Send an empty array (`[]`) to clear all grants. Cannot be combined with `add` or `remove`."
    remove: list[MessageAddedPayloadMessageAclRemoveItem] | None
    "Patch mode: principals whose grants should be removed from the existing list. Cannot be combined with `grants`."


class MessageAddedPayloadMessageActorsItemProfilePicture(TypedDict, total=False):
    file: str | None
    "ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."
    height: int | None
    "Height of the image in pixels. `null` if not known."
    media: str | None
    "ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."
    mime_type: str | None
    'MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.'
    refresh_url: str | None
    "Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."
    url: str | None
    "Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."
    width: int | None
    "Width of the image in pixels. `null` if not known."


class MessageAddedPayloadMessageActorsItem(TypedDict, total=False):
    alias: str | None
    "Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured."
    id: str | None
    'Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.'
    name: str | None
    "Display name of the actor shown in the UI. `null` if no name is set."
    profile_picture: MessageAddedPayloadMessageActorsItemProfilePicture | None
    "Profile picture for the actor. `null` if the actor has no profile picture."


class MessageAddedPayloadMessageAttachmentsItemImageSource(TypedDict, total=False):
    file: str | None
    "ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."
    height: int | None
    "Height of the image in pixels. `null` if not known."
    media: str | None
    "ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."
    mime_type: str | None
    'MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.'
    refresh_url: str | None
    "Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."
    url: str | None
    "Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."
    width: int | None
    "Width of the image in pixels. `null` if not known."


class MessageAddedPayloadMessageAttachmentsItemVariantsItemImageSource(TypedDict, total=False):
    file: str | None
    "ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."
    height: int | None
    "Height of the image in pixels. `null` if not known."
    media: str | None
    "ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."
    mime_type: str | None
    'MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.'
    refresh_url: str | None
    "Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."
    url: str | None
    "Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."
    width: int | None
    "Width of the image in pixels. `null` if not known."


class MessageAddedPayloadMessageAttachmentsItemVariantsItem(TypedDict, total=False):
    content_type: str | None
    'MIME type of this variant\'s file (e.g., `"image/jpeg"`, `"video/mp4"`). `null` if the file is not loaded.'
    created_at: datetime | None
    "When this variant was created (ISO 8601)."
    file: str | None
    "ID of the underlying storage file that backs this variant (`fil_...`)."
    filename: str | None
    "Original filename of the uploaded file for this variant. `null` if the file is not loaded."
    height: int | None
    "Height of this variant in pixels. `null` if not recorded."
    id: Required[str]
    "Media variant ID (`mvr_...`)."
    image_source: MessageAddedPayloadMessageAttachmentsItemVariantsItemImageSource | None
    "Resolved image delivery metadata for this variant, including dimensions and CDN URL. `null` for non-image content types."
    updated_at: datetime | None
    "When this variant was last updated (ISO 8601)."
    url: str | None
    "Signed download URL for this variant, resolved at request time. `null` if the file is unavailable."
    variant_key: str | None
    'Identifier for this variant\'s processing tier. Common values include `"original"` (the unmodified upload) and `"thumbnail"` (a resized preview).'
    width: int | None
    "Width of this variant in pixels. `null` if not recorded."


class MessageAddedPayloadMessageAttachmentsItem(TypedDict, total=False):
    content_type: str | None
    'MIME type of the attached file, e.g. `"image/png"` or `"application/pdf"`. Present on `file`, `artifact`, and `media` types. `null` otherwise.'
    description: str | None
    "Short description. The page meta-description for `scraped_link`, the artifact description for `artifact`, and the task description for `task` types. `null` on other types."
    filename: str | None
    'Original filename of the attached file, e.g. `"report.pdf"`. Present on `file`, `artifact`, and `media` types. `null` otherwise.'
    height: int | None
    "Height in pixels of the media item. Present on `media` type only. `null` otherwise."
    id: Required[str]
    "Unique identifier for this attachment within the message."
    image_height: int | None
    "Height in pixels of the scraped preview image. Present on `scraped_link` type only. `null` otherwise."
    image_source: MessageAddedPayloadMessageAttachmentsItemImageSource | None
    "Image source metadata for inline rendering. Present on `file`, `scraped_link`, `artifact`, and `media` types when the content is an image. `null` otherwise."
    image_url: str | None
    "URL of the preview image extracted from the scraped page. Present on `scraped_link` type only. `null` otherwise."
    image_width: int | None
    "Width in pixels of the scraped preview image. Present on `scraped_link` type only. `null` otherwise."
    media_type: str | None
    'The media category, e.g. `"video"` or `"audio"`. Present on `media` type only; omitted otherwise.'
    name: str | None
    "Display name of the media item. Present on `media` type only. `null` otherwise."
    object: dict[str, Any] | None
    "The full embedded object payload. For `task` type, contains the task record. For `action` type, contains the action definition. For `chart` type, contains the chart with its inline `spec`. Omitted on other types."
    title: str | None
    "Display title. The page title for `scraped_link`, the artifact name for `artifact`, and the task title for `task` types. `null` on other types."
    type: Required[str]
    'The attachment type. One of `"file"`, `"scraped_link"`, `"artifact"`, `"task"`, `"media"`, `"action"`, or `"chart"`. Determines which additional fields are present.'
    url: str | None
    "URL to access the resource. A signed download URL for `file` and `artifact` types; the original URL for `scraped_link`; a media playback URL for `media`. `null` on `task` and `action` types."
    variants: list[MessageAddedPayloadMessageAttachmentsItemVariantsItem] | None
    "Array of available encoding variants for the media item (e.g. different resolutions). Present on `media` type only; omitted otherwise."
    version: int | None
    "Version number of the attached artifact at the time of attachment. Present on `artifact` type only. `null` otherwise."
    width: int | None
    "Width in pixels of the media item. Present on `media` type only. `null` otherwise."


class MessageAddedPayloadMessageReactionsItem(TypedDict, total=False):
    payload: dict[str, Any] | None
    'Type-specific reaction data. For `"emoji_reaction"` reactions, contains an `emoji` key with the Unicode emoji string (e.g., `" "`).'
    type: Required[str]
    'Reaction type identifier. Currently always `"emoji_reaction"` for emoji-based reactions.'
    user: str | None
    "Public ID of the user who added the reaction (`usr_...`)."


class MessageAddedPayloadMessage(TypedDict, total=False):
    acl: MessageAddedPayloadMessageAcl | None
    "Access control list for private messages (grants with `read` action). Only returned to resource owners (and privileged/org-admin viewers) via server-side `field_redactions: [acl: :owner]`; `null` for everyone else."
    actors: list[MessageAddedPayloadMessageActorsItem] | None
    "Resolved actor descriptors for the message sender, combining identity and display metadata. Always contains exactly one entry."
    agent: str | None
    "ID of the agent user that sent this message (`agi_...`). `null` for messages sent by human users."
    agent_mode: Literal["cli", "embedded"] | None
    "Local agent execution mode for this message. One of `cli`, `embedded`, or `null` when the message was not created by a local agent execution path."
    attachments: list[MessageAddedPayloadMessageAttachmentsItem] | None
    "Files, links, tasks, media, artifacts, and actions attached to this message. Empty array if there are no attachments."
    branched_thread: str | None
    "ID of the thread that was branched from this message (`thr_...`). `null` if this message has not spawned a branch thread."
    content: str | None
    "Text content of the message. `null` for messages that contain only attachments."
    created_at: str | None
    "When the message was posted (ISO 8601)."
    has_replies: bool | None
    "Whether this message has at least one reply. Only present when explicitly requested or computed by the server."
    id: Required[str]
    "Message ID (`msg_...`)."
    idempotency_key: str | None
    "Client-supplied idempotency key used to deduplicate message sends. `null` if the sender did not provide one."
    is_deleted: bool | None
    "Whether this message is a deletion tombstone. `true` only on the `message_updated` broadcast emitted when a message is deleted: the original content is replaced with a placeholder and the message no longer exists on the server. Always `false` for live messages."
    legacy_agent: str | None
    "Identifier of the legacy chat agent that sent this message, if applicable. `null` for messages sent by users or modern agent users."
    metadata: dict[str, Any] | None
    "Arbitrary key-value metadata attached to the message. Always present; defaults to an empty object when no metadata has been set."
    org: str | None
    "ID of the organization that owns this message (`org_...`)."
    reactions: list[MessageAddedPayloadMessageReactionsItem] | None
    "Emoji and other reactions added to this message by users. Empty array if no reactions have been added or the association is not preloaded."
    rendering_mode: str | None
    'Display hint for how the message should be rendered. One of `"reply"`, `"direct"`, or `"inline"`. `null` for user-authored messages, which are always rendered as standard replies.'
    replies: list[dict[str, Any]] | None
    "Inline array of reply messages, each serialized as a full message object. Only present when the server has preloaded replies for this message."
    replies_after_cursor: str | None
    "Opaque pagination cursor to fetch replies posted after the current page. Only present when inline replies are included in the response."
    replies_before_cursor: str | None
    "Opaque pagination cursor to fetch replies posted before the current page. Only present when inline replies are included in the response."
    reply_count: int | None
    "Total number of direct replies to this message. Only present when explicitly requested or computed by the server."
    reply_to: dict[str, Any] | None
    "The parent message this message is a reply to, expanded as a full message object when loaded. `null` if this is a top-level message or the association is not preloaded."
    root_message_id: str | None
    "ID of the root message in this reply chain (`msg_...`). `null` for a top-level message. The value is persisted when the reply is created, so callers can correlate a multi-turn session without walking parent messages."
    sandbox: str | None
    "ID of the developer sandbox this message belongs to (`dsb_...`). `null` for non-sandbox messages."
    team: str | None
    "ID of the team this message is scoped to (`tem_...`). `null` if the message is not team-scoped."
    thread: str | None
    "ID of the thread this message belongs to (`thr_...`)."
    type: str | None
    "Optional client-defined classification for the message (for example `note` or `status`). Free-form string up to 64 characters. The value `system` is reserved for platform-authored messages and cannot be set by clients. `null` when unset."
    user: str | dict[str, Any] | None
    "The human user who sent this message. Returns a public ID string (`usr_...`) when the association is not preloaded, or an expanded user object when it is. `null` for messages sent by agents."
    visibility: Literal["default", "private"] | None
    "Message-level visibility. `default` is visible to anyone who can see the parent thread. `private` is restricted to the sender and explicit ACL `read` grantees."


class MessageAddedPayload(TypedDict, total=False):
    "Broadcast when a new message is added to a thread"

    after_cursor: str | None
    before_cursor: str | None
    message: MessageAddedPayloadMessage | None
    "A chat message posted in a thread, including its content, author, attachments, reactions, and optional reply metadata."
    thread_id: str | None


class MessageUpdatedPayloadMessageAclAddItem(TypedDict, total=False):
    actions: Required[list[str]]
    'Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.'
    principal: str | None
    'The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.'
    principal_type: Required[str]
    'The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.'


class MessageUpdatedPayloadMessageAclGrantsItem(TypedDict, total=False):
    actions: Required[list[str]]
    'Array of action strings the principal is permitted to perform, e.g. `["read", "write"]`. Must contain at least one entry.'
    principal: str | None
    'The identifier of the principal. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`; omit entirely when `principal_type` is `"everyone"`.'
    principal_type: Required[str]
    'The kind of principal receiving the grant. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.'


class MessageUpdatedPayloadMessageAclRemoveItem(TypedDict, total=False):
    principal: str | None
    'The identifier of the principal to remove. A string ID for `"user"`, `"team"`, `"org"`, and `"agent"` types; one of `"admin"`, `"member"`, or `"viewer"` for `"org_role"`. Omit when `principal_type` is `"everyone"`.'
    principal_type: Required[str]
    'The kind of principal to remove. One of `"user"`, `"team"`, `"org"`, `"org_role"`, `"agent"`, or `"everyone"`.'


class MessageUpdatedPayloadMessageAcl(TypedDict, total=False):
    add: list[MessageUpdatedPayloadMessageAclAddItem] | None
    "Patch mode: grants to add or merge into the existing list. Cannot be combined with `grants`."
    grants: list[MessageUpdatedPayloadMessageAclGrantsItem] | None
    "Replace mode: the complete new list of grants that replaces all existing entries. Send an empty array (`[]`) to clear all grants. Cannot be combined with `add` or `remove`."
    remove: list[MessageUpdatedPayloadMessageAclRemoveItem] | None
    "Patch mode: principals whose grants should be removed from the existing list. Cannot be combined with `grants`."


class MessageUpdatedPayloadMessageActorsItemProfilePicture(TypedDict, total=False):
    file: str | None
    "ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."
    height: int | None
    "Height of the image in pixels. `null` if not known."
    media: str | None
    "ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."
    mime_type: str | None
    'MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.'
    refresh_url: str | None
    "Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."
    url: str | None
    "Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."
    width: int | None
    "Width of the image in pixels. `null` if not known."


class MessageUpdatedPayloadMessageActorsItem(TypedDict, total=False):
    alias: str | None
    "Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured."
    id: str | None
    'Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.'
    name: str | None
    "Display name of the actor shown in the UI. `null` if no name is set."
    profile_picture: MessageUpdatedPayloadMessageActorsItemProfilePicture | None
    "Profile picture for the actor. `null` if the actor has no profile picture."


class MessageUpdatedPayloadMessageAttachmentsItemImageSource(TypedDict, total=False):
    file: str | None
    "ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."
    height: int | None
    "Height of the image in pixels. `null` if not known."
    media: str | None
    "ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."
    mime_type: str | None
    'MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.'
    refresh_url: str | None
    "Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."
    url: str | None
    "Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."
    width: int | None
    "Width of the image in pixels. `null` if not known."


class MessageUpdatedPayloadMessageAttachmentsItemVariantsItemImageSource(TypedDict, total=False):
    file: str | None
    "ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."
    height: int | None
    "Height of the image in pixels. `null` if not known."
    media: str | None
    "ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."
    mime_type: str | None
    'MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.'
    refresh_url: str | None
    "Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."
    url: str | None
    "Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."
    width: int | None
    "Width of the image in pixels. `null` if not known."


class MessageUpdatedPayloadMessageAttachmentsItemVariantsItem(TypedDict, total=False):
    content_type: str | None
    'MIME type of this variant\'s file (e.g., `"image/jpeg"`, `"video/mp4"`). `null` if the file is not loaded.'
    created_at: datetime | None
    "When this variant was created (ISO 8601)."
    file: str | None
    "ID of the underlying storage file that backs this variant (`fil_...`)."
    filename: str | None
    "Original filename of the uploaded file for this variant. `null` if the file is not loaded."
    height: int | None
    "Height of this variant in pixels. `null` if not recorded."
    id: Required[str]
    "Media variant ID (`mvr_...`)."
    image_source: MessageUpdatedPayloadMessageAttachmentsItemVariantsItemImageSource | None
    "Resolved image delivery metadata for this variant, including dimensions and CDN URL. `null` for non-image content types."
    updated_at: datetime | None
    "When this variant was last updated (ISO 8601)."
    url: str | None
    "Signed download URL for this variant, resolved at request time. `null` if the file is unavailable."
    variant_key: str | None
    'Identifier for this variant\'s processing tier. Common values include `"original"` (the unmodified upload) and `"thumbnail"` (a resized preview).'
    width: int | None
    "Width of this variant in pixels. `null` if not recorded."


class MessageUpdatedPayloadMessageAttachmentsItem(TypedDict, total=False):
    content_type: str | None
    'MIME type of the attached file, e.g. `"image/png"` or `"application/pdf"`. Present on `file`, `artifact`, and `media` types. `null` otherwise.'
    description: str | None
    "Short description. The page meta-description for `scraped_link`, the artifact description for `artifact`, and the task description for `task` types. `null` on other types."
    filename: str | None
    'Original filename of the attached file, e.g. `"report.pdf"`. Present on `file`, `artifact`, and `media` types. `null` otherwise.'
    height: int | None
    "Height in pixels of the media item. Present on `media` type only. `null` otherwise."
    id: Required[str]
    "Unique identifier for this attachment within the message."
    image_height: int | None
    "Height in pixels of the scraped preview image. Present on `scraped_link` type only. `null` otherwise."
    image_source: MessageUpdatedPayloadMessageAttachmentsItemImageSource | None
    "Image source metadata for inline rendering. Present on `file`, `scraped_link`, `artifact`, and `media` types when the content is an image. `null` otherwise."
    image_url: str | None
    "URL of the preview image extracted from the scraped page. Present on `scraped_link` type only. `null` otherwise."
    image_width: int | None
    "Width in pixels of the scraped preview image. Present on `scraped_link` type only. `null` otherwise."
    media_type: str | None
    'The media category, e.g. `"video"` or `"audio"`. Present on `media` type only; omitted otherwise.'
    name: str | None
    "Display name of the media item. Present on `media` type only. `null` otherwise."
    object: dict[str, Any] | None
    "The full embedded object payload. For `task` type, contains the task record. For `action` type, contains the action definition. For `chart` type, contains the chart with its inline `spec`. Omitted on other types."
    title: str | None
    "Display title. The page title for `scraped_link`, the artifact name for `artifact`, and the task title for `task` types. `null` on other types."
    type: Required[str]
    'The attachment type. One of `"file"`, `"scraped_link"`, `"artifact"`, `"task"`, `"media"`, `"action"`, or `"chart"`. Determines which additional fields are present.'
    url: str | None
    "URL to access the resource. A signed download URL for `file` and `artifact` types; the original URL for `scraped_link`; a media playback URL for `media`. `null` on `task` and `action` types."
    variants: list[MessageUpdatedPayloadMessageAttachmentsItemVariantsItem] | None
    "Array of available encoding variants for the media item (e.g. different resolutions). Present on `media` type only; omitted otherwise."
    version: int | None
    "Version number of the attached artifact at the time of attachment. Present on `artifact` type only. `null` otherwise."
    width: int | None
    "Width in pixels of the media item. Present on `media` type only. `null` otherwise."


class MessageUpdatedPayloadMessageReactionsItem(TypedDict, total=False):
    payload: dict[str, Any] | None
    'Type-specific reaction data. For `"emoji_reaction"` reactions, contains an `emoji` key with the Unicode emoji string (e.g., `" "`).'
    type: Required[str]
    'Reaction type identifier. Currently always `"emoji_reaction"` for emoji-based reactions.'
    user: str | None
    "Public ID of the user who added the reaction (`usr_...`)."


class MessageUpdatedPayloadMessage(TypedDict, total=False):
    acl: MessageUpdatedPayloadMessageAcl | None
    "Access control list for private messages (grants with `read` action). Only returned to resource owners (and privileged/org-admin viewers) via server-side `field_redactions: [acl: :owner]`; `null` for everyone else."
    actors: list[MessageUpdatedPayloadMessageActorsItem] | None
    "Resolved actor descriptors for the message sender, combining identity and display metadata. Always contains exactly one entry."
    agent: str | None
    "ID of the agent user that sent this message (`agi_...`). `null` for messages sent by human users."
    agent_mode: Literal["cli", "embedded"] | None
    "Local agent execution mode for this message. One of `cli`, `embedded`, or `null` when the message was not created by a local agent execution path."
    attachments: list[MessageUpdatedPayloadMessageAttachmentsItem] | None
    "Files, links, tasks, media, artifacts, and actions attached to this message. Empty array if there are no attachments."
    branched_thread: str | None
    "ID of the thread that was branched from this message (`thr_...`). `null` if this message has not spawned a branch thread."
    content: str | None
    "Text content of the message. `null` for messages that contain only attachments."
    created_at: str | None
    "When the message was posted (ISO 8601)."
    has_replies: bool | None
    "Whether this message has at least one reply. Only present when explicitly requested or computed by the server."
    id: Required[str]
    "Message ID (`msg_...`)."
    idempotency_key: str | None
    "Client-supplied idempotency key used to deduplicate message sends. `null` if the sender did not provide one."
    is_deleted: bool | None
    "Whether this message is a deletion tombstone. `true` only on the `message_updated` broadcast emitted when a message is deleted: the original content is replaced with a placeholder and the message no longer exists on the server. Always `false` for live messages."
    legacy_agent: str | None
    "Identifier of the legacy chat agent that sent this message, if applicable. `null` for messages sent by users or modern agent users."
    metadata: dict[str, Any] | None
    "Arbitrary key-value metadata attached to the message. Always present; defaults to an empty object when no metadata has been set."
    org: str | None
    "ID of the organization that owns this message (`org_...`)."
    reactions: list[MessageUpdatedPayloadMessageReactionsItem] | None
    "Emoji and other reactions added to this message by users. Empty array if no reactions have been added or the association is not preloaded."
    rendering_mode: str | None
    'Display hint for how the message should be rendered. One of `"reply"`, `"direct"`, or `"inline"`. `null` for user-authored messages, which are always rendered as standard replies.'
    replies: list[dict[str, Any]] | None
    "Inline array of reply messages, each serialized as a full message object. Only present when the server has preloaded replies for this message."
    replies_after_cursor: str | None
    "Opaque pagination cursor to fetch replies posted after the current page. Only present when inline replies are included in the response."
    replies_before_cursor: str | None
    "Opaque pagination cursor to fetch replies posted before the current page. Only present when inline replies are included in the response."
    reply_count: int | None
    "Total number of direct replies to this message. Only present when explicitly requested or computed by the server."
    reply_to: dict[str, Any] | None
    "The parent message this message is a reply to, expanded as a full message object when loaded. `null` if this is a top-level message or the association is not preloaded."
    root_message_id: str | None
    "ID of the root message in this reply chain (`msg_...`). `null` for a top-level message. The value is persisted when the reply is created, so callers can correlate a multi-turn session without walking parent messages."
    sandbox: str | None
    "ID of the developer sandbox this message belongs to (`dsb_...`). `null` for non-sandbox messages."
    team: str | None
    "ID of the team this message is scoped to (`tem_...`). `null` if the message is not team-scoped."
    thread: str | None
    "ID of the thread this message belongs to (`thr_...`)."
    type: str | None
    "Optional client-defined classification for the message (for example `note` or `status`). Free-form string up to 64 characters. The value `system` is reserved for platform-authored messages and cannot be set by clients. `null` when unset."
    user: str | dict[str, Any] | None
    "The human user who sent this message. Returns a public ID string (`usr_...`) when the association is not preloaded, or an expanded user object when it is. `null` for messages sent by agents."
    visibility: Literal["default", "private"] | None
    "Message-level visibility. `default` is visible to anyone who can see the parent thread. `private` is restricted to the sender and explicit ACL `read` grantees."


class MessageUpdatedPayload(TypedDict, total=False):
    "Broadcast when a message is updated or removed"

    message: MessageUpdatedPayloadMessage | None
    "A chat message posted in a thread, including its content, author, attachments, reactions, and optional reply metadata."
    thread_id: str | None


class ThreadEventPayload(TypedDict, total=False):
    "Broadcast thread-level events (agent updates, read state, unread counts)"

    payload: dict[str, Any] | None
    thread_id: str | None
    type: str | None


class SystemEventPayload(TypedDict, total=False):
    "Broadcast system-wide events"

    event: dict[str, Any] | None


class TypingPayloadActorProfilePicture(TypedDict, total=False):
    file: str | None
    "ID of the underlying storage file (`fil_...`). `null` when the image is not backed by a platform storage file."
    height: int | None
    "Height of the image in pixels. `null` if not known."
    media: str | None
    "ID of the associated media record (`med_...`). `null` when the image is not linked to a media entity."
    mime_type: str | None
    'MIME type of the image, e.g. `"image/png"` or `"image/jpeg"`. `null` if not known.'
    refresh_url: str | None
    "Endpoint URL you can call to obtain a fresh signed `url` when the current one has expired. `null` if the URL does not require refreshing."
    url: str | None
    "Signed or public URL for downloading the image. May be time-limited; use `refresh_url` to obtain a new URL when this one expires."
    width: int | None
    "Width of the image in pixels. `null` if not known."


class TypingPayloadActor(TypedDict, total=False):
    alias: str | None
    "Short handle or alias for the actor, used as an alternate display identifier. `null` if not configured."
    id: str | None
    'Composite actor identifier. Format is `"user-<usr_...>"` for human users or `"agent-<agi_...>"` for agents.'
    name: str | None
    "Display name of the actor shown in the UI. `null` if no name is set."
    profile_picture: TypingPayloadActorProfilePicture | None
    "Profile picture for the actor. `null` if the actor has no profile picture."


class TypingPayload(TypedDict, total=False):
    "Broadcast when a participant (human or agent) starts or stops typing. Ephemeral; never persisted."

    actor: TypingPayloadActor | None
    "The entity that authored a message, either a human user or an agent."
    is_typing: bool | None
    thread_id: str | None


# Channel for real-time chat messaging.
# Supports team-scoped and user-scoped threads with keyed, transient, and direct
# thread access patterns.
class ApiChatChannel:
    def __init__(self, channel, join_response=None):
        self._channel = channel
        self.join_response = join_response

    # Join a team-scoped thread by ID
    @staticmethod
    def topic_team_thread(team_id: str, thread_id: str) -> str:
        return f"api:chat:team:{team_id}:thread:{thread_id}"

    # Join a team-scoped thread by ID
    @classmethod
    async def join_team_thread(
        cls,
        socket: "Socket",
        team_id: str,
        thread_id: str,
        *,
        after_cursor: str | None = None,
        before_cursor: str | None = None,
        include_metadata: bool | None = None,
        limit: int | None = None,
    ) -> "ApiChatChannel":
        topic = cls.topic_team_thread(team_id, thread_id)
        channel = socket.channel(topic)
        payload: dict[str, object] = {}
        if after_cursor is not None:
            payload["after_cursor"] = after_cursor
        if before_cursor is not None:
            payload["before_cursor"] = before_cursor
        if include_metadata is not None:
            payload["include_metadata"] = include_metadata
        if limit is not None:
            payload["limit"] = limit
        join_response = await channel.join(payload)
        return cls(channel, join_response)

    # Join or create a team-scoped keyed thread
    @staticmethod
    def topic_team_keyed(team_id: str, key: str) -> str:
        return f"api:chat:team:{team_id}:key:{key}"

    # Join or create a team-scoped keyed thread
    @classmethod
    async def join_team_keyed(
        cls,
        socket: "Socket",
        team_id: str,
        key: str,
        *,
        after_cursor: str | None = None,
        before_cursor: str | None = None,
        include_metadata: bool | None = None,
        limit: int | None = None,
    ) -> "ApiChatChannel":
        topic = cls.topic_team_keyed(team_id, key)
        channel = socket.channel(topic)
        payload: dict[str, object] = {}
        if after_cursor is not None:
            payload["after_cursor"] = after_cursor
        if before_cursor is not None:
            payload["before_cursor"] = before_cursor
        if include_metadata is not None:
            payload["include_metadata"] = include_metadata
        if limit is not None:
            payload["limit"] = limit
        join_response = await channel.join(payload)
        return cls(channel, join_response)

    # Join a team-scoped transient (ephemeral) thread
    @staticmethod
    def topic_team_transient(team_id: str, key: str) -> str:
        return f"api:chat:team:{team_id}:transient:{key}"

    # Join a team-scoped transient (ephemeral) thread
    @classmethod
    async def join_team_transient(
        cls,
        socket: "Socket",
        team_id: str,
        key: str,
        *,
        after_cursor: str | None = None,
        before_cursor: str | None = None,
        include_metadata: bool | None = None,
        limit: int | None = None,
    ) -> "ApiChatChannel":
        topic = cls.topic_team_transient(team_id, key)
        channel = socket.channel(topic)
        payload: dict[str, object] = {}
        if after_cursor is not None:
            payload["after_cursor"] = after_cursor
        if before_cursor is not None:
            payload["before_cursor"] = before_cursor
        if include_metadata is not None:
            payload["include_metadata"] = include_metadata
        if limit is not None:
            payload["limit"] = limit
        join_response = await channel.join(payload)
        return cls(channel, join_response)

    # Join a user-scoped thread by ID, optionally supplying local tools for a personal thread
    @staticmethod
    def topic_user_thread(thread_id: str) -> str:
        return f"api:chat:user:thread:{thread_id}"

    # Join a user-scoped thread by ID, optionally supplying local tools for a personal thread
    @classmethod
    async def join_user_thread(
        cls,
        socket: "Socket",
        thread_id: str,
        *,
        after_cursor: str | None = None,
        before_cursor: str | None = None,
        include_metadata: bool | None = None,
        limit: int | None = None,
        local_tool_provider_id: str | None = None,
        local_tools: list[dict[str, Any]] | None = None,
    ) -> "ApiChatChannel":
        topic = cls.topic_user_thread(thread_id)
        channel = socket.channel(topic)
        payload: dict[str, object] = {}
        if after_cursor is not None:
            payload["after_cursor"] = after_cursor
        if before_cursor is not None:
            payload["before_cursor"] = before_cursor
        if include_metadata is not None:
            payload["include_metadata"] = include_metadata
        if limit is not None:
            payload["limit"] = limit
        if local_tool_provider_id is not None:
            payload["local_tool_provider_id"] = local_tool_provider_id
        if local_tools is not None:
            payload["local_tools"] = local_tools
        join_response = await channel.join(payload)
        return cls(channel, join_response)

    # Join or create a user-scoped keyed thread
    @staticmethod
    def topic_user_keyed(key: str) -> str:
        return f"api:chat:user:key:{key}"

    # Join or create a user-scoped keyed thread
    @classmethod
    async def join_user_keyed(
        cls,
        socket: "Socket",
        key: str,
        *,
        after_cursor: str | None = None,
        before_cursor: str | None = None,
        include_metadata: bool | None = None,
        limit: int | None = None,
    ) -> "ApiChatChannel":
        topic = cls.topic_user_keyed(key)
        channel = socket.channel(topic)
        payload: dict[str, object] = {}
        if after_cursor is not None:
            payload["after_cursor"] = after_cursor
        if before_cursor is not None:
            payload["before_cursor"] = before_cursor
        if include_metadata is not None:
            payload["include_metadata"] = include_metadata
        if limit is not None:
            payload["limit"] = limit
        join_response = await channel.join(payload)
        return cls(channel, join_response)

    # Join a user-scoped transient (ephemeral) thread
    @staticmethod
    def topic_user_transient(key: str) -> str:
        return f"api:chat:user:transient:{key}"

    # Join a user-scoped transient (ephemeral) thread
    @classmethod
    async def join_user_transient(
        cls,
        socket: "Socket",
        key: str,
        *,
        after_cursor: str | None = None,
        before_cursor: str | None = None,
        include_metadata: bool | None = None,
        limit: int | None = None,
    ) -> "ApiChatChannel":
        topic = cls.topic_user_transient(key)
        channel = socket.channel(topic)
        payload: dict[str, object] = {}
        if after_cursor is not None:
            payload["after_cursor"] = after_cursor
        if before_cursor is not None:
            payload["before_cursor"] = before_cursor
        if include_metadata is not None:
            payload["include_metadata"] = include_metadata
        if limit is not None:
            payload["limit"] = limit
        join_response = await channel.join(payload)
        return cls(channel, join_response)

    # Leave the underlying channel.
    async def leave(self):
        await self._channel.leave()

    # Fork a sub-thread from an existing message
    async def api_chat_fork_thread(self, payload: ApiChatForkThreadInput) -> dict[str, Any]:
        return await self._channel.push("api:chat:fork_thread", payload)

    # Mark a thread as read up to a given message
    async def api_chat_mark_thread_read(
        self, payload: ApiChatMarkThreadReadInput
    ) -> dict[str, Any]:
        return await self._channel.push("api:chat:mark_thread_read", payload)

    # List all messages in the current thread
    async def api_chat_list_messages(self, payload: dict) -> dict[str, Any]:
        return await self._channel.push("api:chat:list_messages", payload)

    # Load additional messages with cursor-based pagination
    async def api_chat_load_more_messages(
        self, payload: ApiChatLoadMoreMessagesInput
    ) -> dict[str, Any]:
        return await self._channel.push("api:chat:load_more_messages", payload)

    # Post a new message with optional uploads and reply-to
    async def api_chat_post_message(self, payload: ApiChatPostMessageInput) -> dict[str, Any]:
        return await self._channel.push("api:chat:post_message", payload)

    # Post a simple text message
    async def api_chat_post_simple_message(
        self, payload: ApiChatPostSimpleMessageInput
    ) -> dict[str, Any]:
        return await self._channel.push("api:chat:post_simple_message", payload)

    # Edit an existing message's content
    async def api_chat_edit_message(self, payload: ApiChatEditMessageInput) -> dict[str, Any]:
        return await self._channel.push("api:chat:edit_message", payload)

    # Delete a message
    async def api_chat_delete_message(self, payload: ApiChatDeleteMessageInput) -> dict[str, Any]:
        return await self._channel.push("api:chat:delete_message", payload)

    # Add an emoji reaction to a message
    async def api_chat_add_reaction(self, payload: ApiChatAddReactionInput) -> dict[str, Any]:
        return await self._channel.push("api:chat:add_reaction", payload)

    # Remove an emoji reaction from a message
    async def api_chat_remove_reaction(self, payload: ApiChatRemoveReactionInput) -> dict[str, Any]:
        return await self._channel.push("api:chat:remove_reaction", payload)

    # Signal that the current user has started or stopped typing in the thread
    async def api_chat_typing(self, payload: ApiChatTypingInput) -> dict[str, Any]:
        return await self._channel.push("api:chat:typing", payload)

    # Broadcast when a new message is added to a thread
    def on_message_added(
        self, callback: Callable[[MessageAddedPayload], None]
    ) -> Callable[[], None]:
        return self._channel.on("message_added", callback)

    # Broadcast when a message is updated or removed
    def on_message_updated(
        self, callback: Callable[[MessageUpdatedPayload], None]
    ) -> Callable[[], None]:
        return self._channel.on("message_updated", callback)

    # Broadcast thread-level events (agent updates, read state, unread counts)
    def on_thread_event(self, callback: Callable[[ThreadEventPayload], None]) -> Callable[[], None]:
        return self._channel.on("thread_event", callback)

    # Broadcast system-wide events
    def on_system_event(self, callback: Callable[[SystemEventPayload], None]) -> Callable[[], None]:
        return self._channel.on("system_event", callback)

    # Broadcast when a participant (human or agent) starts or stops typing. Ephemeral; never persisted.
    def on_typing(self, callback: Callable[[TypingPayload], None]) -> Callable[[], None]:
        return self._channel.on("typing", callback)
