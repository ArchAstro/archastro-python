# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: d15c5f3ba3a8

from datetime import datetime

from pydantic import BaseModel, Field


class SystemAccessToken(BaseModel):
    """
    A long-lived API credential associated with a system account, used to authenticate server-to-server requests.
    """

    created_at: datetime | None = Field(
        default=None, description="When this token was created (ISO 8601)."
    )
    created_by_agent_user: str | None = Field(
        default=None,
        description="Agent user that minted this token (`usr_...`). `null` unless an agent minted it.",
    )
    created_by_developer: str | None = Field(
        default=None,
        description="Developer account that minted this token (`dva_...`). `null` unless minted with a developer token.",
    )
    created_by_org: str | None = Field(
        default=None,
        description="Org of the principal that minted this token (`org_...`). `null` on legacy rows.",
    )
    created_by_team: str | None = Field(
        default=None,
        description="Team that minted this token (`tem_...`). `null` unless minted as a team.",
    )
    created_by_user: str | None = Field(
        default=None,
        description="User who minted this token (`usr_...`). Distinct from the token subject. `null` on legacy rows.",
    )
    expires_at: datetime | None = Field(
        default=None,
        description="When the token expires. `null` on legacy rows that predate stored expiry.",
    )
    id: str = Field(..., description="Token ID (`sat_...`).")
    last_used_at: datetime | None = Field(
        default=None,
        description="When this token was last used to authenticate a request. `null` if the token has never been used.",
    )
    name: str | None = Field(
        default=None,
        description="Human-readable label assigned to this token at creation time. `null` when no label was supplied.",
    )
    revoked_at: datetime | None = Field(
        default=None,
        description="When this token was revoked. `null` if the token is still active.",
    )
    scopes: str | None = Field(
        default=None,
        description="Space-separated OAuth scopes stamped on the token. `null` on legacy rows; treat as `full_access`.",
    )
    token: str | None = Field(
        default=None,
        description="Raw bearer token string. Present only in the response to the create request; never returned again after that.",
    )
