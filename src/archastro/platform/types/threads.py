# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 7efd3b7c1931

from datetime import datetime
from typing import Any, Literal

from pydantic import BaseModel, Field

from .common import Acl, Agent, Attachment, Message
from .users import User


class ThreadSettings(BaseModel):
    """
    Configuration settings for a thread that control AI agent behavior and other thread-level preferences.
    """

    agent_enabled: bool | None = Field(
        default=None,
        description="Whether the AI agent is active for this thread. `true` enables AI responses; `false` disables them. Defaults to `true` when settings have not been explicitly configured. `null` when a client explicitly cleared the setting.",
    )


class Thread(BaseModel):
    """
    A chat thread, representing a conversation channel that can be owned by a user, team, or agent and may contain messages, participants, and AI agent activity.
    """

    agent_user: str | None = Field(
        default=None,
        description="ID of the agent that owns this thread (`agt_...`). `null` for user-owned or team-owned threads.",
    )
    created_at: str | None = Field(
        default=None, description="When the thread was created (ISO 8601)."
    )
    creator: str | dict[str, Any] | None = Field(
        default=None,
        description="User who created this thread. Returns a user ID (`usr_...`) by default, or an expanded user object when the association is loaded. `null` if the creator is unknown.",
    )
    description: str | None = Field(
        default=None,
        description="Optional description or purpose statement for the thread. `null` if not set.",
    )
    id: str = Field(..., description="Thread ID (`thr_...`).")
    is_channel: bool | None = Field(
        default=None,
        description="Whether this thread operates as a channel a multi-member broadcast-style conversation.",
    )
    is_default: bool | None = Field(
        default=None,
        description="Whether this is the default thread for its owner. Each user or team has at most one default thread.",
    )
    is_transient: bool | None = Field(
        default=None,
        description="Whether this thread is ephemeral and may be deleted automatically after a period of inactivity or when its TTL expires.",
    )
    is_unlisted: bool | None = Field(
        default=None,
        description="Whether this thread is hidden from public discovery. Unlisted threads are accessible only to direct participants.",
    )
    key: str | None = Field(
        default=None,
        description="Application-defined stable key that uniquely identifies the thread within its scope. Useful for idempotent creation. `null` if not set.",
    )
    kind: str | None = Field(
        default=None,
        description='Thread subtype: `"standard"` for ordinary threads, `"personal"` for a user-and-owned-agents roster, `"slack_mirror"` for the membership-strict mirror of a Slack channel, or `"slashwork_mirror"` for the membership-strict mirror of a Slashwork group. `personal` is an explicit user-thread creation option; mirror kinds are server-derived.',
    )
    last_activity: str | None = Field(
        default=None,
        description="When the most recent message was posted in this thread, falling back to the thread's creation time if it has no messages. Always populated on thread list endpoints (which order by it, after default threads); `null` on endpoints that don't compute activity enrichment.",
    )
    last_message_preview: str | None = Field(
        default=None,
        description="Single-line snippet of the most recent message's text content (first non-empty line, truncated to 140 characters). Populated on thread list endpoints alongside `last_activity`; `null` when the thread has no messages, the latest message has no text content (e.g. attachment-only), or the endpoint doesn't compute activity enrichment.",
    )
    last_message_sender: str | None = Field(
        default=None,
        description="Display name of the sender of the most recent message the same message `last_message_preview` snippets. Populated on thread list endpoints; `null` when the thread has no messages or the endpoint doesn't compute activity enrichment.",
    )
    metadata: dict[str, Any] | None = Field(
        default=None,
        description="Arbitrary key-value metadata attached to the thread. Shape is application-defined; `null` if no metadata has been set.",
    )
    muted: bool | None = Field(
        default=None,
        description="Whether the authenticated user has muted notifications for this thread. `true` suppresses all notification delivery.",
    )
    org: str | None = Field(
        default=None,
        description="ID of the organization this thread belongs to (`org_...`). `null` for threads outside an org context.",
    )
    parent_message: Message | None = Field(
        default=None,
        description="The message that spawned this thread as a sub-thread. `null` for top-level threads.",
    )
    participant: list[str] | None = Field(
        default=None,
        description="Array of participant user IDs (`usr_...`) who are members of this thread.",
    )
    participants: list[User] | None = Field(
        default=None,
        description="Expanded participant user objects for each member of this thread. Populated only when the association is loaded.",
    )
    participating_actor: list[str] | None = Field(
        default=None,
        description="Composite actor identifiers for all participants currently active in this thread. Present only when actor enrichment is requested.",
    )
    participating_agents: list[Agent] | None = Field(
        default=None,
        description="Expanded agent objects for all agents participating in this thread. Present only when agent enrichment is requested.",
    )
    role: str | None = Field(
        default=None,
        description='The authenticated user\'s membership role in this thread, e.g. `"owner"`, `"member"`, or `"viewer"`. `null` if the user is not a member.',
    )
    sandbox: str | None = Field(
        default=None,
        description="ID of the developer sandbox this thread is scoped to (`dsb_...`). `null` for production threads.",
    )
    settings: ThreadSettings | None = Field(
        default=None,
        description="Per-thread configuration settings controlling AI agent behavior for this thread.",
    )
    slug: str | None = Field(
        default=None,
        description="URL-safe slug for the thread, used in human-readable permalinks. `null` if not assigned.",
    )
    sub_threads: list[dict[str, Any]] | None = Field(
        default=None,
        description="Threads that are nested under this thread as replies to a parent message. Present only when sub-thread enrichment is requested.",
    )
    tags: list[str] | None = Field(
        default=None,
        description='Status tags on the thread (e.g. `"blocked"`, `"needs-review"`). Edited by any thread participant via the `/threads/:thread/tags` endpoints and filterable on the thread list endpoints. Empty array if none set.',
    )
    team: str | None = Field(
        default=None,
        description="ID of the team that owns this thread (`team_...`). `null` for user-owned or agent-owned threads.",
    )
    title: str | None = Field(
        default=None,
        description="Human-readable name of the thread. `null` if no title has been set.",
    )
    ttl: str | None = Field(
        default=None,
        description="Offset-free expiry timestamp after which the thread may be automatically cleaned up. `null` if the thread does not expire.",
    )
    unread_count: int | None = Field(
        default=None,
        description="Number of messages in this thread that the authenticated user has not yet read. Present only when read-state enrichment is requested.",
    )
    updated_at: str | None = Field(
        default=None, description="When the thread was last modified (ISO 8601)."
    )
    user: str | None = Field(
        default=None,
        description="ID of the user who owns this thread (`usr_...`). `null` for team-owned or agent-owned threads.",
    )
    visibility: Literal["team", "restricted", "private"] = Field(
        ...,
        description="Who can read the thread: `team` for every owning-team member, `restricted` for team-readable threads with an explicit roster, or `private` for roster-only access.",
    )


class ThreadMessage(BaseModel):
    """
    A single message posted to a thread, as seen from the developer portal. Includes sender information, optional attachments, and scoping identifiers.
    """

    acl: Acl | None = Field(
        default=None,
        description="Access control list for private messages. Only returned to resource owners (and privileged/org-admin viewers); `null` otherwise.",
    )
    admin: dict[str, Any] | None = Field(
        default=None,
        description="Admin-only diagnostic metadata for the message, including execution trajectory details. Only present in developer portal responses.",
    )
    agent: str | None = Field(
        default=None,
        description="Agent ID (`agt_...`) associated with the message. `null` if not agent-scoped.",
    )
    agent_mode: Literal["cli", "embedded"] | None = Field(
        default=None,
        description="Local agent execution mode for this message. One of `cli`, `embedded`, or `null` when the message was not created by a local agent execution path.",
    )
    app: str | None = Field(
        default=None, description="App ID (`app_...`) that the message belongs to."
    )
    attachments: list[Attachment] | None = Field(
        default=None,
        description="Files or media attached to the message. Empty array if no attachments are present.",
    )
    content: str | None = Field(
        default=None,
        description="Text content of the message. `null` if the message contains only attachments.",
    )
    created_at: datetime | None = Field(
        default=None, description="When the message was posted to the thread (ISO 8601)."
    )
    id: str = Field(..., description="Message ID (`msg_...`).")
    metadata: dict[str, Any] | None = Field(
        default=None,
        description="Key-value metadata attached to the message. Always present; defaults to an empty object. The `metadata` query parameter filters on this same object, so a caller can read back the field it selects on.",
    )
    org: str | None = Field(
        default=None,
        description="Organization ID (`org_...`) this message is scoped to. `null` if not org-scoped.",
    )
    root_message_id: str | None = Field(
        default=None,
        description="ID of the root message in this reply chain (`msg_...`). `null` for a top-level message. The value is persisted when the reply is created, so callers can correlate a multi-turn session without walking parent messages.",
    )
    sandbox: str | None = Field(
        default=None,
        description="Sandbox ID (`dsb_...`) this message is scoped to. `null` if not sandbox-scoped.",
    )
    sender: str | None = Field(
        default=None,
        description="Public ID of the sender (e.g. `usr_...` or `agt_...`). `null` for system-generated messages.",
    )
    sender_name: str | None = Field(
        default=None, description="Display name of the message sender. `null` if unavailable."
    )
    sender_type: str | None = Field(
        default=None,
        description='Category of entity that sent the message. One of `"user"`, `"agent"`, or `"system"`.',
    )
    team: str | None = Field(
        default=None,
        description="Team ID (`tea_...`) associated with the message. `null` if not team-scoped.",
    )
    type: str | None = Field(
        default=None,
        description="Optional client-defined classification for the message (for example `note` or `status`). Free-form string up to 64 characters. The value `system` is reserved for platform-authored messages and cannot be set by clients. `null` when unset.",
    )
    user: str | None = Field(
        default=None,
        description="User ID (`usr_...`) associated with the message. `null` if not user-scoped.",
    )
    visibility: Literal["default", "private"] | None = Field(
        default=None,
        description="Message-level visibility. `default` follows thread membership; `private` is limited to the sender and ACL `read` grantees.",
    )


class ThreadReadStatus(BaseModel):
    """
    The read status of a thread for a specific user, indicating how far they have read and how many messages remain unread.
    """

    last_read_message: str | None = Field(
        default=None,
        description="Message ID (`msg_...`) of the last message the user has read in this thread. `null` if the user has never read any message in the thread.",
    )
    thread: str = Field(..., description="Thread ID (`thr_...`) that this read status belongs to.")
    unread_count: int = Field(
        ..., description="Number of messages in the thread that the user has not yet read."
    )
