# Copyright (c) 2026 ArchAstro Inc. All Rights Reserved.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 7beed6fceff3

from datetime import datetime
from typing import Any

from pydantic import BaseModel


# API schema for an inbox notification (recipient view).
class Notification(BaseModel):
    archived_at: datetime | None = None  # When the recipient archived this (nil if not archived)
    created_at: datetime  # When the notification was sent
    id: str  # Notification ID (ntf_...)
    read_at: datetime | None = None  # When the recipient marked this read (nil if unread)
    rendered: dict[str, Any]
    status: str  # unread | read | archived
    type: str  # Notification type (e.g., app_info, custom:deploy_complete)


# API schema for a single notification preference row.
# The recipient (`user_id` / `developer_id`) is intentionally omitted
# every row returned to a viewer belongs to that viewer, so echoing the
# id back adds noise without information.
class NotificationPreference(BaseModel):
    app_id: str | None = None  # App scope for the preference (nil = system / no-app slot)
    channel: str  # Channel (e.g., "email"). `:in_app` is never a preference row.
    created_at: datetime | None = None  # Created timestamp
    enabled: bool  # Whether the channel is enabled for this slot
    id: str  # Preference ID (ntfp_...)
    type: str
    updated_at: datetime | None = None  # Updated timestamp


# Schema for a list of notification preferences.
class NotificationPreferenceList(BaseModel):
    data: list[NotificationPreference]  # Notification preferences for the current viewer
