# Copyright (c) 2026 ArchAstro Inc. Licensed under the MIT License.
# This file is auto-generated by @archastro/sdk-generator. Do not edit.
# Content hash: 6e1e4ed81463

from datetime import datetime
from typing import Any

from pydantic import BaseModel, Field


class AutomationParticipantSlot(BaseModel):
    """
    A named participant slot declared by an automation's workflow. Embedded stages hand work to the agent the invoker names for the slot.
    """

    description: str | None = Field(
        default=None,
        description="Workflow-authored explanation of the slot's role. `null` when the workflow declares none.",
    )
    name: str = Field(
        ...,
        description="The slot's name, as referenced by the workflow. Supply the chosen agent under the top-level `participants[name]` field when invoking.",
    )
    required: bool = Field(
        ...,
        description="Whether the workflow requires this slot to be filled for the run to complete its embedded stages.",
    )
    type: str = Field(
        ...,
        description='The kind of principal the slot accepts. Currently always `"agent_user"` the value supplied at invoke is an agent ID (`agi_...`).',
    )


class AutomationPrefills(BaseModel):
    """
    Locked payload and participant values supplied by the automation owner.
    """

    participants: dict[str, Any] | None = Field(
        default=None,
        description="Participant slot-to-agent mappings applied by the platform. Caller values at these slots must match exactly.",
    )
    payload: dict[str, Any] | None = Field(
        default=None,
        description="Partial invocation payload applied by the platform. A caller may omit these values, but supplying a different value at any locked path is rejected.",
    )


class AutomationRun(BaseModel):
    """
    A single execution of an automation triggered by a platform event or direct invocation. Captures the run's status, input payload, and final result.
    """

    app: str = Field(..., description="ID of the app that owns this automation run (`dap_...`).")
    automation: str = Field(..., description="ID of the automation that was executed (`aut_...`).")
    created_at: datetime | None = Field(
        default=None, description="When the automation run was created (ISO 8601)."
    )
    event_id: str | None = Field(
        default=None,
        description="ID of the platform event that triggered this run. `null` for directly invoked automations.",
    )
    id: str = Field(..., description="Automation run ID (`atr_...`).")
    participants: dict[str, Any] | None = Field(
        default=None,
        description="Invoke-time map of symbolic participant references to agent IDs. `null` when no participants were supplied.",
    )
    payload: dict[str, Any] | None = Field(
        default=None,
        description="The input event payload that triggered this run. Structure varies by automation type. Defaults to an empty object if no payload was provided.",
    )
    result: dict[str, Any] | None = Field(
        default=None,
        description="The output produced after the automation finished executing. Contains workflow-defined keys alongside any returned output. `null` if the run has not yet completed.",
    )
    status: str = Field(
        ...,
        description='Current execution status of the run. One of `"pending"` (queued, not yet started), `"running"` (actively executing), `"completed"` (finished successfully), `"failed"` (finished with an error), or `"cancelled"` (stopped before completion).',
    )
    team: str | None = Field(
        default=None,
        description="ID of the team that owns this run (`tea_...`). `null` if the run is owned by a user rather than a team.",
    )
    updated_at: datetime | None = Field(
        default=None, description="When the automation run record was last updated (ISO 8601)."
    )
    user: str | None = Field(
        default=None,
        description="ID of the user that owns this run (`usr_...`). `null` if the run is owned by a team rather than a user.",
    )
